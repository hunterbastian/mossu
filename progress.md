Original prompt: lets keep improving the UI for the game and interactivlity

Current snapshot (2026-05-13):

- Water refactor closeout tasks: added `src/render/world/WATER_README.md` as a compact ownership map for the water stack, added named intent comments to each profile in `waterProfiles.ts`, and added a `water-shader-chunks` contract that compiles fake surface/fill/volume shader hooks and guards against duplicate shared wave frequency declarations. Captured fresh installed-Chrome water tuning screenshots after the refactor for great lake shore, Silver Bend, highland creek, and shrine approach; artifacts are in `output/water-tuning-refactor/` with `summary.json` confirming no browser messages and matching rendered/gameplay surface Y at each sampled water location. Verification so far: focused lint and contract TypeScript build pass; full gate still pending for the final commit.

- Water shader chunk helper refactor: extracted shared wave uniform declarations and ripple-ring GLSL generation into `src/render/world/waterShaderChunks.ts`, so the surface, fill, and volume shader helpers no longer duplicate the ripple lifetime/limit loop or shared wave uniform declarations. The first installed-Chrome route guard caught a real duplicate `uBaseFrequency`/`uDetailFrequency` declaration in the surface fragment after the extraction; the helper wiring was corrected by removing the now-duplicated surface-local declarations and rerunning the render guard. No numeric shader tuning, material values, geometry, traversal thresholds, profile values, package state, swim contracts, or public water-surface interface intentionally changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The corrected route guard is green at `101.5fps` average / `15.5ms` p95 with `12/12` checkpoints and no console shader errors, with artifacts in `output/perf-guard/water-shader-chunks-fixed/` and `output/perf-guard/water-shader-chunks-fixed-headless.json`.

- Water surface shader helper refactor: extracted the main water-surface `onBeforeCompile` hook into `src/render/world/waterSurfaceShader.ts`, leaving `waterSurfaceFactory.ts` as an 88-line assembler for geometry, materials, layer meshes, shader configurators, and the runtime controller. The moved hook keeps the same shared wave/ripple uniforms, painterly foam texture, scene-lighting registration, vertex displacement GLSL, surface color/foam/glint GLSL, depth-debug path, and map-lookdown path. No shader strings, uniform values, render order, mesh names, material values, geometry algorithm, traversal thresholds, profile values, package state, swim contracts, or public water-surface interface changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The route guard is green at `141.2fps` average / `10.2ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/water-surface-shader/` and `output/perf-guard/water-surface-shader-headless.json`.

- Water fill shader helper refactor: extracted the underfill/fill-layer `onBeforeCompile` hook into `src/render/world/waterFillShader.ts`, leaving `waterSurfaceFactory.ts` to assemble the controller and call the focused volume/fill shader configurators. The moved hook keeps the same shared wave/ripple uniforms, underfill offset, vertex displacement GLSL, fill color/alpha GLSL, and depth-debug behavior so the underfill still rides the same waves as the main surface. No shader strings, uniform values, render order, mesh names, material values, geometry algorithm, traversal thresholds, profile values, package state, swim contracts, or public water-surface interface changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The route guard is green at `117.8fps` average / `12.7ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/water-fill-shader/` and `output/perf-guard/water-fill-shader-headless.json`.

- Sea-of-Thieves-inspired water color blend pass: tightened the open-ocean and atlas-ocean shader color stack around the requested blend without changing water physics, traversal, mesh topology, route definitions, save data, packages, or swim contracts. `src/render/world/oceanSystem.ts` now separates deeper near blue, subsurface turquoise on sunlit wave faces, paler cyan far water, stronger dark troughs, view-angle/Fresnel lift, and sun-direction glint threads/pin highlights. `src/render/app/islandViewerOcean.ts` mirrors the same color logic for no-UI/full-island reference captures with a fixed sun vector. Verification: focused `eslint`, `tsc --noEmit`, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The route guard is green at `119.1fps` average / `12.5ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/sot-water-color/` and `output/perf-guard/sot-water-color-headless.json`.
- Distant sky/backdrop reference pass: tuned the gameplay sky and far set pieces toward the latest floating-islands-above-ocean reference while keeping the backdrop made of distant-only elements. `src/render/world/atmosphereSystem.ts` now has a deeper cyan-blue upper sky fading into pale turquoise horizon haze, larger/slower far cloud banks pushed to the horizon/edge frame, reduced near-sky wisps, a tighter low cloud shelf, and sparse sun-path horizon sparkles. `src/render/world/worldSetPieces.ts` now lays out more faraway floating islands across a wide horizon arc, with a washed haze tier for tiny silhouettes and no nearby skybox props that would expose fake parallax while Mossu moves. No gameplay terrain, traversal, water physics, movement, save data, packages, or route definitions changed. Verification: focused `eslint`, `tsc --noEmit`, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The route guard is green at `152.6fps` average / `8.4ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/distant-sky-reference/` and `output/perf-guard/distant-sky-reference-headless.json`; one-off low-camera Chrome captures were attempted under `output/sky-backdrop-close-check/`, but the maintained route/perf artifacts are the reliable runtime evidence for this pass.
- Reference wave shader pass: tuned the open ocean and island-atlas ocean toward the latest close-up tropical wave reference without changing gameplay water, terrain, routes, movement, save data, swim contracts, packages, or draw topology. `src/render/world/oceanSystem.ts` now uses taller/steeper Gerstner breaker layers, brighter turquoise wave-face tinting, stronger dark trough contrast, long white foam lace, and procedural crest-spray speckles. `src/render/app/islandViewerOcean.ts` mirrors the same taller breaker profile, turquoise wave-face tint, foam lines, and spray speckles for full-island/reference-aerial captures. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build (`PATH=/usr/local/bin:... /usr/local/bin/npm run build`), installed-Chrome route perf, production atlas capture, and `git diff --check` pass. The route guard is green at `145.7fps` average / `8.9ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/reference-wave/` and `output/perf-guard/reference-wave-headless.json`; the production atlas artifact is `output/reference-wave-pass/reference-aerial-wave.png` plus `state.json`. A more aggressive foam-sheet variant was tested and reverted after strict route perf failed at Highland Basin and Windstep Shelf.
- Water volume shader helper refactor: extracted the volume-layer `onBeforeCompile` hook into `src/render/world/waterVolumeShader.ts`, leaving `waterSurfaceFactory.ts` to assemble the controller and call focused shader configurators. The moved hook keeps the same shared wave uniforms, color/profile uniform assignment, vertex displacement GLSL, volume color/alpha GLSL, and depth-debug behavior. No shader strings, uniform values, render order, mesh names, material values, geometry algorithm, traversal thresholds, profile values, package state, or public water-surface interface changed. Verification: focused `eslint`, `tsc --noEmit`, and `git diff --check` pass.
- Water surface layer helper refactor: extracted fill/volume layer mesh construction and volume-geometry creation into `src/render/world/waterSurfaceLayers.ts`, leaving `waterSurfaceFactory.ts` to wire the layers into the controller and shader hooks. No shader strings, render order, mesh names, material values, geometry algorithm, traversal thresholds, profile values, package state, or public water-surface interface changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, and `git diff --check` pass.
- Water scene uniform helper refactor: extracted water scene-lighting fallback uniforms and `material.userData.waterShader` registration into `src/render/world/waterSceneUniforms.ts`, so `waterSurfaceFactory.ts` no longer owns scene color construction or scene uniform registration. No shader strings, uniform values, materials, geometry, traversal thresholds, profile values, package state, or scene-lighting update behavior changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, and `git diff --check` pass.
- Water surface controller refactor: extracted the runtime `WaterSurface` controller class and its resource/dispose/update bookkeeping into `src/render/world/waterSurfaceController.ts`, leaving `waterSurfaceFactory.ts` focused on surface geometry/material/layer shader construction. No shader strings, uniforms, materials, geometry, traversal thresholds, profile values, package state, or public water-surface interface changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, and `git diff --check` pass.
- Water profile uniform helper refactor: extracted profile-driven scalar uniform assignment into `src/render/world/waterProfileUniforms.ts`, so depth, shoreline, foam, clarity, caustic, sparkle, current, and hero-specular knobs are wired through one helper instead of inline in `waterSurfaceFactory.ts`. No shader strings, scalar values, geometry, material values, traversal thresholds, profile data, or package state changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, installed-Chrome route perf, and `git diff --check` pass. The route guard is green at `130.6fps` average / `10.4ms` p95 with `12/12` checkpoints, with artifacts in `output/perf-guard/water-profile-uniforms/` and `output/perf-guard/water-profile-uniforms-headless.json`.
- Water color helper refactor: extracted profile-to-`Color` construction plus surface/fill/volume color-uniform assignment into `src/render/world/waterSurfaceColors.ts` and passed that bundle through the material helper. `waterSurfaceFactory.ts` still owns shader GLSL injection and non-color profile uniforms, but no longer owns water profile color instantiation or repeated color uniform wiring. No shader strings, material values, geometry, traversal thresholds, profile values, or package state changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, forced-PATH production build, and `git diff --check` pass.
- Water material helper refactor: extracted surface/fill/volume `MeshBasicMaterial` creation and profile-specific opacity/color setup into `src/render/world/waterSurfaceMaterials.ts`. `waterSurfaceFactory.ts` now keeps layer mesh wiring plus shader injection, while material tuning lives beside the shared uniform helper as a smaller focused surface. No shader strings, traversal thresholds, geometry, profile values, or package state changed. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, and `git diff --check` pass.
- Water uniform helper refactor: extracted the shared surface/fill/volume wave/ripple uniform setup into `src/render/world/waterShaderUniforms.ts` and replaced the three duplicated `onBeforeCompile` assignment blocks in `src/render/world/waterSurfaceFactory.ts`. This keeps the behavior-preserving split moving without changing shader math, geometry, traversal, profiles, or package state, and makes the critical underfill rule harder to regress because all water layers now receive the same shared uniform object through one helper. Verification: focused `eslint`, `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, and `git diff --check` pass.
- Giant water-system refactor pass: split the runtime water stack into focused ownership modules without changing route definitions, terrain samplers, swim thresholds, movement physics, save data, packages, or shader math. `src/render/world/waterSystem.ts` is now an 80-line facade/coordinator that owns the public `WaterSystem` API, base group setup, depth-debug flag, controller list, and compatibility exports. New modules own the rest: `waterBodies.ts` builds river/lake/creek groups, `waterSurfaceFactory.ts` owns the shader-backed `WaterSurface` and WebGL material factory, `waterRipples.ts` owns actor ripple lifecycle/emission, `waterWaterfalls.ts` owns decorative waterfall panels/spray, and `waterTypes.ts` owns shared water controller/ripple types. Verification: local `eslint`, local `tsc --noEmit`, contract TypeScript build, esbuild contract bundle, contract runner, and `git diff --check` pass. Later verification found the Rollup loader caveat was caused by the Codex shell resolving `node` to `/Applications/Codex.app/Contents/Resources/node`; forcing `/usr/local/bin` first in `PATH` lets production Vite build and installed-Chrome capture run.
- Water bank close-up/probe pass: added a `?qaDebug=1` water probe readout and text-state sample so future tuning can see Mossu's current water kind/profile, depth, bank mask, swim eligibility/state, rendered surface Y, gameplay water Y, terrain/player Y, and flow direction at the player position. `src/simulation/world.ts` now exposes `sampleWaterProbe()` from the same water-selection logic as swim traversal, `src/render/app/GameApp.ts` renders the small debug panel, and `src/render/app/appTextState.ts` includes the probe in `render_game_to_text`. Wake/ripple emission now takes actor movement state instead of only a binary force flag, so Mossu/Karu speed, shallow wading, swim/splash state, roll movement, and entry/exit transitions drive stronger rings without changing swim physics or traversal thresholds. Fresh evidence is in `output/water-bank-closeup-pass/`: installed Chrome sampled opening pools, great lake shore, Silver Bend, highland creek, and shrine approach with screenshots plus `summary.json`; shallow banks report wading with `swimAllowed=false`, the highland creek reports `swimmingSurface`, and rendered/gameplay surface Y match at every sampled spot. Verification: `npm run lint`, `npm run qa`, installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/water-bank-probe-headless.json --screenshot-dir=output/perf-guard/water-bank-probe` pass (`159.9fps` average / `8ms` p95 / `8.8ms` p99, `12/12` checkpoints), and `git diff --check` pass. Caveat: `npm run test:e2e:smoke` was attempted and still fails before test bodies because the local Playwright `chromium_headless_shell-1217` executable is missing.
- Terrain/island/water refactor pass: split behavior-preserving geometry and shader helpers out of the largest render modules while keeping the Sea-of-Thieves-style ocean/water changes intact. `src/render/world/waterSystem.ts` now delegates river/lake surface, filled-surface sampling, flow-direction, and underfill volume mesh construction to `src/render/world/waterSurfaceGeometry.ts` while preserving the old `sampleRenderedWaterSurfaceY` export for contracts. `src/render/app/IslandViewerApp.ts` now delegates the atlas ocean material and tessellated ocean disc to `src/render/app/islandViewerOcean.ts`, and `src/render/world/worldSetPieces.ts` now delegates the smooth terrain-derived floating-island underside mesh to `src/render/world/floatingIslandGeometry.ts`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/refactor-water-island-headless.json --screenshot-dir=output/perf-guard/refactor-water-island` pass (`143.2fps` average / `8.8ms` p95 / `9.7ms` p99, `12/12` checkpoints), and installed-Chrome atlas capture passes at `output/refactor-water-island/reference-aerial.png`. The atlas check caught and fixed a moved-shader `uTime` vertex uniform declaration before final verification. Caveat: package Playwright smoke remains blocked by the missing Chromium cache plus `ENOSPC` from the prior browser-install repair attempt.
- Sea-of-Thieves-style ocean/water pass: pushed the provided bright tropical-ocean reference further into motion/physics readability without changing movement, terrain, route definitions, save data, swim contracts, or packages. `src/render/world/oceanSystem.ts` now uses a seven-layer Gerstner stack with broader rolling swells, crest/slope/trough signals passed to the fragment shader, stronger trough contrast, slope-aware sun glints, and breaker foam/foam-tear lines tied to wave energy instead of color noise alone. `src/render/app/IslandViewerApp.ts` now gives the no-UI atlas ocean a tessellated displaced disc and matching crest foam language for reference-aerial captures. `src/render/world/waterProfiles.ts` and `src/render/world/waterSystem.ts` slightly boost main-river/great-lake wave amplitude, foam, current strokes, specular, and Mossu/Karu wake rings while keeping underfill displacement shared with the main surface. Fresh artifacts: `output/ocean-sea-thieves-pass/reference-aerial-sea-of-thieves.png`, `output/perf-guard/ocean-sea-thieves-headless.json`, and route screenshots under `output/perf-guard/ocean-sea-thieves/`. Verification: `npm run lint`, `npm run qa`, `npm run build`, `git diff --check`, installed-Chrome atlas capture, and installed-Chrome route perf pass (`155.1fps` average / `8.7ms` p95 / `9.3ms` p99, `12/12` checkpoints). Caveat: `npm run test:e2e:smoke` remains blocked before test bodies by the missing Playwright `chromium_headless_shell-1217` cache; the approved `npx playwright install chromium` repair failed with `ENOSPC` while downloading the 165 MB browser archive.
- Ocean wave geometry follow-up: pushed the Three.js runtime ocean beyond color-only tuning by adding a stronger six-layer Gerstner stack, a broad hero swell, finer chop/glint layer, a swell signal passed from vertex to fragment, darker trough shading, and crest-aware breaker foam so the sea reads as moving tropical water instead of a flat turquoise grade. Scope stayed inside `src/render/world/oceanSystem.ts`; no movement, terrain, route, save, swim, camera, or progression changes. Fresh evidence: `output/ocean-waves-pass/reference-aerial-ocean-waves.png` and route screenshots under `output/perf-guard/ocean-waves/`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome atlas capture, and installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/ocean-waves-headless.json --screenshot-dir=output/perf-guard/ocean-waves` pass (`121.1fps` average / `12.5ms` p95 / `15.2ms` p99, `12/12` checkpoints).
- Tropical ocean reference pass: tuned the runtime and atlas ocean shaders toward the provided bright tropical water reference without adding packages, meshes, postprocessing, or changing terrain/movement/contracts. `src/render/world/oceanSystem.ts` now uses a larger rolling Gerstner swell, richer blue-to-cyan distance grade, a turquoise lagoon band, broken long foam-lace/current strokes, brighter crest foam, and subtle shallow caustic tinting. `src/render/app/IslandViewerApp.ts` mirrors the atlas ocean with the same saturated blue/cyan/lagoon palette and long white foam streak language so `?islandViewer=1` captures stay aligned with gameplay. Fresh capture: `output/ocean-reference-pass/reference-aerial-ocean.png`; route screenshots: `output/perf-guard/ocean-reference/`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome atlas capture, and installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/ocean-reference-headless.json --screenshot-dir=output/perf-guard/ocean-reference` pass (`120.4fps` average / `12.2ms` p95 / `17.7ms` p99, `12/12` checkpoints).
- Ocean contact foam follow-up: strengthened the reference-aerial surf read around the island base without changing terrain, movement, camera, routes, save data, swim contracts, or progression. `src/render/world/worldSetPieces.ts` now uses fewer/larger broken white-cyan foam ellipses plus a separate dark blue contact-water band just outside the cliff base, with extra emphasis near the front waterfall face. First dense attempt made the surf read stronger but failed perf at Highland Basin (`32.9ms` p95), so the final version reduces transparent draw calls while preserving the scale cue. Fresh capture: `output/reference-aerial-pass/reference-aerial-contact-foam.png`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome no-UI aerial capture, and installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/contact-foam-headless.json --screenshot-dir=output/perf-guard/contact-foam` pass (`113fps` average / `13.8ms` p95 / `18.5ms` p99, `12/12` checkpoints).
- Reference aerial island pass: pushed the full-island read closer to the provided floating-island reference without changing movement, camera feel, route definitions, save data, swim contracts, or progression. `src/simulation/world.ts` now sharpens the coastal cliff falloff, cleans south/front plateau micro-noise, lightly lifts the meadow plateau, and adds a taller needle-like mountain peak/crown read. `src/render/world/worldSetPieces.ts` adds coarse sheer cliff wall slabs around the rim, a visible broken meadow walking loop, and ocean-contact surf foam patches at the base of the island. `src/render/world/terrainMesh.ts`, `src/render/world/oceanSystem.ts`, and `src/render/app/IslandViewerApp.ts` push cleaner grass/plateau color, saturated blue/cyan ocean, brighter open-air sky, and a new `aerial` no-UI atlas capture preset via `?islandViewer=1&referenceAerial=1&e2e=1`. Fresh evidence: `output/reference-aerial-pass/reference-aerial-no-ui.png` and route captures under `output/perf-guard/reference-aerial/`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome no-UI aerial capture, and installed-Chrome route perf via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/reference-aerial-headless.json --screenshot-dir=output/perf-guard/reference-aerial` pass (`140.3fps` average / `9.6ms` p95 / `10.8ms` p99, `12/12` checkpoints). Caveats: `npm run test:e2e:smoke` remains blocked before test bodies by the missing Playwright `chromium_headless_shell-1217` cache, and headed `npm run art:review` still times out waiting for runtime readiness, leaving `npm run art:compare` red on a zero-capture summary; count the installed-Chrome aerial capture plus perf-route screenshots as the current visual evidence.
- Water/terrain material richness pass: made the larger great lake read more like hero water up close while improving broad material separation across the terrain. `src/render/world/waterProfiles.ts` now exposes per-profile art knobs for shoreline definition, shallow/deep depth-band strength, current-stroke strength, and hero specular strength; the still-pool/great-lake profile is brighter, clearer, foamier at the bank, and deeper blue in the core. `src/render/world/waterSystem.ts` uses those knobs for stronger hand-painted shore foam, milkier lake edges, clearer shallow/mid/deep contour ink, brighter current ribbons, and controlled sparkle/sun-path strokes without adding meshes or postprocessing. `src/render/visualPalette.ts` and `src/render/world/terrainMesh.ts` push grass, shore, damp-bank, rock, and snow colors farther apart and align the lake-basin terrain read with the enlarged west lake. Fresh artifacts: `output/water-material-pass/island-viewer-clean.png`, `island-viewer-top.png`, `island-viewer-profile.png`, and route captures under `output/perf-guard/water-material/`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, and installed-Chrome perf/visual diagnostic via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/water-material-headless.json --screenshot-dir=output/perf-guard/water-material` pass (`130.7fps` average / `11.9ms` p95 / `15.6ms` p99, `12/12` checkpoints). Caveats: `npm run test:e2e:smoke` is still blocked before test bodies by the missing Playwright `chromium_headless_shell-1217` cache, and `node scripts/artReviewRoute.mjs --browser=chrome --deterministic-step` timed out waiting for runtime readiness in this Codex desktop session, so the counted visual evidence for this pass is installed-Chrome atlas capture plus perf-route screenshots.
- River and great-lake composition pass: expanded the west lowland lake into a real great-lake landmark and improved the lowland water read without changing movement constants, save data, route definitions, swim thresholds, or progression. `src/simulation/world.ts` enlarges/repositions `great-lake`, makes it deeper/brighter, and pulls the `silver-braid` branch toward the lake while keeping the Burrow movement contract lane dry. `src/render/world/waterSystem.ts` gives the larger lake a higher-resolution still-water surface. `src/render/world/worldSetPieces.ts` now treats the great lake as a major shoreline with broader damp/sand patches, reeds, and visible boulder clusters around the rim. `src/render/app/IslandViewerApp.ts` labels the great lake basin/inlet/rock shelves in the atlas, and `tests/contracts/islandRegionContracts.ts` adds a west great-lake region contract. Fresh atlas captures: `output/water-lake-pass/overview.jpg`, `topdown.jpg`, `profile.jpg`, and `topdown-water-rocks-clean.jpg`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome `?islandViewer=1&cleanCapture=1&e2e=1` captures, and installed-Chrome headless perf diagnostic via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/water-lake-headless.json --screenshot-dir=output/perf-guard/water-lake-headless-route` (`146.1fps` average / `9.4ms` p95 / `10.4ms` p99, `12/12` checkpoints) pass. Caveat: `npm run test:e2e:smoke` is still blocked before test bodies by the missing Playwright `chromium_headless_shell-1217` cache.
- Terrain composition follow-up: softened the front/south cliff strata after the profile capture showed the new terrace read was useful but too slab-like in atlas profile. `src/render/world/worldSetPieces.ts` now uses lighter warm cliff materials, shorter/inset front shelf bands, and smaller staggered face slabs while preserving the stronger hero front waterfall channel and the terrain sampler changes from the main pass. Fresh follow-up atlas captures: `output/terrain-composition-followup/overview.jpg`, `profile.jpg`, `under.jpg`, and `topdown.jpg`; the debug Rocks/Biomes overlays can still exaggerate a dark jagged rim in profile, so judge final art from clean atlas and route captures. Verification: `npm run lint`, `npm run qa`, `git diff --check`, installed-Chrome art review via `node scripts/artReviewRoute.mjs --headless --browser=chrome --deterministic-step` (`6/6` captures after real-time/canvas retry fallbacks), `npm run art:compare`, and installed-Chrome headless perf diagnostic via `node scripts/perfGuard.mjs --browser=chrome --output=output/perf-guard/headless-chrome-latest.json --screenshot-dir=output/perf-guard/headless-chrome-route` (`94fps` average / `15.9ms` p95 / `18.6ms` p99, `12/12` checkpoints) pass. Caveat: the official headed `npm run perf:guard` is not green; one run completed all `12/12` route checkpoints but failed the strict frame budget at `67.7fps` average / `20.6ms` p95 / `23.6ms` p99, and a final rerun timed out at Amber Tree after Burrow. Treat headed route perf as the next verification risk before committing the terrain pass.
- Terrain composition pass: implemented the planned all-three terrain pass with front/south massing as the primary target and route/forest changes as support. `src/simulation/world.ts` reduces the broad smooth front meadow lift, adds a front-arc shelf mask, stronger stepped terrace bands, cove cuts, a deeper hero waterfall face cut, and extra cliff-step/groove shaping while leaving route definitions, movement constants, save data, swim contracts, and progression untouched. `src/render/world/worldSetPieces.ts` adds stacked front cliff-face slabs/ledges and widens/lengthens the hero front waterfalls without adding another global shell layer. `src/render/world/terrainMesh.ts` adds warmer front terrace/cliff/waterfall color reads. `src/render/app/IslandViewerApp.ts` updates the biome-layout notes, adds atlas labels for stepped front shelves and waterfall-face cut, thins forest instances across the broad front meadow, and keeps evergreen density concentrated around the climb/cliff lips. Fresh atlas captures: `output/terrain-composition-pass/overview.jpg`, `profile.jpg`, `under.jpg`, and `topdown.jpg`. Verification: `npm run lint`, `npm run qa`, `git diff --check`, headed `npm run perf:guard` (`111.9fps` average / `10.6ms` p95 / `11.6ms` p99, `12/12` checkpoints), installed-Chrome Karu route probe via `node scripts/karuRouteProbe.mjs --browser chrome --headed` (no follower failures), installed-Chrome deterministic art-review fallback via `node scripts/artReviewRoute.mjs --headless --browser=chrome --deterministic-step` (`6/6` captures), and `npm run art:compare` pass. Caveats: package `npm run test:e2e:smoke` and `npm run test:e2e:visual` are still blocked before test bodies by the missing Playwright `chromium_headless_shell-1217` cache; headed `npm run art:review` still timed out waiting for runtime readiness in this Codex desktop session, so the installed-Chrome fallback is the counted art-review evidence. Visual note: the front face now has more readable terrace/cliff breaks and a stronger waterfall fall line, but it remains a stylized/procedural blockout rather than final concept-art fidelity.
- Codex Security diff scan: ran the `Codex Security` security-scan workflow against the dirty working-tree changes with a repository threat model focused on Mossu's static browser runtime, local storage, debug/query hooks, DOM sinks, and local browser-automation scripts. Discovery reviewed the changed `islandViewer` route, `window` automation/debug bridge behavior, new Karu roster DOM rendering, local save/quality storage paths, `scripts/artReviewRoute.mjs` filesystem/browser sinks, and the browser-extension runtime-error filter. Result: no technically plausible security candidates survived discovery, so validation and attack-path analysis were skipped per the diff-scan workflow. Scan bundle: `/tmp/codex-security-scans/mossu/d8af2f4_20260512T014158Z/report.md`.
- Concept-art reverify/iteration pass: checked the full-island atlas in real Chrome at `http://127.0.0.1:8000/?islandViewer=1&cleanCapture=1&e2e=1`, compared it against the floating-island concept art, and pushed the scene closer without touching controller/camera feel, route definitions, save data, or movement constants. `src/simulation/world.ts` now has a stronger asymmetric front outline, taller mountain crown/source cleft, deeper stepped cascade spine, brighter broken front meadow shelves, and subtle terrace grooves. `src/render/world/worldSetPieces.ts` warms/enlarges cliff strata, adds a few front shelf ledges, makes the hero front waterfall wider/brighter with stronger impact mist, and smooths the underside taper so the profile/under views show a clearer floating-island air gap over the ocean. `src/render/world/terrainMesh.ts` adds an optional atlas-only edge fragment clip, and `src/render/app/IslandViewerApp.ts` enables it for the whole-island viewer only so gameplay/perf routes do not pay that shader cost. `src/main.ts` now ignores browser-extension-origin runtime errors before surfacing Mossu fatal overlays, which kept a local MetaMask Chrome extension error from masking the game during real-browser review. Fresh concept-reverify JPGs: `output/concept-reverify/island-overview-after.jpg`, `island-profile-after.jpg`, `island-under-after.jpg`, and `island-topdown-after.jpg`. Verification: `npm run lint`, `npm run qa`, `npm run build`, full `npm run test:e2e:smoke` (8/8), headed `npm run perf:guard` (`115.1fps` average / `10.5ms` p95 / `13.1ms` p99, `12/12` checkpoints), `node scripts/artReviewRoute.mjs --headless --browser=chrome --deterministic-step --url=http://127.0.0.1:8000/` (`6/6` captures from the running dev server), `npm run art:compare`, and `git diff --check` pass. Caveats: the island is closer but still not fully concept-art-level; the biggest visual mismatch is still the broad smooth front meadow shelf versus the concept's more dramatic layered cliff terraces and waterfall face. `npm run test:e2e:visual`, `npm run karu:route`, and the Chromium deterministic art-review fallback are currently blocked by a missing Playwright cached `chromium_headless_shell-1217` binary, while the headed `npm run art:review` wrapper still timed out waiting for runtime readiness in this Codex desktop session; the installed-Chrome route fallback above is the counted art-review evidence.
- Concept-art island pass: translated the user-provided floating-island reference into the whole-island terrain/shell/atlas systems without touching controller/camera feel, movement constants, save data, or route definitions. `src/simulation/world.ts` now adds a taller protected mountain crown, stepped cascade terraces, side-shoulder massing, and a brighter south/front meadow shelf while shielding the start-route movement lane enough for the roll-drift contract to stay green. `src/render/world/worldSetPieces.ts` warms the cliff/underside palette, keeps 15 edge-waterfall turns, widens the front hero falls, and trims extra vine/grove micro-meshes after profiling so Burrow/Amber/Skyward stay under budget. `src/render/app/IslandViewerApp.ts` updates the atlas blockout notes with concept-art meadow, forest, and rock masses so the full-island viewer matches the new composition. Docs updated: `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, `docs/NEXT_PASSES.md`, and `docs/KNOWN_ISSUES.md`. Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), escalated `npm run test:e2e:visual` (1/1), escalated `npm run karu:route`, headed `npm run perf:guard` (`99fps` average / `14.8ms` p95 / `20.5ms` p99, `12/12` checkpoints, `1656` renderer calls / `472495` tris), deterministic art-review fallback `node scripts/artReviewRoute.mjs --headless --browser=chromium --deterministic-step` (`6/6` captures), `npm run art:compare`, and `git diff --check` pass. Caveat: the headed `npm run art:review` wrapper opened Chrome but timed out waiting for runtime readiness in this Codex desktop session, so the deterministic fallback is the counted art-review artifact path.
- Concept-art board setup: created `docs/concept-art/` as the durable curated reference-art folder, promoted the user-provided floating-island image from Downloads to `docs/concept-art/floating-island/chatgpt-floating-island-mountain-waterfalls.png`, and added notes on what to borrow versus avoid in `docs/concept-art/floating-island/README.md`. Updated `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md` so future island-shape work checks this board instead of relying on gitignored `output/` screenshots. Verification: `git diff --check` passes.
- Meadow title/loading pass: replaced the retro pixel-ocean loading/title treatment with a softer green meadow-hill direction inspired by animated landscape background design: layered hill bands, cream/cyan sky haze, broad warm sun glow, drifting cloud bands, subtle grass sweep, floating-island accents, and a quiet sprout/dew progress bar. Scope stayed UI-only: `index.html` first-paint CSS, `src/styles/theme/pixel-loading.css`, and `src/styles/theme/pixel-title-menu.css`; no gameplay terrain, movement, HUD, save, or Karu logic changed. Used the local `imagegen` skill and saved the reusable concept prompt to `output/imagegen/mossu-title-meadow-ghibli-reference-prompt.md`; the live API generation was blocked by the account's `billing_hard_limit_reached` response after network escalation, so the final pass uses CSS art rather than a generated bitmap asset. Fresh review captures: `output/imagegen/loading-meadow-firstpaint.png`, `loading-meadow-mobile-firstpaint.png`, `title-meadow-desktop-ready.png`, and `title-meadow-mobile-ready.png`. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), and `git diff --check` pass.
- Persistent Karu friends HUD pass: added an always-visible right-side Karu friends rail in `src/render/app/HudShell.ts`, backed by the same saved companion profile data as the field guide. It shows a compact empty state before recruitment, updates live count/mood/reaction as Karu join, and stays visible while the field guide is open without blocking gameplay input. `src/styles/hud.css` adds the fixed glass rail plus mobile sizing, and `tests/e2e/smoke.spec.ts` now asserts the rail's empty state, 1/2 friend counts, and overlay persistence. Docs updated: `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, and `docs/PLAYTEST_CHECKLIST.md`. Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), and `git diff --check` pass.
- Island terrain-detail and favicon pass: refreshed `public/favicon.svg` into a warmer painterly floating-island/Mossu icon, then tightened the live island silhouette without touching controller/camera feel. `src/simulation/world.ts` now uses a less board-like base contour plus stronger authored headlands/coves shared by the terrain contour and debug boundary. `src/render/world/worldSetPieces.ts` adds tiny perched grove clusters near selected cliff lips, converts the old dark rectangular cliff stickers into thin horizontal ledge strata, and keeps the smooth underside/ocean gap intact. `src/render/world/terrainDecorations.ts` adds sparse pale half-buried stepping stones with grass tufts along the route instead of obvious road geometry. `src/render/world/worldSetPieces.ts`, `src/render/world/grasslandImmersion.ts`, and `src/render/world/WorldRenderer.ts` make the terrain cloud-shadow patches broader, slower, and lightly persistent. `src/render/app/IslandViewerApp.ts` adds `?cleanCapture=1` so clean reference JPGs can hide foreground atlas cloud planes without changing the normal atlas view. Docs updated: `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, `docs/NEXT_PASSES.md`, and `docs/KNOWN_ISSUES.md`. Fresh clean atlas references: `output/debug-viewers/island-terrain-detail-pass/01-overview-clean.jpg`, `02-topdown-asymmetric-outline.jpg`, `03-profile-ocean-gap.jpg`, `04-underbelly-smooth-shell.jpg`, `05-east-headlands-clean.jpg`, and `06-west-coves-clean.jpg`. Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), escalated `npm run test:e2e:visual` (1/1), escalated `npm run karu:route`, headed `npm run perf:guard` (`171.5fps` average / `7ms` p95 / `7.5ms` p99, 12/12 checkpoints), escalated headed `npm run art:review` (6/6 captures), `npm run art:compare`, and `git diff --check` pass.
- Smooth underside follow-up: replaced the last cylinder-style underside shell in `src/render/world/worldSetPieces.ts` with a small custom `BufferGeometry` that samples the island boundary, builds six inward/downward rings, vertex-colors from cliff tan into underside olive, and computes normals so the bottom reads as one smooth terrain-derived taper instead of a separate dark saucer. The profile view no longer shows the earlier dark belt, and the under-view keeps the air gap over the lowered ocean. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Fresh atlas references: `output/debug-viewers/island-smooth-underside/01-under-smooth-underside.jpg`, `02-profile-smooth-underside.jpg`, and `03-overview-smooth-underside.jpg`. Verification: `npm run lint`, `npm run build`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), headed `npm run perf:guard` (12/12 checkpoints, `139.7fps` average / `9.2ms` p95 / `11.3ms` p99), and `git diff --check` pass.
- Island terrain/underside shape pass: changed the shared terrain contour from a plain squircle/superellipse into a modest authored outline so `sampleIslandContour()` and `sampleIslandBoundaryPoint()` now agree on the same headlands/coves, then added edge-biased rim shoulders/headland massing in `sampleBaseTerrainHeight()` without changing route shelves or movement contracts. The underside now reads more like a suspended floating island: removed the saucer-like lower belly/skirt piece from `buildFloatingIslandShell()`, lowered the runtime and atlas ocean level to leave a clearer air gap below the island, and moved waterfall impact mist down with the ocean. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Fresh atlas references: `output/debug-viewers/island-terrain-shape/01-overview-terrain-shape.jpg`, `02-topdown-terrain-shape.jpg`, `03-profile-terrain-shape.jpg`, and `04-under-terrain-shape.jpg`. Verification: `npm run lint`, `npm run build`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), escalated `npm run karu:route`, headed `npm run perf:guard` (12/12 checkpoints, `150.5fps` average / `8.3ms` p95 / `9ms` p99), headed `npm run art:review` (6/6 captures), `npm run art:compare`, and `git diff --check` pass. `npm run test:e2e:visual` still fails the pre-existing dirty-checkout Silver Bend contrast guard (`5.3`, expected `> 6.5`).
- Ocean distance color-grade pass: tuned the atlas and runtime ocean shaders so the sea now grades by distance instead of flattening into one blue plane: rich near blue, turquoise-blue mid range, pale cyan far range, and cream/peach haze at the horizon. `src/render/app/IslandViewerApp.ts` now exposes separate atlas near/mid/far/horizon colors, and `src/render/world/oceanSystem.ts` adds a far-ocean color uniform plus distance bands before the existing wave/fresnel/spec/foam treatment. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Verification: `npm run lint`, `npm run build`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), and `git diff --check` pass. Fresh clean atlas references: `output/debug-viewers/island-ocean-grade/01-overview-ocean-grade.jpg`, `02-profile-ocean-grade.jpg`, and `03-underbelly-ocean-grade.jpg`. Dirty-checkout heavier route gates are still not green and look consistent with the already documented non-ocean visual/perf state: escalated `npm run test:e2e:visual` still fails Silver Bend contrast (`5.3`, expected `> 6.5`), and headed `npm run perf:guard` reaches 12/12 checkpoints with `123.4fps` average / `12.9ms` p95 route-wide but fails Burrow Hollow (`49fps`, `22.3ms` p95) and Amber Tree (`24.9ms` p95) checkpoint budgets.
- Under-island mist pass: added a thin pale humidity layer to the shared floating-island shell so the underside feels suspended over wet air/ocean instead of pasted above the water. `src/render/world/worldSetPieces.ts` now adds broken shoreline mist ellipses around the lower shell plus faint per-waterfall air drift and horizontal impact haze below the existing waterfall ribbons; the shared shell means both gameplay and `?islandViewer=1` get the read without changing terrain samplers, movement, camera feel, route data, or the ocean system. Docs updated: `docs/CURRENT_STATE.md`. Verification: `npm run lint`, `npm run build`, `npm run qa`, full `npm run test:e2e:smoke` (8/8), and `git diff --check` pass. Fresh clean atlas references: `output/debug-viewers/island-under-mist/01-overview-under-mist.jpg`, `02-profile-under-mist.jpg`, and `03-underbelly-mist-ocean.jpg`. Next risk: judge the mist in a manual desktop route/profile walk if the shell starts feeling too foggy from player-height cameras.
- Island biome blockout hierarchy pass: organized `?islandViewer=1` into named Three.js scene folders for `Terrain`, `Water`, `Forests`, `Meadows`, `Rocks`, `Landmarks`, `Lighting`, and `Debug/Blockout`; split the visible biome overlay into meadow/forest/rock blockout groups with clear object names; added atlas text-state hierarchy/biome-layout notes; renamed the layer controls to read as Rocks and Landmarks; and documented the layout in `docs/ISLAND_BIOME_BLOCKOUT.md`. This stays scoped to the debug atlas and does not change gameplay route samplers, movement, save state, or the runtime world renderer. Fresh artifacts: `output/island-biome-blockout/atlas-hierarchy-overview.png` and `output/island-biome-blockout/atlas-hierarchy-state.json`. Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (8/8 before the final wrapper-name cleanup), targeted `npm run test:e2e:smoke -- -g "island viewer route loads terrain atlas"` after the cleanup, production atlas capture/state assertion, and `git diff --check` pass. Broader gameplay route gates are not green in this dirty checkout and appear unrelated to the atlas-only change: escalated `npm run test:e2e:visual` fails Silver Bend contrast (`5.3`, expected `> 6.5`), headed `npm run perf:guard` rerun reaches 12/12 checkpoints but misses route budgets (`96.7fps` average / `19.3ms` p95; Amber Tree p95 `22.8ms`), headed `npm run art:review` closes before runtime readiness, and the deterministic art-review fallback fails canvas PNG capture for `00-burrow-hollow`.
- Hand-painted cloud-bank follow-up: replaced the atlas cloud layer's stretched single-blob shapes with clustered puffy cloud banks for a softer anime-adventure read. `src/render/app/IslandViewerApp.ts` now builds each `?islandViewer=1` horizon bank from multiple overlapping cream highlight, peach body, and cool blue underside lobes, while preserving the existing slow drift/parallax update. Verification: `npm run lint`, `npm run build`, `git diff --check`, and built-site captures pass. Fresh artifacts: `output/debug-viewers/island-painted-clouds/01-overview-painted-clouds.jpg`, `02-profile-painted-clouds.jpg`, and `03-under-painted-clouds.jpg`.
- Peach/cream horizon haze tweak: strengthened the `?islandViewer=1` atlas horizon so the sky and far ocean meet through a warmer cream-peach mist band instead of a mostly turquoise seam. `src/render/app/IslandViewerApp.ts` now has dedicated peach and cream haze uniforms in the atlas sky shader and a subtle cream mist color in the atlas ocean shader near grazing/far horizon angles. Verification: `npm run lint`, `npm run build`, `git diff --check`, and built-site captures pass. Fresh artifacts: `output/debug-viewers/island-peach-haze/01-overview-peach-haze.jpg`, `02-profile-peach-haze.jpg`, and `03-under-peach-haze.jpg`.
- Island overview spec pass: translated the provided Mossu island overview reference into the terrain/water/atlas composition. `src/simulation/world.ts` now has a stronger central alpine spine/source cleft, a west-side great-lake basin that stays clear of Burrow Hollow contracts, and a longer river system from high source toward the lower meadow. `src/render/world/terrainMesh.ts`, `waterProfiles.ts`, and `waterSystem.ts` push the overview toward readable meadow/forest/alpine/cliff/water bands with bluer map-lookdown water and restored route-ground contrast at Silver Bend. `src/render/world/worldSetPieces.ts` keeps the shell from drawing a large atlas rim bar and adds flatter cliff strata around the exterior. `src/render/app/IslandViewerApp.ts` adds the atlas `biomes` layer, denser non-grid evergreen masses, improved overview camera/fog, brighter atlas water, and default guide/pin suppression for cleaner beauty captures. Fresh artifacts: `output/island-spec-pass/atlas-canvas-overview-final-verified.png`, `output/island-spec-pass/atlas-state-overview-final-verified.json`, plus refreshed route/perf/art-review artifacts under `output/perf-guard/route/` and `output/art-review-route/`. Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (8/8), escalated `npm run karu:route`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard` (`118.6fps` average / `14ms` p95 / `16.8ms` p99, 12/12 checkpoints), escalated headed `npm run art:review` (6/6 captures), `npm run art:compare`, and `git diff --check` pass. Next risk: the spec-sheet overview now reads at island scale, but the low meadow foreground is still intentionally open; a future manual beauty pass can add authored meadow texture/landmarks without changing the terrain contract.
- Anime sky/horizon follow-up for the atlas: pushed the `?islandViewer=1` sky toward a warmer painted adventure look without touching gameplay route rendering. `src/render/app/IslandViewerApp.ts` now gives the atlas sky a clearer vertical color story (saturated blue top, soft cyan mid, warm cream horizon, pale turquoise lower mist), a broad off-axis afternoon haze, stronger shader cloud masks, and a small world-fixed cloud-bank group that drifts slowly for parallax while orbiting/flying around the island. Verification: `npm run lint`, `npm run build`, `npm run test:e2e:smoke` (8/8), `git diff --check`, and built-site browser captures pass. Fresh artifacts: `output/debug-viewers/island-ghibli-sky/01-overview-ghibli-sky.jpg`, `02-profile-ghibli-sky.jpg`, and `03-under-ghibli-sky.jpg`. `npm run test:e2e:visual` is still blocked in this dirty checkout by the gameplay Silver Bend contrast guard (`5.3`, expected `> 6.5`) after the usual sandbox preview `listen EPERM` retry; this atlas-only pass does not touch gameplay rendering, but the current checkout is not a clean visual-guard green state. Next risk: the profile preset now shows the cloud banks clearly, but their silhouettes are still procedural/elliptical; if the atlas needs final presentation stills, shape those clouds into softer hand-painted clumps.
- Island atlas sky/horizon pass: tuned the `?islandViewer=1` reference view so whole-island captures have a less flat cyan backdrop and a cleaner ocean-below read without touching gameplay terrain, movement, route samplers, controller feel, or the runtime ocean system. `src/render/app/IslandViewerApp.ts` now uses a camera-following stylized sky dome, a larger shader ocean plane with distance/grazing horizon fade, and softer fog for atlas/reference captures. Fresh artifacts: `output/debug-viewers/island-horizon-final/01-overview-horizon.jpg`, `02-profile-ocean-horizon.jpg`, and `03-underbelly-ocean-horizon.jpg`. Verification: `npm run lint`, `npm run build`, `npm run test:e2e:smoke` (8/8), escalated `npm run test:e2e:visual` (1/1 after the sandbox blocked preview binding), `git diff --check`, and visual capture pass. `npm run qa` is currently blocked by pre-existing dirty-checkout simulation contract failures (`start-burrow` habitat reads `shore` instead of `meadow`, and hillside nest route-read dirt/clearing expectations), so do not treat that failure as caused by the atlas sky/horizon patch. Next risk: the high overview is improved but still has a visible sea/sky seam; if the atlas shots need to be presentation-grade, add a dedicated horizon haze band or share more of the gameplay atmosphere treatment.
- Floating-island edge-shape follow-up: removed the runtime shell's round perimeter `SphereGeometry` cliff bulges and the blocky perimeter cliff-face layer after the atlas/reference captures made those gray shapes read like lollipop blobs or placeholder blocks on the island edge. The shell now relies on the continuous skirt/underbelly form, waterfall lips, and mist instead of protruding marker-like chunks. Scope stays inside `src/render/world/worldSetPieces.ts`; no terrain samplers, movement, camera feel, route data, or save state changed. Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (8/8), headed `npm run perf:guard`, and `git diff --check` pass. Perf guard reached `141fps` average / `10.4ms` p95 / `12.4ms` p99 with all 12 checkpoints reached and renderer calls/tris down to `1218` / `456290`. Refreshed clean atlas references with only terrain/shell/ocean/water visible: `output/debug-viewers/island-clean-references/01-clean-overview.jpg`, `02-clean-side-profile.jpg`, and `03-clean-underbelly.jpg`.
- Floating-island waterfall structure iteration: continued the island-shell pass without changing terrain samplers, movement, camera feel, save state, or route data. `src/render/world/worldSetPieces.ts` now single-sources the edge waterfall turn list for the runtime shell and atlas, increases the edge falls from 11 to 15, and gives each fall a small moss/stone source lip plus faint lower mist so the side silhouette reads more like a floating landmass shedding water. `?islandViewer=1` uses the shared list, so the waterfall pins/counts now match the shell automatically. Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (8/8), headed `npm run perf:guard`, `git diff --check`, and a browser atlas probe pass. Perf guard reached `122.2fps` average / `11.9ms` p95 / `16.2ms` p99 with all 12 checkpoints reached after the extra shell waterfall detail. Browser probe at `http://127.0.0.1:8000/?islandViewer=1&e2e=1` confirmed `waterfallMarkers: 15` and stats copy `15 falls`; snapshot artifact: `output/debug-viewers/island-viewer-15-waterfalls-snapshot.md`.
- Cliff/waterfall density follow-up: added more exterior cliff structure and falling-water accents across the island shell without changing terrain samplers, movement, collision contracts, or route data. `src/render/world/worldSetPieces.ts` now adds varied perimeter cliff faces, ledge shelves, darker crease slabs, and 11 edge waterfall ribbons around the island instead of only 3. Headed Chrome `npm run art:review` completed 6/6 captures after the patch, with one recovered Fir Gate canvas retry; the new highland/ridge/shrine frames preserve the ocean-below read while adding more island-side structure. Verification: `npm run lint`, `npm run qa`, escalated `npm run test:e2e:smoke` (7/7), escalated `npm run karu:route`, escalated `npm run test:e2e:visual`, escalated headed `npm run perf:guard`, headed Chrome `npm run art:review`, `npm run art:compare`, and `git diff --check` pass. Perf guard reached `184.2fps` average / `6.8ms` p95 / `7.5ms` p99 with all 12 checkpoints reached after the added cliff/waterfall meshes. Next risk: if a manual beauty walk wants specific lowland waterfalls to be more visible from Burrow/Amber, tune individual waterfall turns/widths instead of adding another broad mesh layer.
- Latest floating-island structure pass: restored the ocean-below read without changing movement, route samplers, or the new controller/camera feel pass. `src/render/world/oceanSystem.ts` raises the far Gerstner ocean from `y=-340` to `y=-292`, still far below the playable route but high enough for ridge/rim cameras to show blue sea under the island silhouette. `src/render/world/worldSetPieces.ts` now treats the island shell as exterior-only cliff skin, lowers/thins the near rim band, slims the underbelly taper, pushes underside haze lower/fainter, and lengthens the edge waterfalls. Headed Chrome `npm run art:review` completed 6/6 captures after the patch; Highland Basin, Ridge Saddle, Moss Crown Shrine, and the perf-guard Skyward Ledge frame now show clearer blue ocean/air below the route. Burrow Hollow and Amber Tree still have broad highland slopes in the far background, so any remaining lowland sea visibility should be an authored overlook/composition pass rather than a shell or performance-budget change. Verification: `npm run lint`, `npm run qa`, escalated `npm run test:e2e:smoke` (7/7), escalated `npm run karu:route`, escalated `npm run test:e2e:visual`, escalated headed `npm run perf:guard`, headed Chrome `npm run art:review`, `npm run art:compare`, and `git diff --check` pass. Perf guard reached `187.6fps` average / `6.5ms` p95 / `7.2ms` p99 with all 12 checkpoints reached; Burrow, Amber, and Skyward all passed their budgets. Fresh artifacts: `output/art-review-route/03-highland-basin.png`, `04-ridge-saddle.png`, `05-moss-crown-shrine.png`, and `output/perf-guard/route/10-skyward-ledge-shelf.png`. Next risk: do a manual desktop-browser walk around Burrow Hollow/Amber Tree/Skyward Ledge if the lowland opening needs stronger ocean glimpse composition.
- Latest art-review route hardening: fixed the screenshot-timeout cascade in `scripts/artReviewRoute.mjs` without changing the existing `?qaDebug=1&visualProbe=1` flow. The route now parses both `--flag value` and `--flag=value` options, captures screenshots through async canvas PNG/blob retries instead of falling into Playwright page-screenshot timeouts, and makes Karu sync waits advance state directly so a slow headed render loop does not leave partial artifacts. Verification: `node --check scripts/artReviewRoute.mjs`, headed Chrome `npm run art:review`, and `npm run art:compare` pass. Fresh artifacts are in `output/art-review-route/`; `summary.json` reports `completed: true`, `6` captures, all `canvas`, zero fatal browser errors, `browser-render-loop`, and URL `?qaDebug=1&visualProbe=1`. One canvas capture retry recovered on Burrow Hollow and one bounded state-tick warning recovered during Karu sync; neither blocked the completed headed run.
- Latest controller/camera perf triage: profiled the saved Burrow/Amber/Skyward budget miss before touching runtime code. The failed artifact showed `78.9fps` average / `19.6ms` p95 with Burrow Hollow, Amber Tree, and Skyward Ledge over checkpoint budget, and the `pixelRatio=0.92` diagnostic still failed, so this was not a simple render-scale issue. A headed no-screenshot profile on the current dirty checkout measured the route workload at `153.8fps` average / `9ms` p95, a full headed capture profile passed at `157.2fps` average / `9.3ms` p95, and the exact `npm run perf:guard` wrapper now passes at `149.8fps` average / `9.9ms` p95 with all 12 checkpoints reached. No controller/camera or visual-density code was changed because the current route is already under budget and the new feel pass stayed intact. Artifacts: `output/perf-guard/profile-current-skipshots.json`, `output/perf-guard/profile-current-full.json`, and refreshed `output/perf-guard/latest.json`.
- Latest controller + camera feel lab: tuned the moment-to-moment movement layer without adding art/content. Walking input now rises and releases faster, ground acceleration/deceleration/turn correction are stronger, roll exit carries speed longer into normal walking, air momentum and Breeze Float buffers are more forgiving, float exit has a softer gravity handoff, shallow water entry preserves more motion, and wade entry/exit damping is cleaner. `FollowCamera` now recenters sooner after manual look, uses faster walk/roll/air/swim damping, keeps the roll profile active while Mossu is still rolling even during small terrain hops, and exposes camera planar speed for debug probes. Compact and full text-state now expose player planar/vertical speed plus key movement flags. Added movement contracts for walk startup/stop, roll-release carry duration, bounded roll-jump momentum, and upward-arc Breeze Float catch. Browser feel artifact: `output/controller-camera-feel/browser-feel-probe.json` passed all checks (`walk-start-8f` 9.85m/s, `walk-stop-30f` 2.64m/s, roll camera profile stayed `roll`, roll-release carry 22.06m/s, Breeze Float caught, swim entry used swim camera). Verification: `npm run lint`, `npm run qa`, full `npm run test:e2e:smoke` (7/7), escalated `npm run karu:route`, escalated `npm run test:e2e:visual`, and Browser/Playwright feel probe pass. The follow-up perf triage above cleared the earlier `perf:guard` budget miss without runtime changes. The generic develop-web-game screenshot client was attempted through a workspace symlink but hung on canvas choreography/capture and was stopped.
- Latest Spore-like nest/Karu habitat polish: attempted the planned `imagegen` concept-board generation through the local imagegen CLI, but the OpenAI Image API returned `billing_hard_limit_reached`, so no concept PNGs were produced under `output/imagegen/`. Implemented the visual pass directly from the prompt spec using procedural Three.js only: `buildOpeningNestVista()` now has a larger soft biological cup, ribbed pod rim, layered moss cushions, seed-pearl/dew details, sprout arcs, and a clearer warm exit patch toward the first Karu; `buildAmbientBlobNests()` now gives each Karu bedding spot a slightly larger floor, small pod shelter, side leaves, extra cushions, and subtle mood-colored glow seeds. Runtime stays static/batched through the existing world renderer with no new loaders, save schema, or image assets. Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (7/7), escalated `npm run karu:route`, headed Chrome `npm run art:review`, `npm run art:compare`, and `git diff --check` pass. Fresh visual artifacts: `output/art-review-route/00-burrow-hollow.png` and `01-karu-join.png`; the Karu join capture used the page fallback after one canvas timeout, but the headed run completed all 6 captures and `art:compare` passed.
- Latest broad refactor checkpoint: split shared simulation ownership without changing gameplay behavior. `src/simulation/worldTypes.ts` now owns public world/domain types, `src/simulation/worldContent.ts` owns landmark/map/forageable/shadow/scenic/biome-threshold catalog builders, and `src/simulation/progressionObjectives.ts` owns objective copy plus field-guide trail progression. `src/simulation/world.ts` remains the public terrain/water/world sampler contract and re-exports existing type/objective APIs so downstream code stays compatible; direct type consumers now import from `worldTypes`. Added progression contracts for objective sequencing and Summit Circuit trail-progress text. Also fixed the Fir Gate art-review debug stop to use a clearer approach frame and hardened `scripts/artReviewRoute.mjs` around `?qaDebug=1&visualProbe=1` startup. Verification: `node --check scripts/artReviewRoute.mjs`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (7/7), escalated `npm run karu:route`, `npm run art:compare`, and `git diff --check` pass. Headed `npm run art:review` reached startup but hit local Chrome capture timeouts; refreshed artifacts were produced by `node scripts/artReviewRoute.mjs --headless --browser=chromium --deterministic-step`, with `art:compare` passing and warning only about real-time fallback after an `advanceTime` timeout. Next risk: continue large-module work as narrow behavior-preserving slices, starting with `terrainDecorations.ts`, `HudShell.ts`, `WorldRenderer.ts`, `waterSystem.ts`, and `ambientBlobs.ts`, instead of attempting a single rewrite.
- Latest opening cinematic watch pass: played the title-to-gameplay handoff in desktop Chrome and tightened only the opening presentation surface. The sequence now runs in 4.8s, exposes the movement skip after 0.9s, uses a wider aerial valley/FOV reveal with an earlier smooth handoff, fades the opening panel down during handoff, and replaces the first Karu prompt with clearer desktop copy. Real Chrome confirmed the valley/water read, the new `Morning lifts over the valley` overlay copy, `Move to take the trail`, and gameplay handoff. Next risk: judge a shareable capture after a manual full-window recording if the foreground tree framing still feels too heavy.
- Latest Three.js graphics/performance audit: moved grassland pollen/life-signal/leaf-glint point animation from CPU buffer rewrites into a custom `ShaderMaterial`, added lightweight leaf/water glints, culled distant and near-camera tree leaf-wind meshes during route traversal, hardened `art:review` to use `?qaDebug=1` real-browser rendering with canvas-first/page-fallback captures, and expanded `art:compare` with PNG contrast/chroma/detail/distinct-hash validation so stale sky-only frames cannot pass. Debug route anchors now frame Fir Gate, Highland Basin, Ridge Saddle, and the shrine more deliberately, with smoke-test expectations updated. Verification: `npm run lint`, `npm run qa`, headed `npm run perf:guard` (`136.4fps` average, `9.4ms` p95, `12/12` checkpoints), headed `npm run art:review`, `npm run art:compare`, `npm run test:e2e:smoke`, `npm run test:e2e:visual`, `git diff --check`, and the required develop-web-game client pass under `output/web-game-client-check/` all pass. Next risk: Fir Gate's automated frame is valid but still close/occluder-heavy, so it needs a narrow composition pass before treating the screenshot set as presentation-grade.
- Latest five-pass polish: completed the desktop-focused follow-up bundle across art-review reliability, water motion, opening camera, Aero HUD hierarchy, and lightweight world motion. `scripts/artReviewRoute.mjs` now starts gameplay through the debug bridge, clears stale artifacts, uses headed Chrome real-time stepping for visual captures, uses bounded debug-bridge calls for save/Karu sync, records incomplete summaries on failure, and reports whether screenshots came from page or canvas capture. `scripts/artReviewCompare.mjs` now rejects incomplete/stale/missing route artifacts instead of trusting mixed partial runs. Water gained moving lens/current ribbons, bank-lap foam, traveling caustics, stronger downstream profile tuning, and longer Mossu/Karu ripple rings. The opening camera now begins as a wider aerial reveal with softer FOV handoff. Desktop Aero HUD status hierarchy is tighter and more centered, with the objective chip promoted and the breeze chip hidden from the top strip. Grassland ambience gained a bounded leaf/water glint layer. Verification: `node --check scripts/artReviewRoute.mjs`, `node --check scripts/artReviewCompare.mjs`, `npm run lint`, `npm run qa`, headed `npm run art:review`, `npm run art:compare`, escalated `npm run test:e2e:smoke`, and `git diff --check` pass; `output/art-review-route/summary.json` reports 6/6 captures with zero fatal browser errors.
- Previous bug-fix/tooling pass: fixed the art-review route output trap where a partial or stalled `npm run art:review` could leave fresh screenshots next to an old `summary.json`, letting `npm run art:compare` validate mixed artifacts. `scripts/artReviewRoute.mjs` now clears the known route screenshots/JSON/summary before each run, gives page screenshots a 20s timeout, and falls back to direct canvas PNG capture when page screenshotting fails. `scripts/artReviewCompare.mjs` now rejects artifacts newer than the summary, reports missing/invalid summaries as clean actionable errors, and keeps the original caught errors as causes. Verification: stale mixed artifacts were rejected by `npm run art:compare`, current missing-summary output is a short intentional error, `node --check scripts/artReviewRoute.mjs`, `node --check scripts/artReviewCompare.mjs`, `npm run lint`, `npm run qa`, and `git diff --check` pass. Follow-up: the five-pass polish above resolved the headed Chrome stall by using real-time route stepping plus bounded debug-bridge calls, and the current `npm run art:review` + `npm run art:compare` pass is green.
- Latest water movement pass: tuned `src/render/world/waterSystem.ts` and `src/render/world/waterProfiles.ts` so near water reads less like static shimmer and more like flowing water. The shared water shader now has downstream-traveling sheet waves, moving current/glass threads, bend eddy cues, soft bank laps, and longer-lived Mossu/Karu ripple rings while preserving the matching surface/underfill displacement path. River, pool, creek, alpine runoff, and waterfall profiles now move a little more distinctly without adding meshes or postprocessing. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Verification: `git diff --check`, `npm run qa`, `npm run lint`, escalated `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, and escalated headed `npm run perf:guard` pass; perf guard reports `130fps` average / `12.6ms` p95 / `14.7ms` p99 with `12/12` checkpoints and refreshed water-route screenshots under `output/perf-guard/route/`. `npm run art:review` captured the first three route shots but stalled later in headed Chrome, so it was stopped and not counted as a complete pass.
- Latest desktop Frutiger Aero HUD skin: added final `src/styles/theme/frutiger-aero-hud.css` after the pixel layer so desktop gameplay HUD surfaces now use Windows 7 / Frutiger Aero glass: translucent aqua panels, rounded glossy borders, bubble highlights, blue depth, gold actionable prompt state, and active `backdrop-filter` blur/saturation. Preserved the desktop HUD cleanup from the pixel pass: learned-controls quick actions stay hidden after movement and build meta stays hidden from gameplay framing. Verification: bundled-Node `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract bundle/runner, production `vite build`, `eslint`, `git diff --check`, headed desktop capture at `output/frutiger-aero-hud/desktop-gameplay.png`, desktop metrics at `output/frutiger-aero-hud/results.json` (`hud--controls-learned`, `hintVisible: 0`, `buildMetaVisible: 0`, prompt/status/flavor blur active), and the required develop-web-game client pass under `output/frutiger-aero-hud-client/`. Non-escalated headless Chromium still hits the known macOS Mach-port sandbox block.
- Latest opening-minute polish (pass 1, copy): softened the opening sequence overlay copy in `src/render/app/appTitleScreen.ts` from utility/console phrasing ("Habitat boot", "Burrow Hollow comes online", "trail glass, route grass, and a first Karu signal near the lake path", "Move to take over") to the cozy/notebook tone the title screen now uses ("Burrow Hollow", "A warm morning on the moss", "the meadow is awake — water down the hill, a Karu somewhere along the lake path", "Move to begin wandering"). Beats (`meadow`, `water`, `Karu`) and structure preserved so the existing `opening-sequence` CSS layout still fits. Verification: `npm run qa` (11/11 contracts + clean production build), `npm run lint`, `npm run test:e2e:smoke` (7/7), and `git diff --check` all pass. No styling or runtime behavior changed; OPENING_SEQUENCE_SECONDS = 5.4 and OPENING_SEQUENCE_SKIP_AFTER_SECONDS = 1.05 are unchanged. Next opening-minute risk: the camera handoff at progress = 0.72 still snaps to gameplay if the player skips before that point — worth a real-browser look before retuning.
- Latest wiki/repo-doc sync pass: refreshed `wiki/entities/mossu.md` and `wiki/queries/mossu-development-brief.md` against `docs/CURRENT_STATE.md` (2026-05-06) and `docs/NEXT_PASSES.md` (2026-05-05). Recorded the layered UI direction (retro pixel loading + title shell, Aero creature-interface menus, pixel-cozy gameplay HUD, mobile HUD diet, macOS Aqua Karu viewer), the default Nordic filmic preset, and the `npm run art:review` / `npm run art:compare` / `npm run karu:route` / `npm run agent:review` workflow commands. Reordered the wiki "Useful Next Passes" / "Smartest Next Development Moves" sections to match the repo NEXT_PASSES priority (opening minute polish first). Added a `[2026-05-07]` entry to `wiki/log.md`. No code changes; no QA needed for wiki-only edits.

Current snapshot (2026-05-06):

- Latest desktop pixel-cozy gameplay HUD skin: added a final `pixel-cozy-hud.css` theme layer so normal desktop gameplay chips carry the loading/title material language: silver segmented surfaces, blue pixel outlines, gold actionable edges, tiny sprout/leaf icon motifs, keycap styling, and no HUD `backdrop-filter` glass. A desktop-focused follow-up now hides the learned-controls quick-actions panel after movement, skins the flavor ping as the same silver/blue pixel chip, and hides the dev build meta from normal gameplay framing. Verification: bundled-Node `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract bundle/runner, production `vite build`, `eslint`, `git diff --check`, headed desktop Chrome capture under `output/pixel-cozy-hud/desktop-gameplay.png`, and the required develop-web-game client pass under `output/pixel-cozy-hud-client/`.
- Latest mobile HUD diet pass: mobile gameplay now tracks the first real horizontal movement in `HudShell` and switches from the intro control hint to a learned-controls state after the player starts moving. A late `mobile-hud-diet.css` theme layer collapses the mobile top strip to one `Now` objective chip, hides the learned quick-actions hint, removes hidden pouch/roll/stamina/ability nodes from mobile layout flow, and hides the build metadata on mobile to prevent bottom overlap. Verification: bundled-Node `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract bundle/runner, production `vite build`, `eslint`, `git diff --check`, headed Chrome mobile captures under `output/mobile-hud-diet/` with no console/page errors, and the required develop-web-game client pass under `output/mobile-hud-diet-client/`. The final after-movement capture reports `hud--controls-learned`, one visible status metric, zero visible hint/pouch/roll/stamina/ability surfaces, and HUD coverage down to `0.133`.
- Latest pixel title-menu pass: extended the retro ocean loading-screen aesthetic into the main title menu with a sky/ocean split, floating sky islands, tiny pixel clouds, warm gold/blue Mossu logo with sprout, silver/gold pixel buttons, and matching title settings panel chrome. Removed the older "habitat console" subtitle copy so the menu reads simply as `Burrow Hollow`, and added a late-cascade `pixel-title-menu.css` theme layer after the pixel loader. Verification: bundled-Node QA equivalent (`tsc -p tsconfig.contracts.json`, contract bundle/runner, `tsc --noEmit`, production `vite build`), `eslint`, `git diff --check`, escalated `test:e2e:smoke` (7/7), headed Chrome desktop/mobile/settings title screenshots, and the required develop-web-game client pass. Fresh artifacts: `output/title-pixel-menu-review/desktop.png`, `mobile.png`, `settings.png`, `results.json`, and `settings-results.json`, plus `output/title-pixel-menu-client/shot-0.png` and `state-0.json`. Next risk: final art judgement should stay in a normal desktop browser because the menu is intentionally CSS-art heavy and browser screenshot startup remains sensitive to local Chromium sandboxing.
- Latest pixel-loading placement fix: locked the retro Mossu loading bar to a loader-specific bottom anchor so the bundled theme's old `.instant-title > div` minimum height no longer pulls the bar up over the logo, and nudged the two small floating islands fully above the ocean horizon. Scope: `index.html` first-paint CSS plus `src/styles/theme/pixel-loading.css` bundled override. Verification: bundled-Node QA equivalent (`tsc -p tsconfig.contracts.json`, contract runner, `tsc --noEmit`, production `vite build`), `eslint`, `git diff --check`, escalated `test:e2e:smoke` (7/7), headed Chrome desktop/mobile loading screenshots, and the required develop-web-game client pass. Fresh artifacts: `output/loading-pixel-review/desktop.png`, `mobile.png`, and `results.json` with desktop bar top at `719px` vs. logo bottom `515.6px`, mobile bar top at `700px` vs. logo bottom `457.2px`, plus `output/loading-pixel-client/shot-0.png` and `state-0.json`.
- Latest high-fidelity Karu Viewer pass: rebuilt the isolated `?modelViewer=karu` screen against the attached Aqua mockup with an app-window shell, macOS traffic lights, Geist/Geist Mono typography, frosted glass panels, tactile warm selected controls, real shortcut cards, accessible edit/play/turntable controls, a CSS meadow viewport layer, and reusable render helpers for the app shell, hero, sidebar/control sections/buttons, live rig panel, viewport, timeline scrubber, info panel, shortcut cards, and Karu character illustration. The live Karu rig camera/scale is backed out so the chubby dino-like companion stays fully in frame with breathing room. Verification: `npm run format:check:all`, `npm run lint`, `npm run qa`, escalated `npm run test:e2e:smoke`, targeted Playwright desktop/tablet screenshots and font/state metrics, and `git diff --check` pass. Fresh artifacts: `output/model-viewer-high-fidelity/desktop.png`, `tablet.png`, and `metrics.json`.
- Latest Swarms-ready agent review pass: added `scripts/mossuAgentReview.mjs` plus `npm run agent:review` as a dependency-free workflow runner that reads the latest art-review, perf-guard, Karu route, repo-doc, and local wiki evidence. The runner writes `output/agent-review/review.md`, `context.json`, and `swarms-prompt-pack.json` with five lanes: Art Review Council, Performance Triage Team, Karu Companion Polish Review, Docs And Wiki Sync Assistant, and Next-Pass Planner. Swarms remains an optional external workflow layer only, not a Mossu runtime dependency. Docs updated: `docs/AGENT_REVIEW.md`, `docs/README.md`, `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, and `docs/DEV_SCAFFOLDING.md`.
- Latest model-viewer reference pass: reworked `?modelViewer=karu` toward the provided Karu Viewer reference with a soft storybook workshop shell, illustrated sky/header crest, creature thumbnails, pose/lighting icon buttons, warmer note/key cards, a CSS mascot, and a cleaned-up diorama stage. `ModelViewerApp.ts` now adds real drag-to-orbit pointer handling, a drag hint overlay, richer lightweight stage props, and a slightly wider Karu inspection camera so the companion reads fully instead of as a cropped close-up. `src/styles/theme/aero-creature-ui.css` removes the inherited console corner/scanline overlay from the model viewer, handles desktop and mobile stacking without overlap, and keeps large title text within its containers. Verification: `npm run format:check:all`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, required `develop-web-game` client capture, targeted Playwright desktop/mobile screenshots, drag probe, and console-error probe passed. Fresh artifacts: `output/model-viewer-reference-pass/full-desktop.png`, `full-mobile.png`, `state-desktop.json`, `state-mobile.json`, `errors.json`, plus `output/model-viewer-reference-pass/client/`.
- Repo state before this implementation wave: `master` at `0ecc21b`, matching `origin/master`, with a clean worktree. The working tree now includes the sun/progression/docs pass, the anime/painterly visual pass, the sky/water anime follow-up pass, the crispness pass, and the focused Ghibli/anime detail plus tech-cleanup pass.
- Current shippable status: healthy after the latest presentation/performance/sun/Titan cleanup pass, the follow-up save/progression reset pass, the anime/painterly visual pass, the sky/water anime follow-up pass, the crispness pass, the focused route-readability/detail pass, the lighting-mood pass, the quality/settings pass, the hillside nest path pass, the stronger atmosphere/horizon pass, and the two-part tech-health cleanup pass. Current verification evidence: `npm run lint`, `npm run format:check:all`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, escalated headed `npm run art:review`, `npm run art:compare`, and `git diff --check` all passed. Earlier baseline evidence also included `npm run format:check`, headed `npm run perf:guard:headless`, `npm audit --audit-level=high`, and `node --check scripts/perfGuard.mjs`.
- Latest tech-health cleanup pass: split authored world helper code out of `WorldRenderer.ts` into focused set-piece, forageable, map-marker, and co-op visual modules; moved water profile/tuning data into `waterProfiles.ts`; split the monolithic stylesheet into ordered `src/styles/` chunks behind the `src/styles.css` import entrypoint; and moved runtime automation/debug globals into `runtimeBridge.ts`. Normal player URLs no longer expose `advanceTime`, `render_game_to_text`, or `__MOSSU_E2E__`; browser/debug/perf flows opt in with explicit URL params. `scripts/artReviewRoute.mjs` now uses `?e2e=1&qaDebug=1&visualProbe=1`, resolving the prior full-runtime readiness timeout for headed art-review captures. Verification: `npm run lint`, `npm run format:check:all`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, escalated headed `npm run art:review`, and `git diff --check` pass. Fresh artifacts: `output/art-review-route/summary.json` reports six named route captures and zero fatal browser errors.
- Latest intentional codebase cleanup pass: split `terrainDecorations.ts` further by moving shared decoration math into `terrainDecorationMath.ts` and reusable small prop/instanced prop primitives into `terrainSmallProps.ts`; added named debug save presets with `window.mossuDebug.listSavePresets()` and `window.mossuDebug.applySavePreset(id)`; moved field-guide card, pause/map, progress-row, and status DOM builders into `hudSurfaceBuilders.ts`; added `scripts/artReviewCompare.mjs` plus `npm run art:compare`; and replaced the large late `theme-overrides.css` body with ordered imports to named files under `src/styles/theme/`. Verification: `npm run lint`, `npm run format:check:all`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, escalated headed `npm run art:review`, `npm run art:compare`, and `git diff --check` pass. Current route perf summary: average `161.7fps`, p95 `9.1ms`, and `12/12` route checkpoints passing; `output/art-review-route/summary.json` reports six captures and zero fatal browser errors. Non-escalated `npm run test:e2e:visual` and `npm run art:review` both hit the known `listen EPERM` local-server sandbox failure before passing with approval.
- Latest Aero creature UI redesign: added `src/styles/theme/aero-creature-ui.css` as the final late-cascade player UI theme and imported it last from `theme-overrides.css`. The player-facing loading/title, HUD, prompts, pickup cards, pause/settings, field guide, route map, and error overlay now use Windows 7-style blue glass chrome, crisp handheld-RPG grouping, compact keycap/action states, and small Spore-like organism accents while preserving Mossu language and controls. Boot/title copy now says habitat glass / trail signals instead of the older gel/gene wording. Docs updated: `docs/CURRENT_STATE.md` and `docs/GAME_MEMORY.md`. Verification: `npm run format:check:all`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, escalated headed `npm run art:review`, focused escalated UI capture probe, and `git diff --check` pass. Non-escalated visual e2e, art review, and UI probe each hit the known `listen EPERM` local-server sandbox block before passing with approval. Fresh artifacts: `output/art-review-route/summary.json` reports six captures and zero fatal browser errors, and `output/aero-creature-ui/` contains title/HUD/pause/field-guide/map plus 375px screenshots with `ui-bounds.json` reporting zero overflow.
- Latest Nordic filmic route pass: added a `nordic` visual quality preset and made it the default normal-play look. It lifts ACES exposure, lowers bloom intensity, raises bloom threshold for cleaner highlights, feeds a Nordic-film strength into the world lighting mood, shifts fog/sky/horizon/cloud tints toward cool pearl tones, restrains water sparkle, and reduces the far ocean Gerstner plane from `200` to `176` subdivisions while calming wave/spec/foam values. Docs updated: `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, and `docs/README.md`. Verification: `npm run format:check:all`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (first run hit the known preview-server drop, rerun passed), escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, escalated headed `npm run art:review`, `npm run art:compare`, escalated `npm run karu:route`, and `git diff --check` pass. Latest route guard reports `196.7fps` average, `6.3ms` p95, `475280` triangles, pixel ratio `0.98`, and `12/12` checkpoints.
- Latest Karu model-viewer pass: `?modelViewer=karu` now opens the isolated viewer directly on the Karu companion rig, the title screen has a `Karu Viewer` action, the model-viewer shell gets Karu-specific launch copy, and the Karu workshop camera frames the rig closer and higher for inspection. `src/styles/theme/aero-creature-ui.css` now includes a matching Aero creature-interface layer for the model viewer, and `docs/DEVELOPMENT.md` documents the Karu viewer URL. Verification now includes the green `npm run format:check:all` from the Nordic pass, plus the earlier viewer smoke and headed capture evidence under `output/karu-model-viewer/`.
- Latest visible direction: Mossu is a polished cozy Three.js route slice with Aero creature-interface UI, default Nordic filmic lighting, staged world startup, perf-guard tooling, a 3D orbiting sun that drives the scene lighting envelope, stronger horizon atmosphere, layered volumetric-style god-ray bands in the sky shader, cool pearl fog, controlled warm sun haze, soft cloud layers, subtle moving cloud-shadow patches, soft Mossu/Karu outlines, painterly terrain/grass/water banding, and the Mossback Titan removed from active play. Recent focused polish sharpened tree silhouettes, strengthened warm path/clearing reads, added hand-painted grass/brush clumps near route edges, softened fog farther out, reduced glow in camera-critical areas, and kept the cute anime look less blurry through lighter final-image pixel blocking, bloom, paper softening, sky haze, cloud opacity, and milky water edges.
- Latest small route-readability pass: widened route clearing masks around Fir Gate, Whisper Pass, Highland Basin, and Ridge Saddle; added subtle signpost/rock/brush cues near decision points; reduced decision-window haze/glow response; and softened screen-space god rays so they stay light instead of washing over gameplay views.
- Latest grass/personality pass: meadow tufts, route-edge grass, and tall reeds now use slightly different wind rhythms, nearby route-edge grass can lean away from Mossu, and Mossu leaves lightweight pressed path traces behind without adding rain/weather effects.
- Latest camera/movement/animation pass: rolling now gets an immediate readable launch impulse, stronger roll acceleration, and better assisted turning; the follow camera adds subtle speed FOV, shoulder response, and roll recentering; Mossu's body, face, eyes, tufts, and legs now lean/react to turn and acceleration. Tree trunks that opt into camera collision now softly fade only when they sit between the camera and Mossu, reducing foreground occlusion without making the whole forest transparent.
- Latest roll/jump/slope feel pass: roll steering now preserves more creature-like momentum through drift and reverse turns, jumps have wider coyote/buffer windows, roll jumps keep short air momentum carry after release, landings start a brief forward-momentum grace, and roll input now reads hill direction so downhill feels flowier while uphill feels heavier without locking controls.
- Latest sun redesign correction: the visible sun is no longer a camera-facing disc or painted sky-core effect. It is now a small world-space 3D sphere with subtle spherical corona shells, positioned at the actual raised sun rig far above the island, while the sky shader only keeps warm haze/light-shaft atmosphere around that direction.
- Latest lighting-mood pass: added a shared authored sun/atmosphere mood envelope inspired by large-world directional-light staging. Route phase, elevation, shrine proximity, ridge/basin context, and decision clarity now drive sunlight warmth, watercolor fog, horizon tint, sky haze, cloud opacity, lightweight moving cloud-shadow patches, grass/tree dimming, water sparkle, and landmark glow from one state instead of isolated ad hoc shader tweaks.
- Latest atmosphere/horizon pass: the sky dome now receives time, warm-haze, watercolor-fog, landmark-glow, and silhouette mood uniforms so the horizon breathes with the route lighting state. The shader adds a stronger cream-to-cool horizon shelf plus broken fan-shaped god-ray bands that drift very slowly from the real sun direction, giving the sky a more volumetric read without adding a fullscreen postprocess or extra mesh budget. Verification: `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, `npm run test:e2e:visual`, headed `node scripts/perfGuard.mjs --headed --browser=chrome`, and `git diff --check` pass.
- Latest water/shoreline close-up pass: the shared water shader now gives near-camera banks stronger hand-painted foam strokes, slightly milkier shore edges, and clearer shallow/mid/deep painted depth contours while reusing the existing foam texture and draw path. Water profile tuning gently boosts shoreline foam/milk, clarity, and ripple contrast without adding new meshes or postprocessing.
- Latest DeepSec pass: initialized `.deepsec/` for Mossu, wrote a project-specific `INFO.md`, added `data/mossu/config.json` ignores for local env/build/generated folders, and ran the safe regex-only DeepSec scan. The clean scan tracks 29 source/tooling candidates, mostly authored `innerHTML`, rendering randomness, local test-server config, and one GitHub Actions supply-chain check. The AI `process` phase was not run because it would export private source to an external model backend without explicit approval.
- Latest documentation pass: added `docs/CURRENT_STATE.md` as the short current project snapshot and `docs/NEXT_PASSES.md` as the prioritized work queue, refreshed `docs/README.md` and `docs/GAME_MEMORY.md`, and added AGENTS documentation-discipline rules so future agents know when to update repo docs versus wiki pages.
- Latest quality/settings pass: the title and pause settings panels now expose functional Soft / Anime / Crisp visual presets plus pixel-ratio cap, bloom toggle/intensity, fog-strength, and camera-distance controls. Settings persist for normal play, stay defaulted in QA/perf modes, feed `render_game_to_text`, and drive the adaptive render cap, bloom pass, retro texture crispness, world fog density, and FollowCamera distance bias.
- Latest hillside nest spawn pass: Mossu now starts inside the authored Burrow Hollow nest on the side of the opening hill at `(-88, -132)`. The Burrow Hollow landmark/map marker, local grass clearing, and opening nest vista are anchored to the same spot, with a stronger uphill leaf bank plus pebble/flower lead-out toward the route. Movement contracts now use a neutral fixture position so future spawn composition changes do not recalibrate unrelated physics tests. Verification: `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, headed Chrome spawn capture, and `git diff --check` pass.
- Latest nest exit path pass: added a dedicated pressed-grass connector from the hillside nest into the first route beat, feeding route dirt, painted-ground, readability-clearing, terrain-smoothing, and grass-density masks. The opening nest batch now adds subtle flattened grass/dirt patches along the exit so Mossu has a clearer natural roll-out line without adding heavy runtime systems. Route contracts now assert the nest exit remains visible, and the perf route fixture now starts at the actual nest so `start-burrow` is stamped during headed route checks. Verification: `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, and `git diff --check` pass. Custom screenshot capture still hit the known headless readiness/browser-capture instability.
- Latest movement/physics depth pass: ground accel/decel/turn-accel and roll coast deceleration are now multiplied by a per-surface traction table read from `sampleWorldRegion(x, z).material`. Rock and forest_floor grip harder; sand, highland_grass, and shrine_moss let Mossu glide further. Variable jump height landed alongside: releasing Space mid-ascent damps remaining upward velocity by `JUMP_RELEASE_CUT_MULTIPLIER` so taps become short hops, with a one-shot latch per jump so re-pressing in air does not re-cut. Verified with `npm run qa` (movement contract gained traction-table assertions and held-vs-tap arc integration), `npm run lint`, and `npm run test:e2e:smoke` (5/5).
- Latest hold-to-charge jump pass: holding Space during ascent now applies an upward thrust (`JUMP_HOLD_THRUST=40` m/s²) for up to `JUMP_HOLD_MAX_DURATION=0.35s` so vy actually grows during the charge window — Mossu climbs visibly higher the longer Space is held, no standing-still required. The same window now gates the variable-jump-cut: releasing inside the window cuts vy (tap = short hop), releasing after the window has expired leaves the charged arc alone (long hold = full charged jump, no penalty). Floating supersedes thrust so Breeze Float ability behavior is unchanged. Verified with `npm run qa` (movement contract now also asserts `peakVy > JUMP_VELOCITY` proving thrust grows velocity, and `peakHeight > 12m` proving the arc clears a no-thrust ballistic peak).
- Latest biome threshold landmarks pass: added `biomeThresholdLandmarks` data table (`src/simulation/world.ts`) — 5 hero props placed at zone boundaries (plains↔hills, hills↔foothills, foothills↔alpine, alpine↔ridge, ridge↔peak_shrine). Each landmark carries position + clearing radius/strength so the same data drives both visual placement and `sampleBiomeThresholdClearing` (a new clearing widening contribution merged into `sampleRouteReadabilityClearing`). New prop builders in `terrainDecorations.ts`: `makeMossCairn`, `makeStoneCairn`, `makePrayerCairn`, `makeHeroPine`, `makeWindPine`. `buildBiomeThresholdLandmarks()` is wired into WorldRenderer alongside biomeTransitionAccents and participates in collider/wind/small-prop collection. Position-tuning informed by an empirical biome-zone probe — initial picks landed in wrong zones; final positions verified to sit in declared from/toZone. Contract additions in `routeContracts.ts`: each landmark must sit inside playable bounds, sample as fromZone or toZone, sit in a clearing wide enough to read (>0.55), and the threshold-clearing function must peak at the landmark and decay at distance. Verified with `npm run qa` (11/11 contracts), build passes (WorldRenderer chunk +2.7k gzip).
- Latest save/progression status: local save state now has an explicit fresh-start reset path through the pause menu and `window.mossuDebug.resetProgress()`. The pause menu summarizes trail progress and save persistence, and smoke coverage now confirms a reset clears collected route state back to the Burrow Hollow baseline.
- Latest small progression pass: route discovery stamps now give more rewarding flavor pings, Moss Crown Shrine arrival claims a simple Summit Circuit reward, the character screen tracks a 3-sample forage goal, and recruited Karu IDs persist through local save while reset/debug replay clear them cleanly.
- Latest debug-jump status: `?qaDebug=1` now exposes `mossuDebug.jumpTo("fir-gate" | "highland-basin" | "ridge-saddle" | "shrine")` for instant route inspection. The highland, ridge, and shrine jumps land on authored readable bank/clearing positions instead of requiring a full replay.
- Latest crispness/art polish status: a headed browser pass showed the adaptive renderer could collapse to unreadably low DPR, so the normal desktop floor now holds at 0.62, 60Hz rendering no longer downshifts unnecessarily, and Highland Basin/Ridge Saddle/Moss Crown get targeted fog relief while keeping the watercolor distance atmosphere.
- Latest composer-grade lock pass: normal play now prepares the `EffectComposer` before runtime start and keeps the composer path through title, opening, map, pause, character, and transition states. Bloom remains the conservative/high-DPR piece while the anime color grade and retro texture filter stay on whenever composer is available. Verification: `npm run test:contracts`, `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, `npm run perf:guard`, `git diff --check`, and a live dev-server render-path probe confirmed `renderPath: "composer"` plus `retroTextureEnabled: true` through title, gameplay, map, pause, and return-from-pause; the headed route guard passed 12/12 checkpoints at 169.7fps average / 7.6ms p95.
- Latest behavior-preserving bug/perf hygiene pass: fixed the actual BloomPass enable gate so it matches the existing conservative DPR policy and no longer renders below the HUD/QA-reported threshold, restored accumulated `renderer.info` stats under the composer path so perf HUD/guard calls and triangle counts include the full frame instead of only the final fullscreen pass, and corrected grass LOD startup stats so unvisited sources do not appear as already scanned. No gameplay mechanics or visual tuning values changed. Verification: `npm run test:contracts`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, and `git diff --check` pass; the final route guard reached 12/12 checkpoints at 194.6fps average / 6.4ms p95 / 6.9ms p99 with real renderer stats reporting 1340 calls and 490784 triangles.
- Latest Karu/habitat life pass: Karu now have extra idle micro-routines (`groom`, `listen`, and `watch_water`) that can also show on slow recruited companions, Karu notice/join beats gained soft procedural chirps, and meadow/water/shrine pockets gained one bounded 96-point living-habitat signal layer that flutters lightly and reacts to Mossu/Karu moments. This keeps the world feeling more alive without adding a heavier critter sim or changing mechanics. Verification: `npm run test:contracts`, `npm run lint`, `npm run build`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, `npm run qa`, and `git diff --check` pass; the headed route guard reached 12/12 checkpoints at 152.4fps average / 8.4ms p95 / 9.1ms p99. `npm run art:review` still needs a follow-up because the sandbox run hit `listen EPERM` and the escalated run timed out waiting for the runtime-ready hook.
- Latest distant-birds pass: added a fixed 12-instance far-field bird silhouette layer to the grassland immersion system. The birds sit high above distant route/horizon pockets, drift in slow staggered arcs, and use one tiny shared chevron geometry/material so the sky feels more alive without adding gameplay fauna, collision, save state, or AI. Perf HUD now reports the flock budget. Verification: `npm run test:contracts`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, and `git diff --check` pass; the route guard reached 12/12 checkpoints at 167.4fps average / 7.4ms p95 / 8.1ms p99 with 1343 calls and 490832 triangles.
- Latest Mossu/Karu animation feel pass: Mossu's visual rig now adds brake and roll-exit pulses on top of the existing roll/jump/land timing, with softer face/eye lag, cheek compression, tuft follow-through, and leg recovery so transitions feel less abrupt without changing movement physics. Recruited Karu now use staggered follow-hop phases, mood-weighted hop strength, stop-attention poses, and slightly richer tail/face/foot response so companions no longer move in lockstep. Added a visual contract that steps Mossu through roll, brake, and landing pulses and asserts finite transforms. Verification: `npm run test:contracts`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, headed `npm run perf:guard`, and `git diff --check` pass; the route guard reached 12/12 checkpoints at 180.7fps average / 6.7ms p95 / 7.3ms p99 with 1343 calls and 490832 triangles. The required develop-web-game client produced title screenshots/state under `output/animation-feel-client/`; a custom full-gameplay Playwright probe still hit the known full-runtime readiness timeout, so final art judgement should use the maintained route screenshots or a normal desktop browser.
- Latest real-browser evidence: headed `npm run perf:guard` passed the full Burrow Hollow -> Moss Crown Shrine route at 150.2fps average / 9.7ms p95 / 13.4ms p99 with 12/12 checkpoints and refreshed route screenshots under `output/perf-guard/route/`. A normal headed Chrome pass at `http://127.0.0.1:8000/?qaDebug=1` captured the final sky/water anime pass at `output/web-game/sky-water-anime-pass-gameplay.png` with zero console warnings/errors.
- Latest progression/debug verification: `npm run qa`, `npm run test:e2e:smoke`, and headed Chromium visualProbe captures passed after the progression/debug-jump/art polish pass. Route screenshots are in `output/progression-visual-check/highland-basin.png`, `ridge-saddle.png`, and `moss-crown-shrine.png`; the headed pass reported no page or console errors.
- Latest checkpoint baseline: `npm run qa` passed on May 2, 2026, then a headed Chrome `?qaDebug=1` browser sweep used `mossuDebug.jumpTo()` for Highland Basin, Ridge Saddle, and Moss Crown Shrine. The browser sweep landed all three jumps in `third_person`, matched the expected landmarks, reported no warnings/errors beyond normal Vite debug/info logs, and wrote checkpoint screenshots under `output/checkpoint-browser/`.
- Latest Titan status: inactive by design. The preserved implementation lives under `src/simulation/unused/giantMossCreature.ts` and `src/render/objects/unused/MossbackTitanAvatar.ts`; active `src`, `tests`, and built runtime checks should not reference `model=titan` or active Titan state.
- Latest tech-cleanup status: `npm prune` removed the extraneous Next/React/Sharp-related local packages and reduced `node_modules` from roughly 463M to 140M. Playwright/perf browser temp output now defaults to `.codex-tmp/playwright-tmp`, Playwright e2e scripts now unset inherited `NO_COLOR` while forcing plain output so Node color warnings no longer hide browser failures, and the known Three vendor chunk is isolated with a project-appropriate Vite warning threshold so `npm run qa` no longer emits the large-chunk warning.
- Latest perf/QA note: the green headless perf guard reached all 12 checkpoints at roughly 193fps average / 7.2ms p95 in the save/progression follow-up pass, the anime/painterly headed guard reached 179.8fps average / 7.6ms p95, the sky/water headed guard reached 163.9fps average / 7.8ms p95, and the focused detail headed guard reached 150.2fps average / 9.7ms p95. Real-browser review is still preferred for final visual judgement because deterministic/headless WebGL screenshots can be softer or flaky.
- Latest route-readability verification: `npm run qa`, `npm run lint`, `npm run format:check`, escalated `npm run test:e2e:visual`, and headed `npm run perf:guard` pass. Final headed route guard reached 12/12 checkpoints at 156fps average / 8.8ms p95 / 11.1ms p99 and refreshed route screenshots in `output/perf-guard/route/`.
- Latest camera/movement verification: `npm run qa`, `npm run test:e2e:smoke`, required `develop-web-game` client smoke, headed Chrome roll/camera probes, headed `npm run perf:guard`, and `git diff --check` pass. The lowland roll probe captured `output/camera-movement-pass/lowland-roll-camera.png` with `camera.profile=roll`, `fov=56.8`, no console errors, and a 5.3ms sampled frame; the final headed route guard reached 12/12 checkpoints at 142.9fps average / 12.3ms p95 / 19.6ms p99.
- Latest sun verification: `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, `git diff --check`, and headed `npm run perf:guard` pass after the world-space sun correction. The real-browser Chrome capture at `output/sun-worldspace-pass/lowland-far-world-sun.png` shows the raised spherical sun in-scene with zero console/page errors; the route guard passed 12/12 checkpoints at 148.5fps average / 8.7ms p95 / 9.8ms p99.
- Latest roll/jump/slope verification: `npm run qa`, `npm run lint`, `npm run test:e2e:smoke`, headed Chrome movement probe, headed `npm run perf:guard`, and `git diff --check` pass. The movement probe wrote `output/movement-feel-pass/movement-feel-results.json` and route screenshots; the final Shrine samples were forward roll 26.89, drift turn 22.31, reverse input 20.75, and released roll-jump landing 13.82 with no browser console errors. One perf guard run had a noisy Skyward Ledge p95 miss at 21.3ms, then the rerun passed 12/12 checkpoints at 157.4fps average / 8.2ms p95.
- Latest lighting-mood verification: `npm run qa`, `npm run test:e2e:smoke`, headed browser jump captures, headed `npm run perf:guard`, and `git diff --check` pass. The jump capture landed Highland Basin, Ridge Saddle, and Moss Crown Shrine with zero console/page errors and wrote `output/lighting-mood-pass/highland-basin.png`, `ridge-saddle.png`, `shrine.png`, and `lighting-mood-results.json`; the route guard passed 12/12 checkpoints at 202.8fps average / 5.9ms p95 / 6.2ms p99.
- Latest quality/settings verification: `npm run qa`, `npm run test:e2e:smoke`, `npm run lint`, `npm run format:check`, headed settings browser probe, required web-game client smoke, headed `npm run perf:guard`, and `git diff --check` pass. Settings screenshots/results are under `output/quality-settings-pass/`; the browser probe confirmed the Crisp preset, keyboard safety inside title settings, and pause camera-distance updates. The final headed route guard passed 12/12 checkpoints at 212.5fps average / 5.9ms p95 / 6.5ms p99.
- Active watchlist: true WebGPU remains diagnostic because Three `ShaderMaterial` paths are incompatible there, local Playwright preview binding may still need sandbox approval, and final art judgement should still happen in a normal desktop browser after major lighting/fog changes.
- Documentation pass completed here: added a docs index, a known-issues/watchlist doc, and an asset-parking doc so future agents do not have to reverse-engineer current state from this long chronological log.

Historical log:

- Big-world perf baseline pass: tightened `?renderer=webgpu` fallback so Three's internal `webgpu-webgl2` backend is rejected in favor of Mossu's normal WebGLRenderer path with an explicit fallback reason, moved the Silver Bend visual-perf anchor out of the underwater overlay and onto the river bank, and captured the current route/stress baseline in `docs/PERF_BASELINE.md`. Verification: `npm run lint`, `npm run format:check`, `npm run qa`, `npm run test:e2e:smoke`, `npm run test:e2e:visual`, `npm run perf:guard:baseline`, `npm run perf:guard:coop`, and `git diff --check` pass. Fresh artifacts: `output/perf-guard/baseline.json` (114.9fps avg / 12.2ms p95 / 12 of 12 checkpoints), `output/perf-guard/coop-stress.json` (136.9fps avg / 10.0ms p95 / `coopStress` on), `output/perf-guard/renderer-fallback-forced.json` (requested WebGPU -> active WebGL fallback, zero console errors), and `output/perf-guard/big-world-baseline-summary.json`. Active real WebGPU remains diagnostic-only: `output/perf-guard/webgpu.json` reaches 12 of 12 checkpoints but fails because Chrome logs six `NodeMaterial: Material "ShaderMaterial" is not compatible.` errors.

- Retro loading atlas pass: replaced the instant loading shell's visible HTML/CSS with a framed retro habitat-map screen: parchment/bronze frame, map-art vignette, route pins, sim-style status ledger, and organic Spore-like progress fill. `src/main.ts` loading copy now uses atlas/habitat/workshop phrases, and `src/styles.css` includes a small end-of-bundle override so the app UI CSS does not pull the loader back to the older aqua/spore skin after the build stylesheet loads. Verification: `npm run qa` passed, `git diff --check` passed, and frozen built-loader screenshots passed at `output/loading-retro/desktop.png` and `output/loading-retro/mobile.png`. `npm run test:e2e:smoke` remains unstable in this workspace: repeated runs passed different subsets, and the serial run passed game shell plus binder/map before the model-viewer case hit `ERR_CONNECTION_REFUSED` after the preview server dropped.

- Tech-stack tooling pass: upgraded Vite to `7.3.2`, added ESLint flat config with `@eslint/js` + `typescript-eslint`, added Prettier config/ignore and scripts (`lint`, `format`, `format:check`, `format:check:all`), and added `test:e2e:visual` as a deterministic fixed-camera visual/perf guard. The visual guard uses `?visualProbe=1` so `advanceTime()` renders stable canvas frames in e2e mode, samples the preserved WebGL drawing buffer for nonblank/contrast/chroma/hash metrics, and checks deterministic frame cost without relying on screenshot files. `perfGuard` now treats screenshot capture as optional unless `visual.requireScreenshot` is explicitly true, while still requiring canvas visual snapshots. Verification: `npm run lint` passed with existing warnings only, `npm run format:check` passed, `npm run qa` passed on Vite `7.3.2`, `npm run test:e2e:visual` passed with localhost escalation, `npm run test:e2e:smoke` passed, and `git diff --check` passed. Required web-game client was attempted twice against `http://127.0.0.1:4182/?e2e=1&qaDebug=1`; the action loop reached screenshot capture but failed on the repo's known WebGL/Chromium screenshot timeout/headed browser closure, so rely on the green smoke + visualProbe checks for this pass.

- Loading-screen visual pass: replaced the tiny instant shell with an inline CSS island vignette, visible loading status copy, ambient water/Karu/dot motion, and the existing Mossu title treatment while preserving the `[data-loading-status]` bootstrap contract. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and desktop/mobile first-paint screenshots at `output/loading-screen-visual.png` and `output/loading-screen-mobile.png` passed.

- Loading-bar follow-up: replaced the three-dot loader with an organic spore/cell themed progress bar and wired bootstrap loading phases to visible fill progress in `main.ts`. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, the required web-game client pass, and first-paint/progress/mobile screenshots at `output/loading-screen-spore-bar.png`, `output/loading-screen-spore-bar-progress.png`, and `output/loading-screen-spore-bar-mobile.png` passed.

- Spore-themed UI pass: extended the cell/spore loading direction into the title screen, HUD, pause menu, evolution log, sample pouch, map, buttons, and common panels with organic membrane surfaces, green/amber bio highlights, and updated evolution/genome wording. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and live DOM/browser checks passed; screenshots were refreshed at `output/loading-screen-spore-theme.png`, `output/spore-ui-title.png`, `output/spore-ui-pause.png`, `output/spore-ui-log.png`, and `output/spore-ui-map.png`, with the known headless WebGL screenshot timing instability still present on some title captures.

- Clarity + interaction cleanup pass: tightened title-screen hierarchy with visible Play copy, consolidated the topmost organic UI material layer for clearer HUD/menu prompts, made action prompts more explicit for forage/Karu/landmarks/summit feedback, widened forage pickup tolerance, aligned the Moss Crown map marker with the moved shrine, and strengthened warm route/path/form-stroke cues for environment readability. Verification: `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Real-browser title DOM check confirmed logo/subtitle/Play layout no longer overlaps; live WebGL screenshot capture still has the known timeout instability after the scene settles.

- Idle camera orbit pass: added a six-second gameplay input idle timer in `GameApp` and a damped third-person orbit mode in `FollowCamera`. The camera now begins a slow Mossu-centered rotation after no movement/action/camera input, and immediately cancels on movement, camera, click, or wheel control activity. Verification: `git diff --check` and `npm run build` passed. Full `npm run qa` and Playwright/browser probes are currently blocked by system disk pressure (`no space left on device` while writing `.contract-test-build` / Playwright temp artifacts); cleanup of generated artifacts was requested but rejected by the approval system, so do not bypass it without explicit user approval.

- Mountain and hill reference pass: translated the highlandscape spec into broader rounded highland forms, stronger distant peak/ridge massing, a clearer worn path through the upper valley, sparse slope trees, rock groupings, flower flecks, extra highland mist bands, and a warmer hill/rock palette. Verification: direct `npm run qa` equivalent passes through bundled tooling (`tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`) plus `git diff --check`; the `npm run qa` wrapper itself still cannot run in this shell because `npm` is not on PATH.

- Lake and great river reference pass: translated the new waterscape spec into the lower valley with a broader west-side great lake, a controlled lower river braid, widened lower river band, stronger blue lake/river palette, authored shore trees, reeds, flower pockets, and shoreline rock reads. Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`, and `git diff --check` pass. Real-browser Dia read confirmed the lower valley now presents broad blue water, reeds, and rocks, but automated Playwright screenshots remain unreliable in this desktop session due Chromium launch/page-close failures.

- Cinematic smoothing pass landed: camera damping now catches up more slowly with shoulder drift plus landing/roll settle nudges, collection/landing/map-close feedback uses small cinematic kicks, passive HUD refresh is softer/throttled during scenic beats, nearby grass/water react more visibly to Mossu, and the first unrecruited Karu pauses/looks with a gentle hint. Verification: `git diff --check`, `tsc --noEmit`, and production `vite build` pass through the bundled Node/runtime; contract runner is still blocked by pre-existing `src/simulation/world.ts` sampler drift (`lowland grove reads as forest habitat`, `reed-cove swim flag matches pool contract`). Perf guard evidence: first headless candidate completed at 123.2fps avg / 16ms route p95 but failed Burrow p95 by 1.1ms; after HUD refresh throttling, Burrow improved to 91.4fps / 15ms p95, while a second full headless run degraded in highland checkpoints and produced a bad Windstep visual metric, likely headless-session instability rather than a focused lower-meadow regression.

- Peaceful meadow reference pass in progress: translated the provided meadow spec into lower-valley world composition. Added a `silver-braid` creek branch, brighter grass/water palette values, larger opening/amber meadow scenic pockets, a left foreground meadow tree, authored flower drifts, extra creek-bank reads, and a right-side plateau/rock accent band. Verification so far: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, `vite build`, and `git diff --check` pass through the bundled Node/runtime because `npm` is still unavailable on the sandbox PATH. Real-browser visual check remains next before considering this pass complete.
- Perf route guard pass in progress: extending `scripts/perfGuard.mjs` from one saved traversal into a Burrow Hollow -> Moss Crown Shrine route guard with QA route replay, checkpoint screenshots, visual fingerprints, route landmark-stamp checks, and per-checkpoint frame budgets.
- Route guard follow-up: added screenshot-only route camera options so individual checkpoints can frame evidence without changing the replay path, and wired `mossuDebug.faceRouteHeading(heading, cameraOptions)` through `main.ts` / `GameApp` / `FollowCamera`. Verification: `npm run qa` passes, escalated `npm run test:e2e:smoke` passed, and escalated `npm run perf:guard` passed in headed Chrome with 12/12 checkpoints, all required route stamps, 158 fps average, and 10.8ms p95. Later headed/headless reruns flaked before or during Playwright runtime/screenshot work, so keep the latest green `output/perf-guard/latest.json` as the current baseline but do not over-trust screenshot framing in the dense upper-route pockets.
- Perf HUD pass: added `?perfHud=1` as a compact real-browser overlay beside the existing detailed `?perfDebug=1` panel, with `Shift+P` hide/show and `Shift+C` snapshot capture to `window.__MOSSU_PERF_CAPTURE__`/console/clipboard when available. This complements, not replaces, `npm run perf:guard`. Verification: `npm run qa`, escalated `npm run test:e2e:smoke`, and `git diff --check` passed; a custom non-e2e headless HUD probe still hit the known heavy WebGL startup path, so do the final visual read in desktop Chrome at `/?perfHud=1`.
- Latest startup optimization pass: removed the visible "Waking up" loading copy/animation from the instant shell, deferred WebGL bloom/postprocessing initialization until idle, and moved optional world pieces (clouds, mountain atmosphere, valley mist, shadow volumes, highland waterways, forageables, mountain silhouettes, and Karu background life) into staged background slices after the first frame.
- Verification for this pass: `npm run qa` passed, `npm run perf:guard` passed in headed Chrome at 98.6 fps average / 12.9ms p95, and the required web-game client produced `output/startup-optimization/shot-0.png` plus `state-0.json`.
- Follow-up startup note: the first captured opening-frame state still reports an early 100ms frame while background scene work settles, so deeper "zero perceived hitch" work should split grass/tree/decor batch construction into smaller idle jobs or prebuild/cache static geometry data.
- Follow-up pass (same thread): the instant title shell now shows short loading status copy (`[data-loading-status]`) while route chunks import, then fades out via `.instant-title--leaving` instead of a hard `textContent` clear; `GameApp` defers `EffectComposer` + bloom behind `requestIdleCallback` and dynamic `three/examples` imports; `?qaDebug=1` exposes `window.mossuDebug.applySaveState` for position/save replays; `npm run perf:guard` is wired in `package.json` with `scripts/perfGuard.mjs`.
- Latest focus landed: grass spatial-cell LOD, far-grass painterly impostors, no explicit bootstrap loading screen, and gentle idle Karu wander around Mossu.
- Current first-frame policy: `index.html` paints a tiny static Mossu title shell immediately; the real title/game app replaces it when the route chunk starts.
- Current grass policy: near/mid/alpine remain real instanced blades with LOD; far meadow grass is one static patch-impostor mesh and is not part of `windMeshes`.
- Latest verification completed: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`, `git diff --check`, and the required web-game client attempt.
- Browser artifact: `output/far-grass-impostors/shot-0.png` and `state-0.json` were captured; the helper still timed out on title-button click stability, but the captured state shows the opening sequence entered.
- Next recommended real-browser check: open the built or dev app in Chrome, confirm the instant title shell feels acceptable, recruit a few Karu and idle near the opening meadow, inspect distant grass patches from the opening meadow/highland views, and watch `?perfDebug=1` for grass LOD/impostor counts while moving.
- Water underfill fix: the cheap underfill mesh reused the same geometry but not the animated vertex displacement, so gaps could show terrain beside/under the wavy surface; underfill now shares the same wave + ripple uniforms as the main water and applies the depth offset in the vertex shader; underfill opacity slightly raised (0.9 → 0.92 for non-still profiles). Verification: `npm run qa` passed.

- Aqua-glass UI rework landed: replaced the recent spore/cell UI direction with a glossy aqua creature-care layer inspired by the provided Aqua Prime screenshot. Scope stayed UI-only: `HudShell.ts` copy shifted toward habitat / field guide / traits / pouch / care language, `GameApp.ts` title/opening copy now reads as a bright island habitat route, `main.ts` and `index.html` loading copy use habitat/water language, and `src/styles.css` has a final aqua-glass cascade layer for teal translucent panels, circular bubble controls, lime/yellow accents, glossy title/menu surfaces, handbook cards, map panels, and pickup bubble motion. Verified with `npm run qa`, `npm run test:e2e:smoke` (outside the sandbox because local preview binding hit `listen EPERM` inside it), `git diff --check`, and live browser screenshots of title, gameplay HUD, field guide, habitat map, pause menu, and a narrow pause viewport.

- Biome travel feel pass: widened route-readability glades and painted clearings, added reusable `sampleBiomeTransitionOpen()` for softer tier-boundary blending, gated biome-open habitat influence through route/scenic travel bands so off-route groves stay forested, softened terrain zone-color edges in `terrainMesh.ts`, and added a few non-blocking understory anchors to the biome-transition accent layer. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and a targeted no-screenshot Playwright state probe against `http://127.0.0.1:4174/?e2e=1` passed. The required develop-web-game client reached the page but failed on the known `page.screenshot` WebGL timeout; real Safari gameplay screenshot artifact is `output/biome-travel-feel/safari-gameplay.png`.

- Breath-of-the-Wild-like water pass: retuned `src/render/world/waterSystem.ts` toward clearer painterly turquoise water, softer shoreline milk/foam, brighter shallow-bed color, stronger caustic/sparkle reads, and smoother depth banding while preserving the existing shared wave uniforms for the underfill layer. A follow-up opacity clamp keeps authored lakes/pools from reading as overly transparent beige slabs. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and live Chrome visual inspection at `http://127.0.0.1:4176/?qaDebug=1` passed. The opening scenic beat still applies a broad pale screen fade, so judge final water color after moving out of the intro overlay.

- Water audio smoothing pass: lowered ambient water volume, slowed the gain fade, added a subtle flow-based playback-rate curve, and replaced the single native MP3 loop with two audio slots that crossfade before the 5s loop wraps. Verification: `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and a headless `?qaDebug=1&e2e=1` no-screenshot boot probe passed. A full non-e2e audio-state probe still hung while reading the large debug payload, matching recent full WebGL/headless instability; judge the final loudness in a real browser.

- Deeper-water swim-feel pass: made the Windstep/highland waterfall runoff resolve as a true swimmable creek pocket, added a small surface lift when Mossu first enters deep water so entry reads as bobbing into a swim instead of sinking under, and added HUD copy for shallow shelf / deep water / underwater states. Verification: `npm run qa` and `git diff --check` passed; a random-port production browser probe against `dist/` reached gameplay and confirmed spawn water state still reports wading. `npm run test:e2e:smoke` is blocked by an existing Playwright visual run reusing the configured `127.0.0.1:4173` preview server and reporting unrelated `ERR_CONNECTION_REFUSED` resource errors in the first smoke test.

Historical Mossback Titan hostile creature pass (superseded and parked):

- Added a simulation-owned giant moss creature state that patrols authored waypoints, stalks Mossu when nearby, charges at close range, and respawns Mossu back to the burrow when caught.
- Added a large low-poly in-world Mossback Titan rig with stone shell, moss blanket, crystal back growths, clawed legs, predator eyes, walking motion, alert animation, and water ripples.
- Added HUD warning copy plus `render_game_to_text` coverage for Titan mode, position, distance, alertness, and attack count.
- Verification: `npx tsc --noEmit`, `npm run build`, `git diff --check`, and `npm run test:e2e:smoke` pass. Targeted Playwright state probe confirmed teleporting Mossu onto the Titan increments attack count and respawns Mossu at Burrow Hollow. Required develop-web-game client pass completed and wrote `output/mossback-titan/shot-0.png` plus `state-0.json`; the screenshot catches the opening overlay rather than the Titan. Full `npm run qa` is still blocked by the pre-existing habitat contract drift (`lowland grove reads as forest habitat`).
- Titan detail follow-up: pushed the visual from blockout toward the provided spec with a stronger snout/head silhouette, cheek plates, horns, side spikes, teeth, nostrils, eye rims, layered shell plates, per-foot claws, lichen, mushrooms, flowers, a tiny back sapling, vine loops, extra moss clumps, and pulsing crystal/back glow. Verification: `npx tsc --noEmit`, `npm run build`, `git diff --check`, and `npm run test:e2e:smoke` pass. A targeted headless visual screenshot probe still timed out before the QA hook attached, matching the known WebGL readiness instability; do the final creature-aesthetic judgment in a real desktop browser.
- Model viewer Titan follow-up: added a `Mossback Titan` model option to `?modelViewer=1`, with `?modelViewer=1&model=titan` deep-link support and keyboard shortcut `3`. The viewer uses a dedicated reusable Titan preview rig in `src/render/objects/MossbackTitanAvatar.ts`, scaled/framed for the workshop camera with pose animation and notes. Verification: `npx tsc --noEmit`, `npm run build`, and `git diff --check` pass; targeted Playwright state/screenshot probe confirmed `selectedModel: "titan"` and wrote `output/mossback-titan/model-viewer-titan.png`. Browser opened at `http://127.0.0.1:8008/?modelViewer=1&model=titan`. Full `npm run test:e2e:smoke` had the model-viewer test pass, but one game-shell test failed on unrelated `ERR_CONNECTION_REFUSED` resource noise.
- Titan parked follow-up: removed the Mossback Titan from active simulation, world rendering, water ripples, HUD warning copy, `render_game_to_text`, and the model viewer, then moved the preserved implementation into `src/simulation/unused/giantMossCreature.ts` and `src/render/objects/unused/MossbackTitanAvatar.ts`.

Karu idle companion wander pass:

- Recruited Karu now gently drift around Mossu in seeded little orbit slots when Mossu is idle, grounded, close by, and not rolling, floating, swimming, regrouping, or calling them.
- The wander target is intentionally slow and local to the player so it reads as cute companion behavior without breaking follow, roll mimic, or regroup behavior.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`, and `git diff --check` pass. The required web-game client captured `output/karu-idle-wander/shot-0.png` plus `state-0.json`; it hit the known title-button click stability timeout and did not directly recruit Karu in headless QA.

Grass spatial cell LOD pass:

- Added per-mesh spatial buckets for grass LOD sources so LOD refreshes cull by nearby world cells before touching individual blade source records.
- Tuned grass cell sizes per ring: small cells for near grass, larger cells for mid/far/alpine rings where updates are less frequent and density is lower.
- Added `?perfDebug=1` counters for grass LOD cells and source records visited, so runtime profiling can confirm the scan reduction in real Chrome.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`, and `git diff --check` pass using the bundled Node runtime because `npm` is not on the sandbox PATH.

Far grass impostor + no-loading pass:

- Replaced the far meadow blade ring with a single static instanced painterly patch layer, keeping near grass as real blades and mid grass as fewer animated blades.
- Removed the far meadow patch layer from `windMeshes`, so it no longer pays grass wind uniform updates or grass LOD refresh work.
- Added `?perfDebug=1` counters for far grass impostor meshes/patches/estimated triangles alongside the existing blade grass stats.

CPU optimization pass:

- Fixed gameplay small-prop culling so `syncMapLookdownVisibility()` no longer re-enables culled prop batches every frame. Map mode still hides prop batches, and leaving map mode now forces one culling refresh before rendering.
- Skipped hidden highland grass LOD/uniform work while ridge-vista grass is intentionally suppressed, kept remote Mossu cleanup out of normal non-co-op frames, skipped closed-map camera math during normal gameplay, and made local-save persistence revision-based instead of sorting save sets every frame.
- Verification: `npm run qa`, `npm run test:e2e:smoke`, `npm run perf:guard`, `npm run perf:guard:coop`, `npm run perf:guard:candidate`, and `git diff --check` pass. The failing pre-edit Windstep checkpoint improved from 58.8fps / 27.2ms p95 to 133fps / 9.4ms p95 in the normal guard; the final candidate artifact passed at 232.4fps average / 5.5ms p95 with all 12 route checkpoints reached.
- Removed the explicit bootstrap loading screen and deleted the unused `.app-loading` CSS so normal gameplay no longer shows an "Opening the meadow" or workshop loading panel before the title/game app starts.
- Added a tiny inline HTML/CSS Mossu title shell in `index.html` so the browser paints a branded first frame immediately while the real app chunk starts.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract runner, production `vite build`, `git diff --check`, and the required `develop-web-game` client all ran. The client needed the symlink-preserving local runner to resolve Playwright and captured `output/far-grass-impostors/shot-0.png` plus `state-0.json`; the click helper timed out waiting for the title button to become stable but the captured state shows gameplay entered the opening sequence.

Whole-map redesign pass:

- Added shared in-engine atlas metadata for bridges, POIs, and special spots so the HUD atlas and 3D lookdown can stay aligned with the world sampler.
- Widened the authored route/clearing masks and softened the sampled island outline so the world reads more like a deliberate illustrated island map while preserving the existing Burrow-to-shrine route.
- Added map pan/focus controls: scroll zoom still works, WASD/arrow movement pans while `M` is open, drag pans on the canvas, `F` cycles route-stop focus, and `R`/`Home` resets zoom/pan.
- Rebuilt the map overlay structure so the SVG island atlas is first-class beside a compact parchment guide panel with current area, route steps, legend, progress, and controls.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, production `vite build`, contract runner, and `git diff --check` pass. A targeted `?qaDebug=1` browser probe confirmed intro skip, `M` enters `map_lookdown`, `F` recenters to a route marker, keyboard pan changes the map pan, `Home` resets pan/zoom, and `Esc` returns to third-person.

Pretty waterfall pass:

- Upgraded the highland waterfall system from flat muted panels into layered scenic cascades with blue glass veils, white falling ribbons, pulsing foam shelves, warm spray glints, and animated drifting mist puffs.
- Added larger authored cascade placements around Fir Gate, Mistfall Runoff, Cloudback Rill, and the Mistfall basin so the creek route has more visible waterfall moments.
- Updated the static highland accent waterfall ribbons to match the richer water treatment with foam/spray layers, so close-up scenic props and animated waterway falls share the same art direction.
- Added a `?qaDebug=1`-only `teleportPlayerTo(x, z)` hook for targeted route/screenshot QA without affecting normal play.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, production `vite build`, and the contract runner pass. The required web-game client could not run because the skill script could not resolve its own `playwright` import from `/Users/hunterbastian/.codex/skills`; project-local Playwright reached the page, but both normal screenshot and canvas capture paths hung in headless Chromium during WebGL capture.

Breeze Float controls pass:

- Added `Q` as a dedicated Breeze Float input while preserving the previous Space-hold behavior after jumping.
- Added a short Breeze Float input buffer so a tapped/early `Q` press can still catch the float briefly after release, improving ledge and roll-jump timing.
- Updated HUD/control copy so normal play teaches Space as jump and `Q / Space` as Breeze Float.
- Added movement contracts for Q-only Breeze Float and tapped-Q buffered Breeze Float, alongside the existing Space-held float contract.
- Verification: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, production `vite build`, and the contract runner pass. A targeted browser-state probe was attempted against the built app, but the headless Three.js runtime hung before returning the final state, matching the current browser QA instability noted in recent visual passes.

Karu remodel + opening vista pass:

- Extracted the reusable Karu visual rig into `src/render/objects/KaruAvatar.ts` so the model viewer no longer imports the full ambient herd simulation just to preview one Karu.
- Updated gameplay Karu and the model viewer to share that rig, with a softer little-spirit silhouette: wider cloud body, larger glossy eyes, cheek blush, tail, four feet, and extra fluff puffs.
- Added a small authored opening nest/vista decoration layer near spawn, including a soft nest floor, leaf clumps, twigs, stones, flowers, and a pebble/leaf lead-out toward the route.
- Verification: `tsc --noEmit`, `vite build`, contract tests, and route-split browser probing pass. The route probe confirmed `/` loads `GameApp` and `?modelViewer=1` loads `ModelViewerApp`; neither route loads the other app chunk.
- Required `develop-web-game` client pass completed and wrote `output/karu-nest-smoke/shot-0.png` plus `state-0.json`.

Dynamic import workshop pass:

- Route-split the main bootstrap so normal gameplay dynamically imports `GameApp` and `?modelViewer=1` dynamically imports `ModelViewerApp`.
- Added a small Mossu-styled loading state while the selected route chunk loads, without changing the title-screen link or QA hooks.
- Kept `window.advanceTime` and `window.render_game_to_text` attached after either route starts.
- Verification: `tsc --noEmit`, `vite build`, contract tests, and `git diff --check` pass. Build output now includes a tiny bootstrap chunk plus separate `GameApp` and `ModelViewerApp` chunks.
- Browser route probe confirmed `/` loads `index`, `GameApp`, and shared world code without `ModelViewerApp`; `?modelViewer=1` loads `index`, `ModelViewerApp`, and shared world code without `GameApp`.
- Required `develop-web-game` client was attempted through the repo symlink path; it reached the page but timed out while taking the screenshot, matching the known headless screenshot issue in this project.

Movement audio pass:

- Added the provided grass-footstep MP3 as a public game asset at `public/audio/footsteps-grass-loop.mp3`.
- Added a small native `MovementAudio` controller that unlocks from the title-screen interaction, fades footsteps in only during grounded third-person movement, scales volume/rate with speed, and gives rolling a slightly faster/stronger texture.
- Stopped the loop immediately when pause, inventory/model card, or map overlays open, and exposed movement-audio state through `render_game_to_text` for QA probes.
- Added the provided menu-click MP3 as `public/audio/menu-ui-click.mp3` and a reusable `InterfaceAudio` helper for short UI button feedback.
- Wired the click sound through delegated pointer/keyboard activation in the main game shell and model viewer, so current and future buttons get the UI sound without one-off listeners.

HUD / overlay UX pass:

Model viewer pass:

- Started a separate `?modelViewer=1` route so Mossu tools can inspect one character at a time without changing normal gameplay startup.
- Exposed the Karu creature rig for reuse outside the ambient herd simulation, keeping the viewer pointed at real in-game model parts.
- Added a Mossu-themed model viewer shell with model selection, pose buttons, lighting presets, play/pause, turntable, and a timeline scrubber.
- Added a secondary `Model Viewer` action to the main title screen so the workshop is reachable from the game menu.
- Reframed the title-screen workshop entry as the intended `Play -> Model Editor -> Map Editor` flow. Play starts the trail, Model Editor opens the active tool, and Map Editor is staged as the next tool.
- Verification:
- `tsc --noEmit`, `vite build`, contract tests, and `git diff --check` pass. Build was run through a local ad-hoc signed `/tmp/mossu-node` because the installed hardened Node rejects Rollup's native package signature in this sandbox.
- Required `develop-web-game` client captured `output/model-viewer-smoke/shot-0.png` and `state-0.json` for `http://127.0.0.1:8006/?modelViewer=1`.
- Targeted Playwright QA clicked Mossu -> Karu, Roll pose, and Forest lighting, then captured `output/model-viewer-qa/mossu-full.png`, `karu-roll-full.png`, and `results.json`.
- Main-menu link follow-up verification: `tsc --noEmit`, `vite build`, contract tests, and `git diff --check` pass. A targeted production-bundle browser pass confirmed the title-screen `Model Viewer` link has `href="?modelViewer=1"` and opens the viewer; screenshots are in `output/title-model-viewer-link/`.
- The required web-game client was rerun for the title-link follow-up, but its fixed `page.goto(..., waitUntil: "domcontentloaded")` timed out on the production Three.js bundle in this environment before it could capture artifacts; the targeted Playwright pass verified the link with a less brittle navigation wait.
- Workshop-flow follow-up verification: `tsc --noEmit`, `vite build`, contract tests, `git diff --check`, and the required `develop-web-game` client pass all ran after changing the title menu to `Play -> Model Editor -> Map Editor`. Targeted browser QA confirmed the visible labels, confirmed `Play` starts gameplay, confirmed `?modelViewer=1` opens the model viewer, and captured `output/title-workshop-play-flow/title-flow.png` plus `play-after-click.png`.
- Next tool direction: build the map builder as a separate workshop route too, with authored controls layered over procedural generation masks rather than replacing the current world sampler.

Open-world scale/readability follow-up:

- Made the generated island and terrain plane substantially larger, with a later coastal falloff so the playable edge feels less tight around the route.
- Widened route terraces, painted clearings, scenic pocket masks, biome transition glades, and travel-band habitat masks so biome changes feel more like open travel bands than narrow corridors.
- Reduced forest pressure near travel clearings while preserving authored grove contracts, and updated the grass shader island-edge normalization to match the larger island footprint.
- Verification: `npm run qa` passed. A live dev server is running at `http://127.0.0.1:4179/`; browser screenshot tooling still hit the known WebGL capture/ready-timeout path, and `npm run test:e2e:smoke` timed out waiting for its configured preview server on `4173` before tests ran.

Even-larger field follow-up:

- Pushed the highland/open-field direction closer to the provided Mountain & Hill spec: larger terrain plane, wider island radii, broader meadow/route/scenic-pocket masks, softer open valley bowls, and wider rounded hill shoulders.
- Increased grass budgets enough to keep the expanded fields from feeling barren, while making tree/vista protection stricter so broad fields stay readable instead of filling back in with forest.
- Thinned scenic pocket props across rocks, bushes, saplings, flowers, moss, and small clumps so the composition reads as big grassy slopes with sparse detail.
- Verification: `npm run qa` passed and `git diff --check` passed for touched files. The required web-game client was attempted twice through the project-local Playwright symlink, but both runs exited with `Execution context was destroyed, most likely because of a navigation` before producing screenshot artifacts.

Floating-island field/isle-terrain follow-up:

- Reinterpreted the open-field target as a broad floating-island landmass: expanded the terrain plane and island radii again, softened the cliff drop, and made the island silhouette wider like a low grassy island with hills at the outer shoulders.
- Added a larger central-field/vista mask so the main route sits inside broad grassland basins, while keeping the required lowland grove contract as a small protected forest pocket.
- Widened route terraces, painted clearings, route transition glades, scenic pocket influence, and tree-exclusion bands so trees read as edge lines and isolated landmarks rather than a corridor wall.
- Increased terrain tessellation from 160 to 192 segments so the larger island does not lose too much heightfield definition.
- Verification: `npm run qa` passed and `git diff --check` passed for touched files. The required web-game client was attempted again through the project-local Playwright symlink but exited with `Execution context was destroyed, most likely because of a navigation` before screenshots were produced.

Water washout + highland creek pass:

- Reduced the water shader's tendency to blow out into pale slabs by lowering river/creek opacity, clamping shoreline milk/foam brightness, tightening alpha, and making the depth bands less aggressively bright.
- Added shared authored highland creek paths in `src/simulation/world.ts` so water-state sampling and rendered highland creek geometry agree instead of leaving `creekPaths` empty.
- Implemented `buildHighlandWaterways()` in `src/render/world/waterSystem.ts` with narrow foothill/alpine runoff ribbons and three small island-style waterfall accents.
- Tapered the rendered main river through the foothill/highland route so the mid-route creek owns the scene more, instead of one broad river surface covering the slope.
- Reduced mid-route valley mist coverage in `WorldRenderer.ts` after screenshot QA showed it was contributing to the “translucent slab” read over the creek corridor.
- Verification: `npm run build` and `npm run test:contracts` passed during the pass; fresh screenshot artifacts are in `output/qa-water-fix/`.

Grass/performance cleanup pass:

- Reduced terrain tessellation from 240 to 192 segments, dropping terrain from 115,200 triangles to 73,728 triangles.
- Simplified grass blade geometry from 4x6 subdivisions to 2x4 subdivisions, then reinvested that budget into denser grass placement.
- Increased total grass instances from roughly 13.3k to roughly 20.7k while dropping estimated grass triangles from roughly 765k to roughly 412k.
- Added noise-thresholded clumpy grass density in `sampleGrassDensity()` so grass gathers into irregular patches with readable gaps instead of a uniform field.
- Verification artifacts: `output/qa-grass-perf/opening-qa-camera.png` and `opening-qa-camera-state.json`.

- Tightened the normal-play HUD in `src/render/app/GameApp.ts` and `src/styles.css` so the large controls panel is now contextual instead of always visible; it appears when camera control is unlocked or when a menu state needs extra guidance.
- Replaced the old sentence-heavy quick-hint copy with compact keycap action pills so the bottom HUD reads faster during play.
- Added clearer progress framing across the `Esc`, `E`, and `M` surfaces: pause now shows gathered-goods counts, the Adventure Card sections show live count badges, and the map now reports both route progress and goods/field-note totals.
- Kept the frosted-glass direction, but shifted this pass toward hierarchy and disclosure instead of adding more decorative chrome.
- `npm run build` passes after the UX cleanup.

Forageable gathering pass:

- Added authored world forageables in `src/simulation/world.ts` so Mossu can now collect small fruits and plants placed along the route from the meadow start up through the ridge climb.
- Added `src/simulation/forageableProgress.ts` plus new `gatheredForageableIds` / `latestGatheredForageableId` state in `src/simulation/gameState.ts` so these goods auto-gather when Mossu wanders close enough.
- Extended the `E` Adventure Card in `src/render/app/GameApp.ts` with a dedicated `Gathered Goods` section on the right and updated the character summary / prompt text to surface recent forage pickups.
- Added visible in-world pickup props in `src/render/world/WorldRenderer.ts` so fruit and plant collectibles now bob lightly in the scene and disappear once gathered.
- Added matching frosted-glass card styling for gathered goods in `src/styles.css`.
- `npm run build` passes after the forageable-gathering implementation.

Character screen layout follow-up:

- Resized the `E`-menu character preview down so Mossu sits in a smaller left-side model card instead of dominating the whole panel.
- Split the character-screen content into two columns in `src/render/app/GameApp.ts`: stats/upgrades stay in the main column, while `Field Dex` now lives in its own dedicated collections column on the right.
- Updated `src/styles.css` so the collections rail gets more vertical room and the responsive layout still collapses back to one column on smaller screens.
- `npm run build` passes after the character-screen layout adjustment.

Frosted-glass material pass:

- Pushed the full HUD, pause menu, Adventure Card, and Region Map surfaces toward a colder frosted-glass look in `src/styles.css`.
- Reduced the remaining paper/plastic feel by replacing warm opaque fills with more translucent blue-white panes, lighter edge rims, and denser backdrop blur.
- Retained the Pokemon-like menu framing and current overlay layout, but made the material language read more like layered glass than painted cards.
- Kept the recent bounce/gloss motion pass, now with highlights and shell fills tuned to feel diffused through glass instead of sitting on top of it.
- Build follow-up:
- `npm run build` passes after the frosted-glass pass.
- While verifying this pass, the build was temporarily blocked by grass-system type/export drift in `WorldRenderer.ts` and `grassSystem.ts`; that compile issue was cleaned up as part of this turn so verification could complete.

Glossy overlay + pause/menu flow pass:

- Added a dedicated `Esc` pause menu in `src/render/app/GameApp.ts`, with direct transitions to resume, the Adventure Card, and the Region Map.
- Reworked overlay flow so the pause menu, character screen, and map are mutually exclusive instead of visually stacking on top of each other.
- Tightened map behavior so `M` now owns the screen as a proper zoomed-out state while normal HUD chrome hides behind it.
- Added a more bouncy/glossy Frutiger Aero-style motion pass in `src/styles.css` across the live HUD, pause shell, map shell, and Adventure Card.
- `render_game_to_text` now reports pause-menu state in addition to the existing map / character-screen state.
- Verification:
- `npm run build` passes after the overlay-flow and glossy-motion changes.
- Computer Use verified the new pause shell visually on the live local page and confirmed the normal HUD renders separately in gameplay.
- A direct local Playwright keyboard probe was attempted again for `M` / `E` / `Esc`, but it stalled in this environment before producing screenshots or JSON, so keyboard-driven runtime verification is still partial.

Old Pokemon-inspired UI pass:

- Reframed the HUD, map, and character screen toward a retro handheld RPG menu style with cream panels, navy borders, red-blue accent tabs, and more rectangular menu framing.
- Updated `src/render/app/GameApp.ts` copy so the interface now reads as a quest log / adventure card / region map / field dex instead of the previous handbook wording.
- Restyled the major UI surfaces in `src/styles.css` so the HUD, profile screen, and map overlay all share the same older Pokemon-like visual language.
- Added a light Frutiger Aero layer on top of the retro menus with brighter sky-blue glass gradients, stronger white gloss, and softer aqua highlights while keeping the Pokemon-style framing.
- Verification follow-up:
- `npm run build` should be rerun after the retro UI restyle.

Original prompt: PLEASE IMPLEMENT THIS PLAN:

# Full-Map Lookdown Mode On `M` + Slight Gameplay Camera Pullback

- Added `ViewMode` runtime state and one-shot `M` / `Esc` input events.
- Added a blended overhead map camera on `M` that releases pointer lock and returns cleanly to third-person.
- Increased gameplay camera distance by roughly 10% and widened wheel zoom range to match.
- Added lightweight world-space map markers for Mossu, the shrine, and key landmarks.
- HUD now switches to a lighter map presentation while the map is open.
- `npm run build` passes.
- Local dev server for this turn is running at `http://127.0.0.1:4175/` because `4173` and `4174` were already in use.
- Bug sweep:
- Fixed the grass instancing shader compile error in `WorldRenderer.ts` by passing `instanceTint` through the vertex shader instead of illegally declaring it as a fragment `attribute`.
- Wrapped pointer-lock requests in `FollowCamera.ts` so failed browser requests do not throw noisy runtime errors.
- Hid live grass meshes during `map_lookdown` to make the overhead map read more cleanly.
- Re-verified with automated screenshots; build still passes.
- Grass visual pass:
- Increased meadow/alpine grass density.
- Rebuilt the grass blade geometry to be wider, softer, and more tuft-like instead of thin needle cards.
- Added width variation, gentler color clustering, softer alpha shaping, and less harsh posterization in the grass shader.
- Meadow/alpine tint ranges were lightened to better match the painterly reference direction.
- HUD visual pass:
- Added a more field-journal / adventure-panel treatment to the HUD with parchment-mint gradients, olive ink, and warm gold accents.
- Objective chip now has stronger hierarchy and a small badge detail.
- Status strip metrics now have cleaner separators and label/value contrast.
- Hint and ability chips were reshaped and recolored to feel softer and less generic.
- Camera pass:
- Raised the default third-person chase angle slightly and opened the pitch clamp upward so the player can view more terrain from above during normal play.
- Character pass:
- Updated `MossuAvatar` only to match the snowy/fluffy character direction.
- Swapped the body toward a faceted fluffy snowball look with softer off-white shading.
- Added larger glossy oval eyes, softer cheeks, and small top tufts to improve silhouette and roll readability.
- Added more squash/stretch and subtle tuft motion so idle, roll, and airtime feel softer and more buoyant.
- Walk / roll mode pass:
- Added `Shift` as a held roll mode in input and gameplay state.
- Mossu now stays in stub-leg walk form normally, and shifts into a rolling snowball form while `Shift` is held.
- Added simple leg animation and updated the HUD hint to teach `Shift` roll controls.
- Walking speed is now slower than rolling speed so the two forms feel mechanically distinct.

Inventory / interact pass:

- Added `E` as a context action and `Tab` as an explicit satchel toggle in `src/simulation/input.ts`.
- Landmarks now carry keepsake metadata in `src/simulation/world.ts`, with per-landmark interaction radii and satchel copy.
- `src/simulation/gameState.ts` now tracks nearby interactables, cataloged landmark ids, the last cataloged keepsake, and exposes `getInventoryEntries()` for the HUD.
- `src/render/app/GameApp.ts` now opens a trail-satchel panel, pauses movement while it is open, releases pointer lock on open, shows a nearby-keepsake prompt, and exports `window.advanceTime` / `window.render_game_to_text` hooks for automation.
- `src/styles.css` now includes satchel/prompt styling that matches the existing field-journal HUD treatment.

Verification:

- `npm run build` passes after the inventory/interact changes.
- Attempted to run the required Playwright web-game client from an in-process static server so it could reach the built app in this sandbox. The client repeatedly hung with no artifacts written, even after removing the canvas click path.
- Follow-up direct Playwright probes also stalled once the built page was live in headless Chromium: navigation completed and WebGL warnings appeared, but the first `page.evaluate(...)` call never returned.
- Because of that runtime issue, this turn only has build verification plus code-path inspection; the next agent should debug the headless Chromium hang before relying on automated gameplay screenshots for `mossu`.

Character screen pass:

- Replaced the old satchel UI with a dedicated character screen opened by `E` or `Tab`, with `Esc` to close and gameplay paused while it is visible.
- Animal Crossing-inspired UI pass:
- Restyled the persistent HUD and resident handbook in `src/styles.css` toward a softer life-sim presentation with rounded paper panels, wood-and-leaf accents, stitched badges, and warmer cozy copy.
- Updated `src/render/app/GameApp.ts` HUD labels and handbook phrasing so the interface reads more like an island resident journal than a generic debug overlay.
- Extended the same guidebook treatment to the full-map overlay with a resident stamp, softer chart copy, a decorative compass rose, and subtle opening / marker pulse motion.
- This pass is presentation-only: gameplay flow, controls, and character-screen behavior remain the same.
- Verification follow-up:
- `npm run build` should be rerun after the UI restyle to catch any layout-adjacent TypeScript/CSS regressions.

- Added a dedicated Three.js preview renderer in `src/render/app/CharacterPreview.ts` so the left side of the menu shows a live Mossu model instead of reusing the world camera.
- Reworked `src/render/app/GameApp.ts` to render profile-oriented HUD copy plus a two-column character screen with `Stats`, `Upgrades`, and `Collections`.
- Reworked `src/simulation/gameState.ts` so collections auto-log when Mossu enters an existing landmark interaction radius, and added a `getCharacterScreenData()` view-model payload for the menu.
- The character screen shows real current traversal stats, `Breeze Float` as the active upgrade, three locked placeholder upgrades, and discovered vs undiscovered keepsakes.

Verification follow-up:

- `npm run build` still passes after the character-screen refactor.
- The required bundled Playwright client now reaches the browser but still exits with `Target page, context or browser has been closed` and produces no artifacts in this environment.
- An escalated direct Playwright pass was attempted to validate `E` / `Tab` / `Esc`, map-to-profile transition, and automatic collection discovery, but it appears to hang before writing screenshots or JSON.
- Treat browser validation for this character-screen turn as incomplete; the implementation is currently verified by successful build and code-path review only.
- Water pass:
- Replaced the tube-based river and creek geometry in `WorldRenderer.ts` with terrain-hugging ribbon surfaces so the waterways read as actual water instead of rounded pipes.
- Added a lightweight animated water shader pass with moving shimmer, center-depth tinting, and softer bank fade.

Mountain-route worldbuilding slice:

- Re-authored the current floating-island slice around a clearer climb: Burrow Hollow -> Silver Bend -> Fir Gate / Whisper Pass -> Mistfall Cascade / Basin -> Windstep Shelf -> Cloudback Ridge -> Moss Crown Shrine.
- Extended `world.ts` with new upper-route scenic pockets and landmarks, rebalanced the 14 orb placements so the first 8 naturally guide the lower climb, and kept the last 6 on the alpine/ridge route after `Breeze Float`.
- Strengthened terrain staging with small authored shelves/rises along the intended mountain route while keeping the overall island footprint intact.
- Warmed plains terrain/light, cooled alpine/ridge terrain, opened the start lane a bit, and pushed the upper route harder through rocks, pines, waterways, moss, and atmosphere.
- Updated ambient plains fauna to berry-blue fuzzy blobs without changing their calm behavior.
- Tightened the opening chase camera and increased grass fade around the player to reduce foreground swallowing.
- `npm run build` passes.
- Fresh dev server for this pass: `http://127.0.0.1:4177/`.
- Live Dia check: the opening frame is improved and shows the climb/river structure more clearly, but there is still room for another small presentation pass if needed.
- Water verification:
- `npm run build` passes after the water refactor.
- Manual Safari screenshot check on the local Vite preview confirmed the main river now reads as a flat flowing surface instead of a rounded tube.
- Water flow direction:
- Added downhill-aware water flow detection so ribbon streams authored in reverse point order still animate downhill instead of scrolling uphill.
- Tsushima-style water stage 1:
- Refactored `WorldRenderer.ts` water creation behind controller/profile seams so river and creek ribbons now share `WaterProfile` presets plus `createWaterSurface(...)` instead of ad hoc animated mesh traversal.
- Extended ribbon geometry with explicit `aChannel`, `aBank`, `aSlope`, and `aFlowT` attributes, then rebuilt the WebGL `MeshStandardMaterial` shader patch to layer broad flow, detail shimmer, slope-scaled motion, bank foam, deeper center-channel tint, and warm grazing-angle highlights.
- Highland waterways now use dedicated presets for foothill creek, alpine runoff, and waterfall outflow so steep descents read faster/brighter than the calmer main river.
- `npm run build` passes after the refactor, and a manual Safari check against `http://127.0.0.1:8000/` confirmed the river still reads as a flat surface while the mountain runoff sections look more disturbed and downhill-oriented.
- Water graphics follow-up:
- Pushed the shader away from clean procedural stripes by adding profile-level reflection/sediment/clarity controls, denser ribbon cross-sections, domain-warped vertex motion, and fragment-side FBM breakup for currents, eddies, sparkle, and shoreline sediment tint.
- The current pass now mixes cooler sky reflection with warm glints, uses muddier shallow banks, and breaks foam/highlights into less uniform ribbons so the water should read less flat and synthetic.
- `npm run build` passes after this graphics-focused pass. A fresh in-session screen capture failed with `could not create image from display`, so this follow-up is build-verified but not archived with a new screenshot artifact.
- Water depth follow-up:
- Added shallow-bed and depth-language controls to each water profile so calmer water can reveal a muted riverbed tint while deeper channels darken more decisively.
- Extended the fragment shader with fake bed visibility, pebble/sediment breakup, moving caustic light in shallows, and stronger depth shadow through the center channel.
- `npm run build` passes after the depth pass. This turn did not produce a fresh screenshot artifact.
- Swim pass:
- Added shared water-volume sampling in `src/simulation/world.ts` for the filled main river plus authored creek/pool checks, with explicit surface heights, current direction, and swimmable-depth gating.
- Reworked `src/simulation/gameState.ts` so deep water no longer behaves like ground: Mossu now enters a `swimming` state, floats near the water surface, gets carried slightly by current, uses `Space` as a swim stroke, and cannot roll while in swimmable water.
- Synced player-facing systems to the new state: `render_game_to_text` now reports `swimming` and `waterDepth`, `FollowCamera.ts` eases into a slightly closer/lower swim framing, `MossuAvatar.ts` uses a swim bob and disables leg visibility while swimming, and `WorldRenderer.ts` now pulls water surface offsets from the shared world constants so the visible river sits high enough to match the swim volume.
- `npm run build` passes after the swim pass.
- Tried the required `develop-web-game` Playwright client against the local dev server after the swim implementation, but it still fails from the skill path with `ERR_MODULE_NOT_FOUND: Cannot find package 'playwright' imported from .../web_game_playwright_client.js`. No automated swim screenshot artifact was produced from this attempt.
- Water Ghibli + sparkle pass:
- Kept the work scoped to the water shader in `WorldRenderer.ts`: softened the main tint/depth blend slightly, reduced some of the harsher depth darkening, and added a restrained `sparkleColor` / `sparkleStrength` profile control across river and creek presets.
- Added a small animated sparkle mask that scatters shallow sun twinkles across the surface instead of broad synthetic stripes, so the water should feel a little more hand-painted and a little less procedural.
- `npm run build` passes after this pass. No fresh screenshot artifact was captured this turn.
- Lighting / meadow paintover pass:
- Regraded `WorldRenderer.ts` toward warmer late-afternoon sunlight with earlier fog falloff, cooler sky bounce, larger cream-tinted cloud masses, and more atmospheric distant mountains.
- Reworked plains / foothill terrain tinting and grass instancing so the meadow reads broader and more painterly: taller/wider blades, directional sweep, brighter straw-cream tips, and less harsh posterization.
- Added a shader-driven sky dome so the opening sky is no longer a flat solid color and carries soft sun bloom plus painterly cloud breakup.
- `npm run build` passes after the lighting pass.

UI interactivity follow-up:

- Converted the pause menu actions from static cards into real clickable/focusable buttons backed by `data-ui-command` handlers in `GameApp`.
- Added focus handoff for title, pause, handbook, map, and gameplay return paths so keyboard/mouse overlay flow feels intentional instead of passive.
- Made handbook section tabs interactive and added smooth scrolling to Profile / Cards / Pouch sections.
- Made keepsake and pouch-good holo cards keyboard focusable with the same glare/tilt language used for pointer hover.
- Verification: `tsc --noEmit`, `vite build`, existing contract runner, and `git diff --check` pass using the bundled Node runtime because `npm` is not on the sandbox PATH.
- Browser QA caveat: the required `develop-web-game` client still cannot resolve `playwright` from the skill path in this checkout, and targeted Playwright against the built app timed out before `window.render_game_to_text` attached, matching the known headless Mossu/WebGL startup issue. Vite preview also cannot start right now because the disk only has about 116 MB free and cannot write its temp config file.
- Headless Playwright screenshots against the local Vite server were not reliable for this scene: Chromium captured an over-washed, faceted WebGL frame that does not appear safe to use as visual truth. A real browser check is still the right next verification step for final tuning.
- Hybrid meadow lighting + grass pass:
- Recentered the opening-plains look toward the second reference without losing the soft Mossu mood: lower warmer sun, later fog falloff, bluer sky separation, and fewer larger cloud masses.
- Added an opening-meadow emphasis mask in `WorldRenderer.ts` so plains/hills terrain and grass get the strongest olive/straw treatment near the start route while alpine/ridge grass stays close to the lighter existing treatment.
- Reworked meadow grass to read more like a field: slimmer/taller blades, stronger left-to-right lean, banded wind, brighter grazing highlights, and distance-aware color compression so the near field stays blade-defined while the mid/far field fills in as a coherent mass.
- Added a small `FollowCamera.ts` framing tweak so the initial spawn view carries a bit more horizon and less immediate downward tilt, which better exposes the opening meadow treatment.
- `npm run build` passes after the hybrid meadow pass.
- Real-browser verification used a local Vite server plus Safari + `screencapture` instead of headless Chromium. The on-screen shot confirms the updated meadow/river framing path is live, though Safari reused an existing localhost tab so the captured URL bar was not a clean proof of the temporary port.
- Starter spirit close-up verification:
- Local dev server for this verification run is on `http://127.0.0.1:4179/`.
- The stock `develop-web-game` Playwright client still fails in this repo flow: direct launch from the skill path cannot resolve the local `playwright` package, and the symlinked repo-path launch hangs with no artifacts in `output/spirit-check`.
- Added a repo-local fallback probe at `.codex-tmp/spirit-closeup-check.mjs`, but headless Chromium needed escalation and still did not yield usable screenshot artifacts in this environment.
- Computer Use + Safari did verify the live build loads and the starter scene still reflects the lighter/paler ambient-spirit pass at a wide shot.
- The current browser-control path did not produce a trustworthy close-up of the fauna cluster, so idle/wander silhouette verification is still incomplete.
- If close-up spirit validation is required next, the most reliable follow-up is a temporary debug spawn/camera hook that places one ambient spirit directly in front of the start view for screenshots.
- Spirit close-up debug hook:
- Added a debug-only `?spiritCloseup=1` URL flag in `src/render/app/GameApp.ts` and `src/render/world/WorldRenderer.ts`.
- In that mode, `WorldRenderer` re-stages the first few ambient spirits near the spawn camera and scales the lead spirit up slightly so the opening frame can show the fauna clearly without changing normal gameplay.
- `npm run build` passes after the hook.
- Safari + Computer Use verification against `http://127.0.0.1:4179/?spiritCloseup=1` now shows the staged spirits in-frame near Mossu; the pale fluffy silhouette and larger dark eyes read correctly in-context.

Map readability pass:

- Reworked `M` map mode in `src/render/app/GameApp.ts` from a mostly-camera-based overview into a dedicated illustrated HUD map board built from real world data.
- Added a stylized floating-island silhouette, river path, climb route line, compass, labeled landmarks, route-step sidebar, live player marker, shrine marker, and discovered/unvisited landmark states.
- Kept the existing `map_lookdown` camera as the background path, but the useful readability now comes from the map overlay rather than from the raw 3D terrain alone.
- `src/styles.css` now includes a full map-board presentation with parchment shell, legend, route-step states, and landmark/marker styling.

Verification:

- `npm run build` passes after the map overlay work.
- Live headless Playwright state check against `http://127.0.0.1:4178/` confirmed `mode` switches from `third_person` to `map_lookdown` after pressing `M`.

Performance responsiveness follow-up:

- Reduced normal gameplay DOM churn by throttling HUD refreshes to 12 Hz unless an overlay, stamina, pickup, landmark, forage, or Karu prompt needs immediate feedback.
- Made adaptive render quality react sooner to slow frames, with a slightly lower minimum pixel ratio for temporary lag recovery.
- Stopped grass LOD from rebuilding instance buffers just because a timer elapsed; it now waits for player movement plus the existing frame cadence after the initial build.
- Reduced live water ripple shader budget from 8 ripple sources to 4, which keeps player/Karu ripples but halves the fixed ripple loop work in the water shader.
- Verification: `npm run test:contracts`, `git diff --check`, and `npm run build` pass.
- Browser smoke note: the bundled `develop-web-game` Playwright client still cannot resolve its own `playwright` import from the skill path. A direct local Playwright probe could reach the page with `waitUntil: "commit"`, but the deterministic `advanceTime` loop hung in this environment and was killed; real Chrome was opened at the fresh Vite URL instead.

Subtle bloom pass:

- Added a lightweight Three.js `EffectComposer` path with `RenderPass` + `UnrealBloomPass` in `src/render/app/GameApp.ts`.
- Bloom is intentionally restrained (`0.14` strength, `0.48` radius, `0.82` threshold) and turns off automatically in map lookdown or when adaptive quality drops pixel ratio below `0.74`.
- Perf debug now reports whether bloom is currently on or off.
- Verification: `npm run test:contracts`, `git diff --check`, and `npm run build` pass.
- Browser smoke note: the page booted with `render_game_to_text` available and two canvases present on `http://127.0.0.1:4192/`; headless screenshot capture still timed out on WebGL readback, so final visual judgment should be from real Chrome.
- Full-page screenshots remain unreliable because the WebGL canvas can hang the capture, but a targeted element screenshot of `.world-map__shell` succeeded and was visually inspected at `/tmp/mossu-map-overlay-panel.png`.
- Green-grass-v3 comparison pass:
- Used Computer Use to inspect `https://green-grass-v3.vercel.app/` directly and translated the strongest traits into `mossu`: cleaner blue sky, lower warmer sun, narrower taller meadow blades, stronger shared blade lean, and more aggressive near-to-far field compression.
- Reworked `WorldRenderer.ts` so the opening plains rely more on coherent meadow massing than painterly breakup: simpler olive/straw terrain underpainting, sparser larger cloud masses, calmer atmospheric veil, meadow-biased placement/orientation, and denser opening-field instancing without forcing the alpine route onto the same look.
- Added another `FollowCamera.ts` adjustment to lower the gameplay rig and carry more horizon into the opening view after the live Safari check still felt too lookdown-heavy.
- `npm run build` passes after the green-grass comparison pass, and Computer Use + Safari verification confirms the opening field is denser and more directional even though the overall game remains more stylized/low-poly than the reference.
- Grass V3 technique-followup:
- Pushed the meadow closer to the reference technique rather than just the color grade by switching opening-field grass to crossed-card blade geometry, reducing alpha cutoff, and thickening the base/far-field fusion in the grass fragment shader.
- Kept alpine/ridge grass on the lighter single-plane path so the extra volume stays concentrated in the start meadow instead of inflating the whole world.
- Fixed a local compile blocker in `src/render/app/CharacterPreview.ts` by filling the newer `PlayerState` water fields used elsewhere in the repo.
- `npm run build` passes after the technique-followup, and a live Safari check on `http://127.0.0.1:4182/` confirms the meadow now reads fuller and less like isolated spikes.
- Meadow LOD + self-shadow pass:
- Split the opening meadow into separate near/mid/far grass passes in `WorldRenderer.ts`, each with its own blade geometry, density bias, size profile, and camera-distance fade band so the field can read differently across depth instead of relying on one shared grass mesh.
- Extended the grass shader with explicit tier fade uniforms plus stronger self-shadowing: darker roots, more body occlusion away from the sun stripe, and heavier distance compression for the far field.
- Alpine/ridge grass stayed on a single lighter pass while the meadow tiers use 3-plane near grass, 2-plane mid grass, and a wider single-plane far field.
- `npm run build` passes after this pass, and a live Safari check on `http://127.0.0.1:4183/` shows the meadow tiers blending without an obvious seam from the opening camera.
- Shoreline milkiness pass:
- Shifted the water art direction toward a softer Studio Ghibli edge treatment by adding per-profile `shorelineMilkColor` and `shorelineMilkStrength` controls in `WorldRenderer.ts`.
- Updated the water fragment shader so shallow banks blend into a pale silty wash before the foam line, with less harsh edge contrast and slightly softer shoreline alpha.
- Fixed adjacent compile drift in `WorldRenderer.ts` and `src/render/app/CharacterPreview.ts` so the shoreline pass lands in a clean state instead of sitting on top of unrelated TypeScript breakage.
- `npm run build` passes after the shoreline pass.
- Opening lake pass:
- Added a small swimmable lake near the starting meadow by carving a dedicated basin and water volume into `src/simulation/world.ts`.
- Added a radial lake water surface in `src/render/world/WorldRenderer.ts` so the opening area now has a calm pond using the same stylized water shading family as the river instead of a flat placeholder mesh.
- Reduced grass density inside the basin so the lake reads as a real opening-area landmark rather than grass clipping through the water.
- `npm run build` passes after the opening lake pass.
- Opening lake enlargement + live verification:
- Increased the opening lake footprint substantially by widening the basin, deepening it, and broadening the rendered radial surface so it reads more like a real lake from the starting camera.
- Re-verified the opening view live in Safari against `http://127.0.0.1:4186/`; the updated lake is now clearly visible in the start area.
- Saved the verification capture to `output/opening-lake-verify-4.png`.
- Systems refactor pass:
- Extracted the full grass mesh/shader pipeline out of `src/render/world/WorldRenderer.ts` into `src/render/world/grassSystem.ts`, then rewired the renderer to use the new module instead of carrying a second inlined grass implementation.
- Extracted map projection/data helpers into `src/render/app/worldMap.ts` so `GameApp.ts` no longer owns the SVG namespace, viewbox math, route-path generation, or label-layout tables.
- Extracted landmark collection/progress helpers into `src/simulation/landmarkProgress.ts` so `GameState.ts` can focus on player simulation while the landmark catalog, nearby-target, and closest-landmark logic live in one place.
- `npm run build` passes after the refactor pass.
- World renderer subsystem split:
- Extracted the remaining scene-heavy renderer subsystems into dedicated modules: `src/render/world/waterSystem.ts`, `src/render/world/atmosphereSystem.ts`, `src/render/world/ambientBlobs.ts`, `src/render/world/terrainDecorations.ts`, and shared helpers in `src/render/world/sceneHelpers.ts`.
- Removed the duplicate inlined water, sky/cloud/atmosphere, ambient blob, and terrain-decoration implementations from `src/render/world/WorldRenderer.ts`, leaving the renderer to compose and orchestrate the systems instead of owning their factory code directly.
- Preserved the current blob blink/idle-pose behavior in the extracted ambient system so the refactor does not regress creature animation while reducing the renderer’s state surface.
- `npm run build` passes after the renderer subsystem split.
- Game state subsystem split:
- Reworked `src/simulation/gameState.ts` into a coordinator over focused player-simulation modules instead of a monolithic traversal file.
- Extracted shared traversal constants into `src/simulation/playerSimulationConstants.ts`, transient timer state into `src/simulation/playerSimulationRuntime.ts`, locomotion into `src/simulation/movementPhysics.ts`, water traversal into `src/simulation/waterTraversal.ts`, stamina/ability handling into `src/simulation/staminaAbilities.ts`, and void fall / respawn logic into `src/simulation/respawnSystem.ts`.
- Kept landmark and forageable progress in the existing extracted modules so `gameState.ts` now mainly wires frame state, character-screen data, and the top-level update order together.
- `npm run build` passes after the game state subsystem split.
- Ongoing coordinator refactor pass:
- Extracted character-screen view shaping out of `src/simulation/gameState.ts` into `src/simulation/characterScreenData.ts`, so the simulation coordinator no longer owns the full Adventure Card presentation model.
- Extracted the DOM-heavy HUD, pause menu, Adventure Card, and region-map shell out of `src/render/app/GameApp.ts` into `src/render/app/HudShell.ts`, leaving `GameApp.ts` focused on app flow, view-mode transitions, input gating, and renderer/camera orchestration.
- `npm run build` passes after the coordinator refactor pass.

Foraging loop pickup pass:

- Reworked the existing forageable scaffold from auto-gathered fruit/plants into explicit pouch pickups: seeds, shells, moss tufts, berries, smooth stones, and feathers.
- `E` now gathers the nearest ungathered forageable instead of collecting it automatically just by walking near it.
- Added a nearby forage prompt, pouch state in `render_game_to_text`, and updated gathered-good binder cards to use the new item kinds.
- Added distinct in-world pickup visuals for each forage kind, with bob/sway behavior and disappear-on-gather state.
- `npm run build` passes after this pass.

Riparian forest-pocket pass:

- Kept this automation run scoped to water-bank / forest-transition composition in `src/render/world/terrainDecorations.ts`.
- Added guarded riparian pockets to the existing water-bank accent layer: soft canopy-shadow washes, moss pads, grass clumps, lip pebbles, and lowland/foothill saplings at selected lake, river, and braid transition anchors.
- The new pockets use wetness, slope, and playable-bound checks before placement, so they should decorate bank edges without changing controls, route checkpoints, or water simulation.
- Verification:
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `npm run test:contracts` passes: camera, controls, water-state-agreement, route-checkpoints.
- `git diff --check` passes.
- Required `develop-web-game` Playwright client was attempted against `http://127.0.0.1:4194/`; direct skill-path launch still could not resolve repo-local `playwright`, while the repo-local symlink with `--preserve-symlinks --preserve-symlinks-main` reached Chromium and failed with the existing macOS sandbox `MachPortRendezvousServer ... Permission denied` error.
- The temporary Vite server for this run is still listening on `127.0.0.1:4194` because sandboxed `kill 9710` returned `Operation not permitted`.
- Next visual check: inspect the opening lake rim, Silver Bend bank, Fir Gate braid, and Windstep / ridge river pockets in Safari or Dia before adding more density.

Water-bank / forest threshold polish:

- Added small sedge, moss, pebble, and wash accents along the meadow, fir-gate, and alpine branch channels so side rivers read with the same bank language as the main river and starting pools.
- Added a few pool-rim sedge patches around the opening lake and starting shoals to soften the water-to-grass transition.
- Added subtle canopy-ground shadow patches at authored biome-transition anchors so forest edges feel more grounded and scaled without changing the playable route.
- Verification:
- `npm run build` passes. Vite still emits the existing large chunk warning for the main JS bundle.
- `npm run test:contracts` passes: camera, controls, water-state-agreement, route-checkpoints.
- `git diff --check` passes.
- The required `develop-web-game` Playwright client was attempted against `http://127.0.0.1:4193/`, but Chromium launch failed under the macOS sandbox with `MachPortRendezvousServer ... Permission denied`.
- The temporary Vite server for this run is still listening on `127.0.0.1:4193` because sandboxed `kill 4264` returned `operation not permitted`.

Pouch HUD pass:

- Added a compact live pouch strip to the gameplay HUD that shows gathered forage counts by kind.
- The pouch stays hidden until Mossu is near a forageable or has just gathered one, then lingers briefly after pickup.
- The strip highlights the nearby forageable category even before the count is nonzero, so the player can read what `E` will add to the pouch.
- `render_game_to_text` now includes `pouchCounts` under `characterScreen.gatheredGoods` for lightweight verification.
- `npm run build` and `git diff --check` pass after this pass.

Interactive pouch follow-up:

- Converted pouch count chips into stable interactive buttons instead of passive rebuilt labels.
- Hovering, focusing, or clicking a pouch category now selects it and opens a small detail tray.
- Nearby forageable categories explain the `Press E` action, while gathered categories summarize stored counts and point to the binder.
- `npm run build` and `git diff --check` pass after this pass.

Overnight world polish shoreline/forest-edge pass:

- Added translucent silty shoreline wash patches around the main river and starting-water pools so bank rims have a clearer dry-to-wet read before reeds/pebbles.
- Added a few forest-edge understory patches using existing moss, bush, and grass primitives to make lowland-to-foothill forest transitions feel less abrupt without changing the playable route.
- Kept the pass scoped to `src/render/world/terrainDecorations.ts`.
- Verification: `npm run build`, `npm run test:contracts`, and `git diff --check` pass. Vite still reports the existing large bundle warning.
- Playwright follow-up: the repo-local symlinked `develop-web-game` client still cannot launch Chromium in this sandbox because `MachPortRendezvousServer` registration is denied. The temporary Vite server on `http://127.0.0.1:4190/` could not be killed from this sandbox (`Operation not permitted`) and was still listening at the end of the run.

Overnight pool-rim / forest-scale follow-up:

- Added a subtle procedural bank-lip lift around starting-water pools in `src/simulation/world.ts`, strongest around the opening lake, so pools have a clearer raised dry rim instead of blending straight into flat grass.
- Added narrow pebble-trail lip markers to the main river, branch channels, and starting pools in `src/render/world/terrainDecorations.ts` to make the water-bank edge easier to read from the gameplay camera.
- Added four authored forest scale ramps at lowland and foothill edges, stepping from saplings into larger trees so the forest boundary reads less abrupt without touching route checkpoints or controls.
- Verification:
- `npm run build` passes. Vite still emits the existing large chunk warning for the main JS bundle.
- `npm run test:contracts` passes: camera, controls, water-state-agreement, route-checkpoints.
- `git diff --check` passes.
- Playwright follow-up: the skill client was retried against `http://127.0.0.1:4191/`. The first run hit the known skill-path `playwright` resolution issue; the repo-local symlink with `--preserve-symlinks` reached Chromium but Chromium still failed with `MachPortRendezvousServer ... Permission denied`. The temporary Vite server was stopped cleanly with Ctrl-C.

Overall QA pass:

- No gameplay/world code changes were made in this pass; this was a verification sweep over the current local worktree.
- Static verification passed: `npm run build`, `npm run test:contracts`, and `git diff --check`.
- Code-search sanity check found no conflict markers, debuggers, TODO/FIXME items, or unexpected source errors in `src`, `tests`, `package.json`, or `index.html`.
- Safari smoke verification on `http://127.0.0.1:4196/` rendered the title screen, entered gameplay, collected the nearby forageable with `E`, opened and closed the map with `M` / `Esc`, and opened the handbook with `Tab`.
- Screenshots captured under `output/qa-overall/`: `safari-smoke-2.png`, `safari-after-enter.png`, `safari-after-e.png`, `safari-after-m.png`, `safari-after-esc.png`, and `safari-after-tab.png`.
- Headless Playwright state capture remains unreliable in this macOS sandbox: direct Vite targets were unreachable from sandboxed Chromium, and an in-process static-server probe hung around `advanceTime` before the browser context closed.
- Remaining risks: the existing Vite large chunk warning remains, and the handbook should get a dedicated responsive-height check because the non-fullscreen Safari smoke view showed inner scrolling near the bottom of the panel.

Visual anchor scene polish implementation:

- Added a dedicated `anchor-scene-accents` visual layer for the six review anchors: opening meadow, opening lake shore, Silver Bend, Fir Gate forest edge, highland creek/waterfalls, and shrine approach.
- The new layer uses the existing instanced small-prop batching for clover, flowers, reeds, moss, grass clumps, pebbles, and shrubs, while only adding a few authored mature trees, rock accents, shadows, and side waterfall ribbons.
- Tuned water profiles to reduce milky shoreline glare and sparkle washout while preserving readable shallow/deep bands.
- Updated the visual contract test roots so the new anchor small-prop batch is covered by the black-plant / instance-color checks.
- Verification:
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `git diff --check` passes.
- Required `develop-web-game` client notes: direct skill-path launch still cannot resolve repo-local `playwright`; the repo-local symlink launch with escalated local-network access loaded the built static preview, entered gameplay, and captured `output/anchor-scene-polish/shot-0.png` plus `state-0.json`.
- Headless/desktop visual caveat: real Chrome was opened with the local preview URL, but macOS screen capture returned black/desktop captures instead of a useful Chrome frame. Treat the Playwright gameplay screenshot as the reliable captured artifact for this run and do a human Chrome route review before shipping.
- Temporary Vite and Python preview servers were stopped after QA.

Roll mode / Karu mimic pass:

- Shift rolling is now stamina-free. The stamina meter is reserved for Breeze Float, and the character screen copy now says that explicitly.
- Added simulation state for `rollHoldSeconds` and `rollModeReady`; holding Shift for about 3 seconds now drives a small Roll Mode HUD indicator.
- Roll jump still preserves forward momentum, and holding Space after the roll jump can transition into Breeze Float while consuming stamina only for the float.
- Recruited Karu now tighten their follow slots while Mossu rolls, speed up slightly to keep pace, tuck their feet, bounce, and rotate through a soft rolling mimic pose instead of only hopping behind.
- `render_game_to_text` now reports roll hold and roll-ready state for lightweight QA.
- Future animation plan:
- Phase 1: add Mossu roll-charge anticipation, a tiny dust puff, and a clearer "ready" body squash when Shift is held long enough.
- Phase 2: give Karu staggered mimic delays so the herd rolls in a cute wave instead of all matching the same frame.
- Phase 3: build a roll-jump transition pose: squash, launch stretch, air tuck, then Breeze Float fluff/ear spread.
- Phase 4: add landing recovery feedback for roll landings, slope landings, and roll-to-float landings.
- Verification:
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- Required `develop-web-game` browser smoke was attempted against `http://127.0.0.1:8002/`, but headless Chromium timed out before `domcontentloaded`. A custom Shift-specific Playwright probe was also attempted and hit the same headless initialization problem before `render_game_to_text` became available. The temporary server and hung probe were stopped.

Karu true-roll follow-up:

- Promoted Karu rolling from a visual-only mimic blend into explicit `blob.rolling` state.
- Recruited Karu now enter that state with a tiny per-slot stagger when Mossu is rolling, stay out of roll state while floating or bank-waiting, and use stronger catch-up movement while rolling.
- The Karu roll pose now keys off their own rolling state, and `render_game_to_text` reports `fauna.rollingCount` so QA can tell whether the herd is actually rolling.
- The HUD can briefly surface how many Karu are rolling with Mossu.
- Verification:
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `git diff --check` passes.

Mossu/Karu soft collision pass:

- Added lightweight planar collisions between Mossu and Karu without introducing a full physics engine.
- Karu now use slightly different collision radii for idle, recruited, and rolling states, so rolling herd members feel rounder and less ghost-like.
- Overlaps are resolved mostly by nudging Karu away, with a smaller Mossu nudge while grounded and not swimming; both sides also get a small velocity response when they run into each other.
- The world update order now applies Karu collision before the Mossu avatar pose update, so the visible body follows the adjusted position in the same frame.
- `render_game_to_text` now reports `fauna.mossuCollisionCount` for QA probes.
- Verification:
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `git diff --check` passes before this progress note.
- The required `develop-web-game` client loaded a built static preview on `http://127.0.0.1:8003/`, entered gameplay, and captured `output/karu-collision-smoke/shot-0.png` plus `state-0.json`.

Experimental WebGPU renderer pass:

- Added an async app bootstrap so the normal game path still starts on the existing WebGL renderer while `?renderer=webgpu` or `?webgpu` lazy-loads Three's WebGPU renderer chunk.
- Added renderer backend reporting to `render_game_to_text` and the `?perfDebug=1` overlay: requested backend, active backend, WebGPU browser availability, and fallback reason if WebGPU initialization fails.
- Kept the current EffectComposer bloom on the WebGL path only. WebGPU skips the WebGL postprocessing stack for now, because the current bloom pass is tied to `WebGLRenderer`.
- Added a WebGPU-compatible sky dome fallback. The existing shader sky stays on WebGL, while WebGPU avoids Three's `ShaderMaterial` incompatibility warning during startup.
- Verification:
- `npm run build` passes. Vite now emits the WebGPU renderer as a separate async chunk and still reports the existing large main chunk warning.
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- Browser smoke against the built preview loaded `?renderer=webgpu&perfDebug=1`, entered gameplay, and reported `activeBackend: "webgpu"` with no fallback reason.
- Headless browser caveat: the WebGPU smoke is enough to confirm startup/backend selection, but real Chrome performance and visual parity still need a human pass because Mossu's water/grass/tree animation shaders are still WebGL-style `onBeforeCompile` hooks.

Reference UI + shader polish pass:

- Restyled the live HUD toward the provided storybook/Pokemon/Animal Crossing reference: cream cards, navy outlines, separated top-right status pills, keyboard-card controls, a warmer Roll Mode card, and a small illustrated route map inside the Trail Note.
- Tightened the bottom quick-action labels so they read more like game UI (`Click camera`, `Tab inventory`) instead of dev labels.
- Improved shader/art direction details: grass now uses stronger painterly root/mid/tip bands, water clamps glare/sparkle brightness with more controlled contour lines, and distant/highland tree flutter is damped while broad sway remains.
- Added a modest amount of extra opening-meadow flowers/clover/understory so the first scene reads closer to the lush reference without adding new mesh types.
- Verification:
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- Remaining visual note: the shader polish is still mostly on the WebGL material path. WebGPU startup works from the previous pass, but full visual parity would need porting the custom grass/water/tree shader hooks to WebGPU-compatible node/TSL materials.

Karu little-spirit visual pass:

- Reworked the Karu rig in `src/render/world/ambientBlobs.ts` toward the provided "Little Spirit" reference: lighter cloud-blue fur texture, horizontal fluffy body, larger glossy eyes, rounded snout, tiny nose/mouth, blush cheeks, tail puff, and four paws instead of two front nubs.
- Preserved existing Karu behavior, recruitment, rolling, collision, and herd state; this was a visual rig pass only.
- Updated Karu idle/roll animation scaling so the new body, tail, paws, and fur puffs keep their intended proportions while still bobbing, sniffing, hopping, and tucking during roll mimic.
- Verification:
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- Required `develop-web-game` client ran against `http://127.0.0.1:8005/?spiritCloseup=1` and captured `output/karu-spirit-redesign-final/shot-0.png` plus `state-0.json`. The client still reports the known start-button click timeout, but gameplay state and screenshot are captured after entering the game.

Compact HUD pass:

- Added a compact override in `src/styles.css` so the storybook HUD keeps the same visual style but leaves more gameplay visible.
- Reduced the Trail Note card, top status pills, bottom quick-action buttons, prompt chips, pouch card, pickup card, ability pill, and Roll Mode card by roughly 10-15% depending on component.
- Verification:
- `npm run build` passes. Vite still reports the existing large JS chunk warning.
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `git diff --check` passes.
- Required `develop-web-game` client ran against `http://127.0.0.1:8005/` and captured `output/ui-compact-pass/shot-0.png` plus `state-0.json`. The client still reports the known start-button click timeout, but gameplay state and screenshot are captured after entering the game.

Mossu eye-readability pass:

- Kept the character-face change scoped to eyes only: no nose or mouth marks were added.
- Enlarged Mossu's glossy oval eyes, added small white catchlight meshes, and added a simple periodic blink/squint response that also reacts to landing, rolling, jumping, and Karu-call pulses.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing large chunk warning.
- `git diff --check` passes.
- Required `develop-web-game` client initially failed from its installed path because `playwright` resolved relative to `~/.codex`; rerunning the same client through `.codex-tmp/web_game_playwright_client.js` with `--preserve-symlinks-main` captured `output/mossu-eye-readability/shot-0.png` and `state-0.json` from `http://127.0.0.1:8007/?modelViewer=1`.

Mossu secondary-motion pass:

- Added a small spring layer for Mossu's fluff puffs and top tufts: each keeps its authored base offset, trails behind local movement, compresses/rebounds on landing, lifts on jump/call/swim pulses, and gets a little extra side sway during roll/call states.
- Kept the motion scoped to `MossuAvatar.ts`; no gameplay constants, controls, or model-viewer UI were changed.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing large chunk warning.
- Required `develop-web-game` client captured `output/mossu-secondary-motion/shot-0.png` and `state-0.json` from `http://127.0.0.1:8008/?modelViewer=1`; the attempted normal click on the Roll pose timed out after resolving the button, so the captured artifact is the idle model-viewer state.
- A targeted force-click Roll probe confirmed the roll pose state path before timing out during WebGL screenshot capture; treat the required client screenshot as the reliable visual artifact for this pass.

Model viewer lag fix:

- The model viewer was laggier than gameplay because it capped pixel ratio at 1.7, kept shadow maps enabled, and called full `updateUiState()` DOM queries/writes every render frame.
- Matched the viewer closer to gameplay performance policy by capping pixel ratio at 1.1, disabling model-viewer shadows, and moving timeline scrub updates out of the full per-frame UI refresh path.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing large chunk warning.
- `git diff --check` passes.
- Reopened Dia at `http://127.0.0.1:8008/?modelViewer=1&perf=1` so the browser loads the rebuilt model-viewer chunk.

Model viewer Mossu ground-height fix:

- Fixed Mossu clipping through the model-viewer platform by adding a preview-only ground lift before feeding the synthetic `PlayerState` into `MossuAvatar.update()`.
- Gameplay height/simulation code was left untouched; the issue was specific to the model-viewer preview player being authored at `y = 0`.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing large chunk warning.
- `git diff --check` passes.
- Reopened Dia at `http://127.0.0.1:8008/?modelViewer=1&groundfix=1` so the browser loads the rebuilt model-viewer chunk.

Three.js geometry / shader / lighting focus pass:

- Used the installed `threejs-geometry`, `threejs-shaders`, and `threejs-lighting` project skills as references for a focused scene-readability pass.
- Added a single instanced `terrain-form-strokes` layer that places 96 low-opacity brush/contour ellipses along the authored route to clarify terrain shelves and clearings without many individual prop meshes.
- Tuned the grass shader so gust fronts add a subtle darker wind-combed band plus brighter tip sheen, making wind movement read better without adding instances.
- Raised water sparkle slightly and added a shallow shoreline glow term so water edges and current highlights feel warmer and more painterly while keeping the existing glare clamps.
- Promoted ambient, hemisphere, and sky-bounce lights to mood-controlled fields, then slightly reduced fog density and tuned lowland/highland sun/fill levels for better readability.
- Verification:
- `node node_modules/typescript/bin/tsc --noEmit` passes.
- `/usr/local/bin/node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing large chunk warning.
- `/usr/local/bin/npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- `git diff --check` passes.

Opening sequence + first map/water overhaul pass:

- Added a short visual-only wake-up sequence after the title screen: Mossu starts in the meadow nest, the HUD stays hidden, input is paused, the camera pans from the nest toward the river route, and movement/jump/interact/menu inputs can skip after the first beat.
- Staged nearby and distant Karu groups around the opening meadow so the first view establishes the herd fantasy instead of relying on random scatter.
- Added an opening shoreline composition layer with damp painted ground patches, sand/wet-sand rims, reeds, reed tips, stepping stones, and small bank accents around the starting pools.
- Tightened starting water profiles and shader clamps so the lowland pools read less like pale translucent slabs: reduced opacity/glare/milk, controlled shallow/deep bands, and softened shoreline edge brightness.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing async chunk warning.
- `npm run test:contracts` passes: camera, controls, habitats, movement, visuals, water-state-agreement, route-checkpoints.
- Required `develop-web-game` client captured `output/opening-water-sequence-smoke-2/shot-0.png` and `state-0.json`; the screenshot shows the wake-up overlay, Mossu, staged Karu, and start-area water/shoreline accents. A custom no-screenshot Playwright state probe was inconclusive in this environment because headless startup did not expose `window.render_game_to_text()` before timeout, so the game-client artifact remains the reliable visual QA result for this pass.

Dual UI click sound pass:

- Added `public/audio/destiny-ui-click.mp3` from the provided download while keeping the existing `menu-ui-click.mp3`.
- Reworked `InterfaceAudio` to use two UI click variants with a small per-variant audio pool, alternating between the original Mossu click and the new Destiny-like click so repeated UI interactions feel less identical.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing async chunk warning.
- Contract checks passed via the local tooling path after `npm` was unavailable on this shell PATH: `tsc -p tsconfig.contracts.json`, `esbuild tests/contracts/runContracts.ts ...`, and `/tmp/mossu-node .contract-test-build/runContracts.mjs`.
- Required `develop-web-game` client captured `output/ui-click-dual-sound/shot-0.png` and `state-0.json` from the built app; server logs confirmed both `/audio/menu-ui-click.mp3` and `/audio/destiny-ui-click.mp3` were requested.

Water ambience audio pass:

- Added `public/audio/water-river-loop.mp3` from the provided river sound.
- Added `sampleWaterAmbience()` in `src/simulation/world.ts` so water audio proximity is driven by the actual world water resources: main river, branch rivers, highland creek paths, and starting pools.
- Added `AmbientWaterAudio` with a looping river bed that unlocks on the title-screen Play gesture, fades in only near water, fades out away from water, and reports proximity/source state through `render_game_to_text()`.
- Added water ambience contract coverage for river centers, starting pools, a highland creek, and a far dry meadow.
- Verification:
- `/tmp/mossu-node node_modules/typescript/bin/tsc --noEmit` passes.
- `/tmp/mossu-node node_modules/vite/bin/vite.js build` passes. Vite still reports the existing async chunk warning.
- Contract checks passed via local tooling: `tsc -p tsconfig.contracts.json`, `esbuild tests/contracts/runContracts.ts ...`, and `/tmp/mossu-node .contract-test-build/runContracts.mjs`.
- Required `develop-web-game` client captured `output/water-ambient-audio/shot-0.png` and `state-0.json`; state showed `audio.waterAmbience.active: true`, `proximity: 1`, and server logs confirmed `/audio/water-river-loop.mp3` was requested after pressing Play.

Breeze Float animation polish pass:

- Added an explicit `player.floating` runtime state, fed by the existing Breeze Float movement result, reset on void/respawn, and exposed through `render_game_to_text()` for QA.
- Upgraded `MossuAvatar` so Breeze Float now has its own blend: hover lift, fluff and top-tuft spread, softer body stretch, brighter alert eyes, tucked legs, and a small landing recovery after ending the float.
- Updated the model viewer Glide pose to drive the same runtime float state, so the workshop pose now previews the gameplay silhouette instead of only using generic airborne movement.
- Added a light Karu support reaction: recruited Karu stop roll-mimicking while Mossu is floating and use a small breeze-hop/fluff response nearby without changing their follow or roll rules.
- Verification:
- Contract checks passed via local tooling because `npm` is unavailable on this shell PATH: `tsc -p tsconfig.contracts.json`, `esbuild tests/contracts/runContracts.ts ...`, and the bundled Node runtime running `.contract-test-build/runContracts.mjs`.
- `tsc --noEmit` passes.
- `vite build` passes and still emits the existing async chunk warning.
- `git diff --check` passes.
- Browser QA caveat: Vite preview still fails with `ENOSPC` while writing its temp config, the required `develop-web-game` client still cannot resolve Playwright from its skill folder, and custom headless Chrome probes remained unstable in this environment. The deterministic contract now verifies the new floating state; final visual judgment should be done in real Chrome.

Floating island shell read pass (WorldRenderer `buildFloatingIslandShell`):

- Named group `floating-island-shell`; split lower mass into upper taper + darker lower taper + cool underbelly sphere; rim torus lip; three horizontal mist discs under the break; ten small downward cones for hanging rock read; cliff bulges nudged outward; moss band slightly thicker with subtle emissive.
- Verification: `npm run qa` passed.

Ghibli-style tree pass (`terrainDecorations`):

- Instanced round forest: more canopy blobs, puff clusters, roots/branches, blossom reads; pine stack split into rotated tiers + top hip sphere; shared wind material gains `vFoliage` sun-tint, soft light lift, stronger sway/gust, cache key `mossu-instanced-tree-wind-ghibli`.
- `makeRoundTree` / `makePineTree` / saplings aligned with the same design language; landmark trees: extra lobes, emissive leaves, slightly softer trunk, more sphere segments.
- Verification: `npm run qa` passed.

Animation curves + environment response pass:

- Added reusable motion curves in `src/render/motionCurves.ts` and routed Mossu's movement pulses through ease-out/back/elastic curves so roll start/end, jump, landing, swim, and Breeze Float reactions stop feeling linear.
- Curved major camera blends: opening vista handoff, map lookdown blend, idle orbit distance/polar movement, and landing/roll settle now use smoother easing on top of the existing damping.
- Gave unrecruited Karu a small first-encounter look-at blend so they pause and turn toward Mossu instead of snapping from idle motion.
- Added a cheap scene-response pulse in `WorldRenderer` that lightly lifts fog/light/glow on landing, rolling movement, and first Karu beats without adding post FX or extra geometry.
- Expanded the CSS cinematic motion layer across map steps, binder/card entries, gathered goods, pouch items, tabs, buttons, and pickup cards with reduced-motion fallbacks.
- Verification:
- `git diff --check` passes.
- Direct QA equivalent passes through the available local tooling: `tsc --noEmit`, `tsc -p tsconfig.contracts.json`, contract bundling with esbuild, contract runner, and production `vite build`. Vite still reports the existing large async chunk warning.
- Perf baseline attempt wrote `output/perf-guard/animation-env-baseline.json` with 114.7fps average / 16.8ms route p95 and all 12 route checkpoints reached, but the run used `--skip-screenshots`, so visual contrast/chroma/nonblank checks failed as zero and a few checkpoints exceeded the 20ms p95 checkpoint budget. Candidate perf comparison could not run in this session because the required escalated browser action was rejected by the local usage/approval limit.
- Browser smoke is still pending: the required `develop-web-game` client resolved after symlinking the skill script into `.codex-tmp`, but the old dev server was gone and starting a new local Vite server failed under sandbox networking with `listen EPERM`.

Cinematic co-op + Spore UI/perf guard pass:

- Added `?coopStress=1` shared-world stress coverage with remote Mossu avatars, remote grass/water wakes, and shared collection events surfaced through the debug/perf payloads.
- Added the Spore-like organic membrane UI skin across the title, HUD, map, binder/card, popup, and menu surfaces; the title now uses the ambient green/blue blurred background, cream/gold plaque, three orbs, and `Mossu`-only logo treatment.
- Tightened the black-flash guard path with explicit renderer/backend debug state and render-path suppression logging, then softened the terrain dark palette without adding shader cost.
- Made the perf guard the baseline/candidate workflow by adding explicit startup/per-step warmups, `--url-param`, and `npm run perf:guard:coop`; trimmed grass budgets and adaptive pixel floor for the 60fps target.
- Verification:
- `npm run qa` passes.
- `npm run test:e2e:smoke` passes.
- `git diff --check` passes.
- `node --check scripts/perfGuard.mjs` passes.
- `npm run perf:guard` passes at 87.6fps average / 15.7ms p95 with all 12 route checkpoints, screenshots, contrast, chroma, and nonblank checks green.
- `npm run perf:guard:coop` passes at 102fps average / 15.3ms p95 with all 12 route checkpoints, screenshots, contrast, chroma, and nonblank checks green.
- Visual captures are under `output/ui-spore-check/` for title, gameplay, and map; direct binder screenshot capture stayed flaky, but the binder/card UI is covered by the CSS pass plus the normal QA/smoke route.

Mossback Titan creature pass:

- Shared the detailed Mossback Titan rig between the model viewer and in-world wandering threat, replacing the older duplicate world mesh path.
- Restyled the Titan toward the living-island reference: smaller raised meadow back, trees, crystal peaks, pond/waterfall, visible lower jaw teeth, claws, and a clearer head-forward silhouette.
- Added stronger rig animation states for mouth opening, jaw drop, body breathing, leg stepping/lift, eye/crystal alert pulses, and threat poses (`idle`, `sniff`, `roll`, `hop`, `rest`).
- Verification: `npm run qa` passes; `git diff --check` passes; model viewer render capture passes at `output/mossback-titan/model-viewer-titan-island.png`.

Tiny refactor pass:

- Cleared the current ESLint warning set by removing unused imports/parameters, prefixing parked helper builders, and eliminating one unused material binding without changing gameplay behavior.
- Moved `GameApp`'s static runtime constants, paused input snapshot, debug save payload type, quality flag helper, control-activity helper, and perf-number formatter into `src/render/app/appRuntimeConfig.ts`.
- Extracted `WorldRenderer.syncMapLookdownVisibility()` so the main world update path no longer owns the map-mode visibility toggle block inline.
- Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny perf/debug refactor pass:

- Added `src/render/app/appPerformance.ts` for perf capture constants, rolling frame sample helpers, performance snapshot shaping, perf-capture payload creation, and compact/full perf debug panel text.
- Kept `GameApp` responsible for collecting renderer/world/camera state and debug hotkey timing, but removed inline perf panel formatting and percentile/sample buffer logic from the class body.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny title/opening DOM refactor pass:

- Added `src/render/app/appTitleScreen.ts` for the title-screen DOM and opening-sequence overlay DOM construction.
- Kept `GameApp` responsible for title state, Play/start transitions, keyboard handling, opening-sequence progress, pointer focus, audio unlock, and postprocessing suppression.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny save-state refactor pass:

- Added `src/render/app/appSaveState.ts` for local save runtime state, restore/persist signature checks, stored payload application, and local-storage writes.
- Kept `GameApp` responsible for deciding when persistence is enabled, applying the paused update after restore, and triggering save persistence from debug replay and frame ticks.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny text-state refactor pass:

- Added `src/render/app/appTextState.ts` for the compact e2e and full `render_game_to_text` JSON serializers, including player, save, Titan, co-op, character-screen, fauna, audio, performance, water debug, and QA payload shaping.
- Kept `GameApp` responsible for collecting live snapshots from state, camera, audio, world, performance, and overlay timing, then passing them into the serializer.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny map-drag refactor pass:

- Added `src/render/app/appMapDrag.ts` for map pointer capture, drag delta tracking, release, and cancel behavior.
- Kept `GameApp` responsible for map-mode gating, wheel zoom, and passing drag deltas into `FollowCamera.panMapViewFromDrag()`.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny debug-replay refactor pass:

- Added `src/render/app/appDebugReplay.ts` for QA/debug player teleport, replay-position application, player movement reset, and debug save payload application.
- Kept `GameApp` responsible for camera snapping, perf-debug world flushing, Titan reset placement, state update/persistence, and HUD sync after debug replay.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny postprocessing-runtime refactor pass:

- Added `src/render/app/appPostProcessing.ts` for postprocessing runtime state, schedule gating, suppression timing, render-path transitions, retro-texture uniform updates, and compact suppressed-ms calculation.
- Kept `GameApp` responsible for composer/pass construction, renderer/composer rendering, context-loss event handling, and render-path logging payloads.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny render-quality refactor pass:

- Added `src/render/app/appRenderQuality.ts` for adaptive pixel-ratio sampling, frame/sample accumulation, and the latest quality sample frame time.
- Kept `GameApp` responsible for applying the chosen pixel ratio to the renderer, composer, and underwater overlay.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Tiny underwater-effect refactor pass:

- Added `src/render/app/appUnderwaterEffect.ts` for camera/player underwater target-intensity calculation.
- Kept `GameApp` responsible for ticking the `UnderwaterEffect` instance with `dt`, `elapsed`, and the computed target intensity.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Vite still reports the existing large Three.js chunk warning during production build.

Preferred render-resolution policy pass:

- Made Mossu's preferred render resolution explicit as an internal pixel budget instead of a fixed browser window size: normal play targets roughly `1600x900`, while low-quality/stress paths target roughly `1280x720`.
- `appRenderQuality.ts` now computes min/max/initial DPR from viewport size, device DPR, quality mode, and the preferred pixel budget, so 1440p/4K displays stop rendering unnecessary extra internal pixels while still filling the whole browser window.
- Resize now refreshes the render-resolution policy and clamps active DPR immediately if the viewport changes; the adaptive scaler can still step down/up inside the current policy.
- Perf snapshots/HUD now include preferred, viewport, and internal render resolution details, and a new `render-quality` contract protects the policy for normal, 4K, and low-quality cases.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, and `git diff --check` pass. The first non-escalated visual guard attempt hit the known local preview `listen EPERM` sandbox failure on `127.0.0.1:4173`.

UI readability scale follow-up:

- Raised the late HUD micro-scale values in `src/styles.css` a notch so the status strip, prompts, quick actions, stamina/roll widgets, pouch HUD, pickup card, and small metadata read less tiny while preserving the compact field-gadget layout.
- Follow-up screenshot review showed the broad layout fit, but the top status labels and quick-action captions were still too fine; bumped those text/tap-target values one more step while keeping the HUD compact.
- Mobile HUD overrides were bumped proportionally so narrow screens keep the same hierarchy without shrinking the text back to the previous too-small values.
- Cleared a pre-existing lint failure in `scripts/perfGuard.mjs` by removing the unused `runMeasuredSegment()` helper after confirming the guard now only calls the render-aware segment runner.
- Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, and `git diff --check` pass. Rebuilt Vite preview screenshots passed at `output/ui-scale-check/desktop-hud.png` and `output/ui-scale-check/mobile-hud.png`; the first custom static-server probe was discarded because it left the app on the instant loading shell instead of matching Vite preview behavior.

Weekly wiki sync + Karu held-recruitment pass:

- Created the local weekly `Mossu weekly wiki sync` cron automation so repo docs/progress are compared against `/Users/hunterbastian/wiki` Mossu pages every Monday after larger passes.
- Changed Karu recruitment from tap/cluster pickup to held individual invitation: tap `E` remains normal interaction, holding `E` near an unrecruited Karu recruits only the nearest individual, and holding from too far away does not pull in the rest of the pocket.
- Tightened the Karu invite radius and updated HUD/control copy to teach `Hold E Invite Karu`; existing held `E` call/regroup behavior remains available when no unrecruited Karu is nearby.
- Added smoke coverage for tap-vs-hold recruitment, one-at-a-time behavior, and walking up to the next Karu before another recruit.
- Updated docs to replace stale cluster-recruitment and unsaved-state notes across `docs/GAME_MEMORY.md`, `docs/TECHNICAL_OVERVIEW.md`, `docs/REDESIGN_ROADMAP.md`, `docs/SYSTEMS_AUDIT.md`, `docs/PLAYTEST_CHECKLIST.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_PASSES.md`, `docs/README.md`, and `AGENTS.md`.
- Added lightweight fauna stats to the compact `?e2e=1` text payload so browser probes can verify Karu counts without loading the full debug JSON.
- Verification: `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, the required develop-web-game client pass (`output/karu-hold-route/shot-0.png`, `shot-1.png`), and the focused Karu route probe (`output/karu-route-probe/summary.json`) pass. The first custom Playwright probe hit the local Chromium MachPort sandbox denial and passed when rerun with approved unsandboxed browser launch.
- Playtest notes: the probe confirms tap `E` does not recruit, held `E` recruits exactly one Karu, recruited count stays stable across opening/narrow, slope-bank, shallow-water, Highland Basin, and Ridge Saddle debug checkpoints, and no browser console errors were reported. This is a focused automated probe; a full real-browser manual route walk with followers physically traversing from Burrow to Moss Crown is still worth doing before companion polish expands.

Karu join focus + art review route pass:

- Added a clear Karu recruitment beat: when a Karu joins, the HUD shows one small `Karu joined Mossu's trail` prompt, softens the surrounding HUD for roughly two seconds, hides noisy controls/status chips during that beat, and gives the recruited Karu a lightweight hop/glow pulse.
- Hardened the join HUD timer with a small minimum-frame guard so slow frames or screenshot capture cannot skip the prompt entirely.
- Added `scripts/artReviewRoute.mjs` and `npm run art:review`; the script builds/serves the production bundle, opens headed Chrome, uses `mossuDebug.jumpTo()` for named route checkpoints, and writes screenshots/JSON to `output/art-review-route/`.
- Updated repo docs and `AGENTS.md` so future visual passes use the new art-review route alongside `npm run qa`, smoke tests, and perf guards.
- Verification: `node --check scripts/artReviewRoute.mjs`, `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, `git diff --check`, and headed Chrome `node scripts/artReviewRoute.mjs --headed --browser=chrome` pass. The latest art-review summary reports no fatal browser console errors, and the current production build no longer emits the old large Three chunk warning.

Movement-state polish + grass/path interaction pass:

- Smoothed transitions between walk, roll, jump, landing, float, swim, and wade without adding new mechanics: roll release now keeps a short carry timer, wade slowdown blends in/out, float release eases back into full gravity, and swim entry preserves a small amount of incoming planar momentum.
- Added extra protection for the rough edge cases found in browser testing: roll release on slopes keeps projection speed briefly, airborne roll release keeps a bounded carry instead of snapping to normal air speed, and roll-to-water handoffs let the swim clamp honor the same short carry window.
- Tuned Mossu's pressed-grass/path traces to appear more reliably on readable route bands and shoulders, last a little longer near route edges, and stay within the existing 34-stamp instanced budget.
- Expanded movement contracts for roll-release carry, airborne roll release, gradual wade entry, wading roll release, float-exit gravity, and swim-entry momentum.
- Focused browser probe output: `output/movement-state-probe/summary.json` reports walk `9.36`, roll `26.89`, roll-release `22.20`, Breeze Float active after jump, `7/34` active Mossu trace stamps, valid Fir Gate wading transition, and no fatal console errors. Screenshots landed at `output/movement-state-probe/movement-route-traces.png` and `output/movement-state-probe/fir-gate-water-transition.png`.
- Verification: `npm run test:contracts`, `npm run lint`, `npm run build`, `npm run qa`, `npm run test:e2e:smoke`, headed Chrome `npm run perf:guard`, headed Chrome `node scripts/artReviewRoute.mjs --headed --browser=chrome`, and `git diff --check` pass. `npm run perf:guard` reported route avg `139fps`, p95 `9.2ms`, and all `12/12` route checkpoints/stamps passing.

Agent health + baseline checkpoint prep:

- Added a local `CLAUDE.md` entry point so direct Claude Code sessions read `AGENTS.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_PASSES.md`, and `docs/GAME_MEMORY.md` before making Mossu changes.
- Tightened `.prettierignore` for generated DeepSec outputs and vendored `.claude/skills` files, then ran the repo formatter so `npm run format:check:all` has a clean baseline.
- Updated CI to run `npm run lint` and `git diff --check` alongside `npm run qa`, matching the documented local shippable bar more closely.
- Removed the completed checkpoint item from `docs/NEXT_PASSES.md` and recorded the local Claude routing pointer in `docs/CURRENT_STATE.md`.
- Verification: `npm run format`, followed by the full verification gate before commit.

Populated handbook laptop-density pass:

- Used the `handbook-populated` debug save preset to open the field guide at `1366x768` and `1280x720` production-build viewports.
- Initial captures showed the field-guide shell fit the viewport, but the populated card content was too vertically oversized: route cards only partially filled the visible area and the pouch section was pushed down.
- Added a laptop-height-only density rule in `src/styles/handbook.css` for the character screen: tighter shell padding, smaller left-page preview, denser stats/cards, smaller inventory art chips, and corrected binder-spine positioning so it stays in the page gutter.
- Rechecked screenshots at `output/handbook-playtest/laptop.png` and `output/handbook-playtest/compact-laptop.png`; route traits now show a full two-by-two card group above the fold, pouch samples are visible, and the browser reported no fatal console errors.
- Verification so far: `npm run build` passed after the CSS change. The focused probe required approval for the known local static-server `listen EPERM` sandbox restriction.

Karu route follow + Moss Crown destination silhouette pass:

- Fixed the main Karu route-follow failure mode: recruited Karu could be restored from save/debug state while their meshes stayed near their original Burrow pocket, so route jumps and long climbs could leave HUD-visible followers hundreds of meters behind.
- Stabilized recruited follow behavior with damped per-follower heading, short-lived dry-bank hold targets for non-brave deep-water edges, stronger far catch-up, lost-follower rehome slots, and smoother recruited vertical settling after large terrain/water changes.
- Added follower telemetry to `render_game_to_text` plus `npm run karu:route` / `scripts/karuRouteProbe.mjs`, which checks three recruited Karu from Burrow-side terrain through slope banks, shallow water, highland slope, narrow ridge turns, and Moss Crown.
- Strengthened the climb promise with brighter near/far destination mountain layers and a taller Moss Crown shrine silhouette with crown spires, moss caps, a moss ring, and a subtle glow disc.
- Updated `docs/CURRENT_STATE.md`, `docs/NEXT_PASSES.md`, `docs/GAME_MEMORY.md`, and `docs/KNOWN_ISSUES.md` so future work treats Karu drift protection and the first destination-silhouette pass as current state, not untouched queue items.
- Verification: `npm run format:check:all`, `npm run lint`, `npm run qa`, escalated `npm run karu:route`, `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, and `git diff --check` pass. The route probe reports no follower failures; the Moss Crown checkpoint keeps all three recruited Karu under the 30m route threshold.
- Browser/art caveat: `npm run art:review` built successfully, but headed Chrome route capture did not produce fresh artifacts in this Codex desktop session because the page/browser closed or screenshot capture timed out. Do not treat the older `output/art-review-route/` screenshots as current evidence for this pass; a manual real-browser art judgement is still the next visual risk.

Full test sweep after Chrome enablement:

- Re-ran the current Mossu verification bar on the dirty checkout: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, escalated `npm run karu:route`, `npm run art:compare`, and `git diff --check` all pass.
- Added the heavier rendering checks because the checkout includes camera, water, world, HUD, and e2e changes: escalated `npm run test:e2e:visual`, direct `node scripts/artReviewRoute.mjs --headless --browser chromium --deterministic-step`, `npm run art:compare`, and headed `npm run perf:guard` pass.
- Real Chrome plugin pass against the production build reached the title screen, gameplay, field guide, 3D island map, and pause/settings surfaces. Visual sanity looked playable at Burrow Hollow, with the field guide and map fitting the viewport; Chrome console only showed the common extension-style async response message during the pass.
- Fresh artifacts: `output/karu-route-probe/summary.json`, `output/art-review-route/summary.json`, and `output/perf-guard/latest.json`. Perf guard reports `12/12` route checkpoints, route avg `133fps`, p95 `10.1ms`, p99 `11.9ms`, `1186` renderer calls, `439008` tris, pixel ratio `0.98`, and bloom on.
- Harness caveat: `npm run art:review` itself was flaky in this desktop session. The headed Chrome npm wrapper first closed before captures, the npm headless retry exposed a transient stale-asset 404, and the direct script path completed only after deterministic stepping fell back to real-time. Treat the captured route screenshots as fresh evidence, but keep the art-review wrapper reliability on the watchlist.

Karu profile celebration pass:

- Added saved Karu companion profiles from `recruitedKaruIds`, with stable names, mood traits, and profile details aligned with the existing Karu mood ids.
- Added a cute join companion-card moment: after held recruitment, the HUD shows a small `profile unlocked` card with the Karu mood, friend index, trait, and a hint to open the field guide.
- Added a top-of-column `Karu friends` field-guide section so recruited Karu remain visible after the transient join prompt fades; the section shows an empty waiting state before recruitment and saved friend cards afterward.
- Added the recruited Karu profile payload to the full text-state bridge and smoke coverage for join-card visibility plus the saved field-guide card.
- Verification: `npm run lint`, `npm run qa`, escalated `npm run test:e2e:smoke`, escalated `npm run test:e2e:visual`, `npm run art:compare`, escalated `npm run karu:route`, and `git diff --check` pass. The non-escalated smoke and Karu route runs hit the known `listen EPERM` local preview sandbox issue before passing with approved local-server permissions.
- Visual artifacts: `output/karu-profile-review/join-card-element-final.png` captures the new companion-card moment, and `output/karu-profile-review/field-guide-karu-card-final.png` captures the Karu friends section visible near the top of the field guide. Deterministic Playwright page screenshots needed title/HUD opacity overrides because CSS transitions can freeze in the synthetic `advanceTime` path; the real Dia browser reached the live Mossu title and gameplay handoff normally.

Cozy living-nest warmth pass:

- Pushed the Burrow Hollow starter nest further toward a cozy living cradle: a faint warm hearth glow sits inside the moss cup, soft blanket-like lining petals fill the bed, and tiny seed-lamp sprouts warm the rim without blocking Mossu or the exit path.
- Added matching subtle warmth to Karu habitats with low-opacity glow discs and tiny comfort seeds behind each creature-scale bedding spot. The habitats stay readable but remain secondary to the Karu.
- Kept the pass procedural and static through the existing Three.js set-piece builders; no new assets, loaders, save data, gameplay rules, or runtime texture dependencies were added.
- Verification: `npm run lint`, `npm run qa`, escalated full `npm run test:e2e:smoke`, escalated `npm run karu:route`, deterministic `node scripts/artReviewRoute.mjs --headless --browser=chromium --deterministic-step`, `npm run art:compare`, headed `npm run perf:guard`, and `git diff --check` pass.
- Visual artifacts: `output/art-review-route/00-burrow-hollow.png`, `output/art-review-route/01-karu-join.png`, `mossu-cozy-burrow-hollow.png`, and `mossu-cozy-karu-join.png`. The Playwright browser visual pass confirmed the new cozy details are visible around the nest rims and Karu bedding; the first Karu review angle can still drift behind large tree trunks and should be treated as framing triage, not a nest regression.
- Harness caveat: the headed `npm run art:review` wrapper reached gameplay but Chrome closed during the first screenshot capture in this Codex desktop session. The deterministic headless artifact path completed 6/6 captures after falling back from a timed `advanceTime(450, true)` call to real-time waits; `npm run art:compare` accepted the completed summary with that warning.

Floating-island debug atlas pass:

- Added `?islandViewer=1`, a debug-only `IslandViewerApp` chunk for orbiting the entire floating island outside Mossu's normal player camera.
- The atlas reuses the real generated terrain mesh, water system, highland waterways, floating island shell, route landmarks, map POIs, river branches, and island boundary sampler so it reflects the current terrain contract instead of a separate sketch.
- Added overview/top/profile presets, drag-to-orbit, shift/right-drag panning, wheel zoom, landmark focus buttons, route/river/edge guide lines, landmark pins, and a profile angle that shows the island shell over the ocean below.
- Extended the runtime bridge mode union and smoke coverage so `/?islandViewer=1&e2e=1` reports `mode: "island_viewer"` with landmark and terrain counts.
- Updated repo docs to list the route in `docs/DEVELOPMENT.md`, `docs/TECHNICAL_OVERVIEW.md`, `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, and `docs/NEXT_PASSES.md`.
- Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, `npm run perf:guard`, escalated `npm run karu:route`, escalated `npm run test:e2e:visual`, deterministic art-review fallback, `npm run art:compare`, and `git diff --check` pass after the route addition. `npm run perf:guard` reports `129.8fps` average / `13.7ms` p95 with all `12/12` checkpoints reached. A Playwright browser pass at `http://127.0.0.1:8000/?islandViewer=1` loaded the atlas and captured `output/island-viewer/overview.png` plus `output/island-viewer/profile.png`.
- Verification caveat: the headed `npm run art:review` wrapper hit a `render_game_to_text` timeout after entering gameplay and starting capture. The direct deterministic fallback `node scripts/artReviewRoute.mjs --headless --browser=chromium --deterministic-step` completed `6/6` captures after real-time/canvas-capture retry fallbacks, and `npm run art:compare` accepted the resulting summary.
- Next risk: the atlas is a planning tool, not an authored route composition fix. Use it to choose specific Burrow/Amber/Skyward overlooks before adding more terrain or waterfall density.

Floating-island atlas planning controls:

- Added an `Under` preset to the island atlas so the underbelly/ocean relationship can be inspected without dragging the camera below the route by hand.
- Added atlas layer toggles for terrain, shell, ocean, water, guide lines, landmark pins, and waterfall pins; the toggles are debug-view only and do not change gameplay rendering.
- Mirrored the floating-shell waterfall placement into small atlas waterfall pins, making the current cliff/waterfall distribution visible even when the shell or terrain layer is toggled; the latest island iteration raises this shared count to 15.
- Extended the atlas text bridge and smoke assertions with visible layer and waterfall marker counts so the planning affordances stay covered by `/?islandViewer=1&e2e=1`.
- Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke`, and a real browser check at `http://127.0.0.1:8000/?islandViewer=1&e2e=1` pass after the planning-control addition. The browser payload confirmed `viewPreset: "under"`, `11` waterfall markers, and visible layers updating after toggling terrain off. Screenshot artifact: `output/island-viewer/under-layers.png`.

Debug viewer UI and atlas fly mode:

- Restyled the island atlas and 3D model viewer toward a neutral Vercel/Linear-inspired debug tool language: white surfaces, thin gray borders, compact typography, mono labels/readouts, restrained shadows, and a subtle purple active state.
- Expanded the island atlas zoom-out ceiling from `1750m` to `3200m`, with wider overview/top defaults so the whole floating island is easier to read as a planning object.
- Added a `Fly WASD` atlas mode: clicking the button enters free-fly navigation, mouse drag turns the camera, `W/A/S/D` moves forward/left/back/right, `Q/E` move down/up, `Shift` speeds up, and wheel nudges along the fly direction.
- Extended the atlas text bridge and smoke test to assert `maxDistance >= 3200`, enter fly mode, press `W`, advance time, and verify the camera target moves.
- Verification: `npm run lint`, `npm run qa`, `npm run test:e2e:smoke` (8/8), and `git diff --check` pass. Playwright browser checks at `http://127.0.0.1:8000/?islandViewer=1&e2e=1` and `http://127.0.0.1:8000/?modelViewer=karu&e2e=1` passed; the atlas browser probe confirmed `flyMode: true`, `maxDistance: 3200`, and a WASD fly step moving the camera target by `26.25m`, while the model-viewer probe confirmed `selectedModel: "karu"` and the neutral panel styling. Browser snapshots: `output/debug-viewers/island-viewer-linear-fly-snapshot.md` and `output/debug-viewers/model-viewer-linear-snapshot.md`. Playwright page screenshot capture still timed out on the active WebGL views, so snapshots/state probes remain the reliable browser artifact path for this tooling route.

Ghibli/anime shader pass:

- Reworked the active render treatment toward a warmer Ghibli/anime read: the final color grade now pushes cream highlights, cooler teal-green shadows, modest value bands, subtle paper grain, and restrained peach horizon haze without replacing the existing post stack.
- Tuned scene lighting, sky, atmosphere, terrain, grass, river water, and ocean shaders/materials together so the island reads less gray and more hand-painted: cleaner saturated blue sky, cream/cyan horizon, drifting cloud-band hints, watercolor terrain patches, richer blue-green water depth, shoreline ink/milk, warm shimmer, and distance-graded ocean color.
- Kept the Sildur-inspired part focused on warmth, bloom/glints, and reflective water energy rather than heavy gloss: bloom is slightly stronger but still restrained, and the grade stays contract-friendly for route readability.
- Fixed the only remaining perf-guard blocker from the stronger grade: Fir Gate and Whisper Pass foreground pines were timing-clean but visually counted as blank because the near-camera silhouettes fell almost pure black. Added a narrow dark-evergreen pine leaf/trunk material floor and moved the hand-tree shader tint/floor hook onto Three's active `opaque_fragment` chunk.
- Updated `docs/CURRENT_STATE.md`, `docs/GAME_MEMORY.md`, `docs/NEXT_PASSES.md`, and `docs/KNOWN_ISSUES.md` so the shader direction, tree-silhouette caveat, and next manual beauty check are current.
- Verification: `npm run lint`, `npm run qa`, escalated `npm run test:e2e:visual` (the non-escalated run hit the known `listen EPERM` preview bind), escalated `npm run test:e2e:smoke` (8/8), escalated `npm run karu:route` (the non-escalated run hit the same local-bind sandbox issue), headed `npm run perf:guard`, headed `npm run art:review`, and `npm run art:compare` pass. `npm run perf:guard` reports `105.2fps` average / `10.7ms` p95 / `11.2ms` p99, `1611` renderer calls, `469567` tris, pixel ratio `0.98`, bloom on, and all `12/12` checkpoints reached. `npm run art:review` completed `6/6` captures after recoverable canvas retry warnings, and `npm run art:compare` accepted the route summary.
- Artifacts: `output/perf-guard/latest.json`, `output/perf-guard/route/05-fir-gate-forest-grove.png`, `output/perf-guard/route/06-whisper-pass-climb.png`, and `output/art-review-route/summary.json`. Next risk is manual taste, not the automated bar: walk the route in a real desktop browser and tune only if close pines feel too dark, water too saturated, or haze too warm.
