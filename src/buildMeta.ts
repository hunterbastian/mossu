/** Injected at compile time via Vite `define` (see vite.config.ts). */
export const MOSSU_APP_VERSION = __MOSSU_VERSION__;
export const MOSSU_BUILD_TIME = __MOSSU_BUILD_TIME__;
export const MOSSU_SUPPORT_URL = __MOSSU_SUPPORT_URL__;
